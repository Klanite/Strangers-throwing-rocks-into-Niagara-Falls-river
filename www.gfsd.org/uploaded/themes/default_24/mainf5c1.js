buildinfo={buildname:"fs-webpack-build",ver:"7.1.2",template:"",FED:"Geledia",PM:"Jamie M Parker",Designer:"Jon Kirgis"},function(){var __webpack_modules__={233:function(module,exports){eval("var __WEBPACK_AMD_DEFINE_ARRAY__, __WEBPACK_AMD_DEFINE_RESULT__;/*\n * Copyright Martin Auswöger\n *\n * For the full copyright and license information, please view the LICENSE\n * file that was distributed with this source code.\n */\n\n(function(\n\twindow,\n\tdocument,\n\t/*eslint-disable no-shadow-restricted-names*/\n\tundefined\n\t/*eslint-enable no-shadow-restricted-names*/\n) {\n'use strict';\n\n(function (factory) {\n\t/*global define*/\n\t/* istanbul ignore next: don’t cover module definition */\n\tif (true) {\n\t\t!(__WEBPACK_AMD_DEFINE_ARRAY__ = [], __WEBPACK_AMD_DEFINE_RESULT__ = (function() {\n\t\t\treturn factory;\n\t\t}).apply(exports, __WEBPACK_AMD_DEFINE_ARRAY__),\n\t\t__WEBPACK_AMD_DEFINE_RESULT__ !== undefined && (module.exports = __WEBPACK_AMD_DEFINE_RESULT__));\n\t}\n\t/*global module*/\n\telse {}\n}(function(config) {\n\nconfig = config || {};\n\n// Public API\n/*eslint-disable dot-notation*/\nvar api = {\n\t'reprocess': reprocess,\n\t'reparse': reparse,\n\t'reevaluate': reevaluate,\n\t'config': config,\n};\n/*eslint-enable dot-notation*/\n\nvar REGEXP_ESCAPE_REGEXP = /[.?*+^$[\\]\\\\(){}|-]/g;\nvar SELECTOR_REGEXP = /\\.?:container\\((?:[^()]+|\\([^()]*\\))+\\)/gi;\nvar SELECTOR_ESCAPED_REGEXP = /\\.\\\\:container\\\\\\(((?:[^()]+?|\\\\\\([^()]*\\\\\\))+?)\\\\\\)|\\[data-cq~=[\"']((?:\\\\.|[^\"'])+)[\"']\\]/gi;\nvar QUERY_REGEXP = /^(?:(.+?)([<>]=?|=))??(?:(min|max)-)?([a-z-]+?)(?:-(hue|saturation|lightness|alpha))?(?:([<>]=?|=|:)(.+))?$/;\nvar ESCAPE_REGEXP = /[.:()<>=%,]/g;\nvar SPACE_REGEXP = / /g;\nvar LENGTH_REGEXP = /^(-?(?:\\d*\\.)?\\d+)(em|ex|ch|rem|vh|vw|vmin|vmax|px|mm|cm|in|pt|pc)$/i;\nvar NUMBER_REGEXP = /^-?(?:\\d*\\.)?\\d+$/i;\nvar COLOR_REGEXP = /^rgba?\\s*\\([0-9.,\\s]*\\)$/i;\nvar URL_VALUE_REGEXP = /url\\(\\s*(?:([\"'])(.*?)\\1|([^)\\s]*))\\s*\\)/gi;\nvar ATTR_REGEXP = /\\[.+?\\]/g;\nvar PSEUDO_NOT_REGEXP = /:not\\(/g;\nvar ID_REGEXP = /#[^\\s\\[\\\\#+,.:>~]+/g;\nvar CLASS_REGEXP = /\\.[^\\s\\[\\\\#+,.:>~]+/g;\nvar PSEUDO_ELEMENT_REGEXP = /::[^\\s\\[\\\\#+,.:>~]+/g;\nvar PSEUDO_CLASS_REGEXP = /:[^\\s\\[\\\\#+,.:>~]+/g;\nvar ELEMENT_REGEXP = /[a-z-]+/gi;\nvar FIXED_UNIT_MAP = {\n\t'px': 1,\n\t'pt': 16 / 12,\n\t'pc': 16,\n\t'in': 96,\n\t'cm': 96 / 2.54,\n\t'mm': 96 / 25.4,\n};\n\nvar queries;\nvar containerCache;\nvar styleCache;\nvar processedSheets = createCacheMap();\nvar requestCache = {};\nvar domMutations = [];\nvar processed = false;\nvar parsed = false;\nvar documentElement = document.documentElement;\nvar styleSheets = document.styleSheets;\nvar createElement = document.createElement.bind(document);\nvar requestAnimationFrame = window.requestAnimationFrame || setTimeout;\nvar observer;\nvar scheduledCall;\n\n/**\n * @param {function()} callback\n */\nfunction reprocess(callback) {\n\tpreprocess(function() {\n\t\tprocessed = true;\n\t\treparse(callback);\n\t});\n}\n\n/**\n * @param {function()} callback\n */\nfunction reparse(callback) {\n\tif (!processed) {\n\t\treturn reprocess(callback);\n\t}\n\tparseRules();\n\tbuildStyleCache();\n\tparsed = true;\n\treevaluate(true, callback);\n}\n\n/**\n * @param {boolean}         clearContainerCache\n * @param {function()}      callback\n * @param {Array.<Element>} contexts\n */\nfunction reevaluate(clearContainerCache, callback, contexts) {\n\tif (!parsed) {\n\t\treturn reparse(callback);\n\t}\n\tupdateClasses(clearContainerCache, contexts);\n\tif (callback) {\n\t\tcallback();\n\t}\n}\n\n/**\n * Schedule the execution of step 1, 2 or 3 for the next animation frame\n *\n * @param {number}          step                1: reprocess, 2: reparse, 3: reevaluate\n * @param {boolean}         clearContainerCache\n * @param {Array.<Element>} contexts\n */\nfunction scheduleExecution(step, clearContainerCache, contexts) {\n\n\tif (!scheduledCall) {\n\t\tscheduledCall = {\n\t\t\t_step: step,\n\t\t\t_clearContainerCache: clearContainerCache,\n\t\t\t_contexts: contexts,\n\t\t};\n\t\trequestAnimationFrame(executeScheduledCall);\n\t\treturn;\n\t}\n\n\tscheduledCall._step = Math.min(scheduledCall._step, step);\n\n\t// Merge parameters for reevaluate\n\tif (scheduledCall._step === 3) {\n\t\tscheduledCall._clearContainerCache = scheduledCall._clearContainerCache || clearContainerCache;\n\t\tif (scheduledCall._contexts && contexts) {\n\t\t\tscheduledCall._contexts = scheduledCall._contexts.concat(contexts);\n\t\t}\n\t}\n\n}\n\n/**\n * Executes the scheduled call from scheduleExecution() in an animation frame\n */\nfunction executeScheduledCall() {\n\n\tvar call = scheduledCall;\n\tscheduledCall = undefined;\n\n\tif (call._step === 1) {\n\t\treprocess();\n\t}\n\telse if (call._step === 2) {\n\t\treparse();\n\t}\n\telse {\n\t\treevaluate(call._clearContainerCache, undefined, call._contexts);\n\t}\n\n}\n\n/**\n * Starts observing DOM events and mutations\n */\nfunction startObserving() {\n\n\tif (config.skipObserving) {\n\t\treturn;\n\t}\n\n\t// Reprocess now\n\tscheduleExecution(1);\n\n\twindow.addEventListener('DOMContentLoaded', scheduleExecution.bind(undefined, 1, undefined, undefined));\n\twindow.addEventListener('load', scheduleExecution.bind(undefined, 1, undefined, undefined));\n\twindow.addEventListener('resize', scheduleExecution.bind(undefined, 3, true, undefined));\n\n\tvar MutationObserver = window.MutationObserver || window.WebKitMutationObserver;\n\tif (MutationObserver) {\n\t\tobserver = new MutationObserver(checkMutations);\n\t\tobserver.observe(document.documentElement, {\n\t\t\tchildList: true,\n\t\t\tsubtree: true,\n\t\t});\n\t}\n\telse {\n\t\twindow.addEventListener('DOMNodeInserted', onDomMutate);\n\t\twindow.addEventListener('DOMNodeRemoved', onDomMutate);\n\t}\n\n}\n\n/**\n * Check DOM mutations and reprocess or reevaluate\n *\n * @param  {Array.<MutationRecord>} mutations\n */\nfunction checkMutations(mutations) {\n\n\t// Skip iterating the nodes, if a run is already scheduled, to improve performance\n\tif (scheduledCall && (scheduledCall._level < 3 || !scheduledCall._contexts)) {\n\t\treturn;\n\t}\n\n\tvar addedNodes = [];\n\tvar stylesChanged = false;\n\n\tvar replacedSheets = [];\n\tprocessedSheets.forEach(function(newNode) {\n\t\treplacedSheets.push(newNode);\n\t});\n\n\tarrayFrom(mutations).forEach(function(mutation) {\n\n\t\taddedNodes.push.apply(addedNodes, arrayFrom(mutation.addedNodes).filter(function(node) {\n\t\t\treturn node.nodeType === 1;\n\t\t}));\n\n\t\tarrayFrom(mutation.removedNodes).forEach(function(node) {\n\t\t\tvar index = addedNodes.indexOf(node);\n\t\t\tif (index !== -1) {\n\t\t\t\taddedNodes.splice(index, 1);\n\t\t\t}\n\t\t\telse if (\n\t\t\t\t(node.tagName === 'LINK' || node.tagName === 'STYLE')\n\t\t\t\t&& replacedSheets.indexOf(node) === -1\n\t\t\t) {\n\t\t\t\tstylesChanged = true;\n\t\t\t}\n\t\t});\n\n\t});\n\n\taddedNodes.forEach(function(node) {\n\t\tif (node.sheet && replacedSheets.indexOf(node) === -1) {\n\t\t\tstylesChanged = true;\n\t\t}\n\t});\n\n\tif (stylesChanged) {\n\t\tscheduleExecution(1);\n\t}\n\telse if (addedNodes.length) {\n\t\tscheduleExecution(3, false, addedNodes);\n\t}\n\n}\n\n/**\n * Event handler for DOMNodeInserted and DOMNodeRemoved\n *\n * @param  {MutationEvent} event\n */\nfunction onDomMutate(event) {\n\n\tvar mutation = {\n\t\taddedNodes: [],\n\t\tremovedNodes: [],\n\t};\n\tmutation[\n\t\t(event.type === 'DOMNodeInserted' ? 'added' : 'removed') + 'Nodes'\n\t] = [event.target];\n\n\tdomMutations.push(mutation);\n\n\t// Delay the call to checkMutations()\n\tsetTimeout(function() {\n\t\tcheckMutations(domMutations);\n\t\tdomMutations = [];\n\t});\n\n}\n\n/**\n * Step 1: Preprocess all active stylesheets in the document\n *\n * Look for stylesheets that contain container queries and escape them to be\n * readable by the browser, e.g. convert `:container(width >= 10px)` to\n * `\\:container\\(width\\>\\=10px\\)`\n *\n * @param {function()} callback\n */\nfunction preprocess(callback) {\n\n\tvar sheets = arrayFrom(styleSheets);\n\n\t// Check removed stylesheets\n\tprocessedSheets.forEach(function(newNode, node) {\n\t\t// Original stylesheet has been deleted\n\t\tif (sheets.indexOf(node.sheet) === -1) {\n\t\t\tif (newNode && sheets.indexOf(newNode.sheet) !== -1 && newNode.parentNode) {\n\t\t\t\tsheets.splice(sheets.indexOf(newNode.sheet), 1);\n\t\t\t\tnewNode.parentNode.removeChild(newNode);\n\t\t\t}\n\t\t\tprocessedSheets.delete(node);\n\t\t}\n\t});\n\n\tvar done = -1;\n\tfunction step() {\n\t\tdone++;\n\t\tif (done === sheets.length) {\n\t\t\tcallback();\n\t\t}\n\t}\n\tsheets.forEach(function(sheet) {\n\t\tpreprocessSheet(sheet, step);\n\t});\n\tstep();\n\n}\n\n/**\n * @param {CSSStyleSheet} sheet\n * @param {function()}    callback\n */\nfunction preprocessSheet(sheet, callback) {\n\tif (sheet.disabled) {\n\t\tcallback();\n\t\treturn;\n\t}\n\tif (!config.preprocess) {\n\t\tvar rulesLength = -1;\n\t\ttry {\n\t\t\trulesLength = sheet.cssRules.length;\n\t\t}\n\t\tcatch(e) {\n\t\t\t// Do nothing\n\t\t}\n\t\t// Check if cssRules is accessible\n\t\tif (rulesLength !== -1 || !sheet.ownerNode.getAttribute('crossorigin')) {\n\t\t\tcallback();\n\t\t\treturn;\n\t\t}\n\t}\n\tvar ownerNode = sheet.ownerNode;\n\tvar tag = ownerNode && ownerNode.tagName;\n\tif (tag === 'LINK' && !processedSheets.has(ownerNode)) {\n\t\tloadExternal(ownerNode.href, function(cssText) {\n\t\t\t// Check again because loadExternal is async\n\t\t\tif (sheet.disabled || !cssText) {\n\t\t\t\tcallback();\n\t\t\t\treturn;\n\t\t\t}\n\t\t\tpreprocessStyle(ownerNode, fixRelativeUrls(cssText, ownerNode.href));\n\t\t\tcallback();\n\t\t});\n\t}\n\telse if (tag === 'STYLE') {\n\t\tpreprocessStyle(ownerNode, ownerNode.innerHTML);\n\t\tcallback();\n\t}\n\telse {\n\t\tcallback();\n\t}\n}\n\n/**\n * Load external file via AJAX\n *\n * @param {string}           href\n * @param {function(string)} callback Gets called with the response text on\n *                                    success or empty string on failure\n */\nfunction loadExternal(href, callback) {\n\tvar cacheEntryType = typeof requestCache[href];\n\tif (cacheEntryType === 'string') {\n\t\tcallback(requestCache[href]);\n\t\treturn;\n\t}\n\telse if (cacheEntryType === 'object') {\n\t\trequestCache[href].push(callback);\n\t\treturn;\n\t}\n\trequestCache[href] = [callback]\n\tvar isDone = false;\n\tvar done = function(response) {\n\t\tif (!isDone) {\n\t\t\tresponse = response || '';\n\t\t\trequestCache[href].forEach(function(cachedCallback) {\n\t\t\t\tsetTimeout(function() {\n\t\t\t\t\tcachedCallback(response);\n\t\t\t\t});\n\t\t\t});\n\t\t\trequestCache[href] = response;\n\t\t}\n\t\tisDone = true;\n\t};\n\tvar xhr = new XMLHttpRequest();\n\txhr.onreadystatechange = function() {\n\t\tif (xhr.readyState !== 4) {\n\t\t\treturn;\n\t\t}\n\t\tdone(xhr.status === 200 && xhr.responseText);\n\t};\n\ttry {\n\t\txhr.open('GET', href);\n\t\txhr.send();\n\t}\n\tcatch(e) {\n\t\tif (window.XDomainRequest) {\n\t\t\txhr = new XDomainRequest();\n\t\t\txhr.onprogress =\n\t\t\t\t/* istanbul ignore next: fix for a rare IE9 bug */\n\t\t\t\tfunction() {};\n\t\t\txhr.onload = xhr.onerror = xhr.ontimeout = function() {\n\t\t\t\tdone(xhr.responseText);\n\t\t\t};\n\t\t\ttry {\n\t\t\t\txhr.open('GET', href);\n\t\t\t\txhr.send();\n\t\t\t}\n\t\t\tcatch(e2) {\n\t\t\t\tdone();\n\t\t\t}\n\t\t}\n\t\telse {\n\t\t\tdone();\n\t\t}\n\t}\n}\n\n/**\n * Replace relative CSS URLs with their absolute counterpart\n *\n * @param  {string} cssText\n * @param  {string} href    URL of the stylesheet\n * @return {string}\n */\nfunction fixRelativeUrls(cssText, href) {\n\tvar base = resolveRelativeUrl(href, document.baseURI);\n\treturn cssText.replace(URL_VALUE_REGEXP, function(match, quote, url1, url2) {\n\t\tvar url = url1 || url2;\n\t\tif (!url) {\n\t\t\treturn match;\n\t\t}\n\t\treturn 'url(' + (quote || '\"') + resolveRelativeUrl(url, base) + (quote || '\"') + ')';\n\t});\n}\n\n/**\n * @param  {string} url\n * @param  {string} base\n * @return {string}\n */\nfunction resolveRelativeUrl(url, base) {\n\tvar absoluteUrl;\n\ttry {\n\t\tabsoluteUrl = new URL(url, base).href;\n\t}\n\tcatch(e) {\n\t\tabsoluteUrl = false;\n\t}\n\tif (!absoluteUrl) {\n\t\tvar baseElement = createElement('base');\n\t\tbaseElement.href = base;\n\t\tdocument.head.insertBefore(baseElement, document.head.firstChild);\n\t\tvar link = createElement('a');\n\t\tlink.href = url;\n\t\tabsoluteUrl = link.href;\n\t\t// Catch error in iOS 7.0\n\t\ttry {\n\t\t\t// Fix for a bug in Opera 12\n\t\t\tdelete baseElement.href;\n\t\t}\n\t\tcatch(e) {\n\t\t\t// Do nothing\n\t\t}\n\t\tdocument.head.removeChild(baseElement);\n\t}\n\treturn absoluteUrl;\n}\n\n/**\n * @param {Node}   node    Stylesheet ownerNode\n * @param {string} cssText\n */\nfunction preprocessStyle(node, cssText) {\n\tprocessedSheets.set(node, false);\n\tvar escapedText = escapeSelectors(cssText);\n\tvar rulesLength = -1;\n\tif (escapedText === cssText) {\n\t\ttry {\n\t\t\trulesLength = node.sheet.cssRules.length;\n\t\t}\n\t\tcatch(e) {\n\t\t\trulesLength = -1;\n\t\t}\n\t\t// Check if cssRules is accessible\n\t\tif (rulesLength !== -1) {\n\t\t\treturn;\n\t\t}\n\t}\n\tvar style = createElement('style');\n\tstyle.textContent = escapedText;\n\tstyle.media = node.media || 'all';\n\tnode.parentNode.insertBefore(style, node);\n\tnode.sheet.disabled = true;\n\tprocessedSheets.set(node, style);\n}\n\n/**\n * @param  {string} cssText\n * @return {string}\n */\nfunction escapeSelectors(cssText) {\n\treturn cssText.replace(SELECTOR_REGEXP, function(selector) {\n\t\treturn '.' + selector.substr(selector[0] === '.' ? 1 : 0)\n\t\t\t.replace(SPACE_REGEXP, '')\n\t\t\t.replace(/\"/g, '')\n\t\t\t.replace(ESCAPE_REGEXP, '\\\\$&')\n\t\t\t.toLowerCase();\n\t});\n}\n\n/**\n * Step 2: Parse all processed container query rules and store them in `queries`\n * indexed by the preceding selector\n */\nfunction parseRules() {\n\tqueries = {};\n\tvar rules;\n\tfor (var i = 0; i < styleSheets.length; i++) {\n\t\tif (styleSheets[i].disabled) {\n\t\t\tcontinue;\n\t\t}\n\t\ttry {\n\t\t\trules = styleSheets[i].cssRules;\n\t\t\tif (!rules || !rules.length) {\n\t\t\t\tcontinue;\n\t\t\t}\n\t\t}\n\t\tcatch(e) {\n\t\t\tcontinue;\n\t\t}\n\t\tfor (var j = 0; j < rules.length; j++) {\n\t\t\tparseRule(rules[j]);\n\t\t}\n\t}\n}\n\n/**\n * @param {CSSRule} rule\n */\nfunction parseRule(rule) {\n\tif (rule.cssRules) {\n\t\tfor (var i = 0; i < rule.cssRules.length; i++) {\n\t\t\tparseRule(rule.cssRules[i]);\n\t\t}\n\t\treturn;\n\t}\n\tif (rule.type !== 1) {\n\t\treturn;\n\t}\n\tsplitSelectors(rule.selectorText).forEach(function(selector) {\n\t\tselector = escapeSelectors(selector);\n\t\tselector.replace(SELECTOR_ESCAPED_REGEXP, function(match, query, queryUnescaped, offset) {\n\t\t\tvar precedingSelector =\n\t\t\t\t(\n\t\t\t\t\tselector.substr(0, offset)\n\t\t\t\t\t+ selector.substr(offset + match.length).replace(/^((?:\\([^)]*\\)|[^\\s>+~])*).*$/, '$1')\n\t\t\t\t)\n\t\t\t\t.replace(SELECTOR_ESCAPED_REGEXP, '')\n\t\t\t\t.replace(PSEUDO_ELEMENT_REGEXP, '')\n\t\t\t\t.replace(/:(?:active|hover|focus|checked|before|after)/gi, '');\n\t\t\tif (!precedingSelector.substr(-1).trim()) {\n\t\t\t\tprecedingSelector += '*';\n\t\t\t}\n\t\t\tvar query = parseQuery(queryUnescaped || unescape(query));\n\t\t\tif (query) {\n\t\t\t\tquery._selector = precedingSelector;\n\t\t\t\tquery._className = queryUnescaped ? undefined : unescape(match.substr(1));\n\t\t\t\tquery._attribute = queryUnescaped;\n\t\t\t\tqueries[precedingSelector + match] = query;\n\t\t\t}\n\t\t});\n\t});\n}\n\n/**\n * @param  {string} query\n * @return {Array.<{_prop: string, _filter: string, _types: array, _values: array, _valueType: string}>}\n */\nfunction parseQuery(query) {\n\n\tif (!(query = QUERY_REGEXP.exec(query))) {\n\t\treturn;\n\t}\n\n\tvar values = [query[7], query[1]].filter(Boolean);\n\tvar valueType =\n\t\t(query[5] || values[0].match(NUMBER_REGEXP)) ? 'n' :\n\t\tvalues[0].match(LENGTH_REGEXP) ? 'l' :\n\t\tvalues[0].match(COLOR_REGEXP) ? 'c' :\n\t\t's';\n\tif (valueType === 'n') {\n\t\tvalues = values.map(parseFloat);\n\t}\n\telse if (valueType === 'c') {\n\t\tvalues = values.map(function(value) {\n\t\t\treturn parseColor(value).join(',');\n\t\t});\n\t}\n\n\treturn {\n\t\t_prop: query[4],\n\t\t_filter: query[5],\n\t\t_types: [\n\t\t\tquery[6] !== ':' ? query[6]\n\t\t\t\t: query[3] === 'min' ? '>='\n\t\t\t\t: query[3] ? '<='\n\t\t\t\t: '=',\n\t\t\tquery[2] && query[2].replace(/[<>]/, function(match) {\n\t\t\t\t// Invert the left side comparison operator\n\t\t\t\treturn match === '<' ? '>' : '<';\n\t\t\t}),\n\t\t].filter(Boolean),\n\t\t_values: values,\n\t\t_valueType: valueType,\n\t};\n\n}\n\n/**\n * Unescape backslash escaped string\n *\n * @param  {string} string\n * @return {string}\n */\nfunction unescape(string) {\n\treturn string && string.replace(/\\\\(.)/g, '$1');\n}\n\n/**\n * Split multiple selectors by `,`\n *\n * @param  {string} selectors\n * @return {Array.<string>}\n */\nfunction splitSelectors(selectors) {\n\treturn (selectors.match(/(?:\\\\.|\"(?:\\\\.|[^\"])*\"|\\([^)]*\\)|[^,])+/g) || [])\n\t\t.map(function(selector) {\n\t\t\treturn selector.trim();\n\t\t});\n}\n\n/**\n * Builds the styleCache needed by getOriginalStyle\n */\nfunction buildStyleCache() {\n\tstyleCache = {\n\t\twidth: {},\n\t\theight: {},\n\t};\n\tvar rules;\n\tfor (var i = 0; i < styleSheets.length; i++) {\n\t\tif (styleSheets[i].disabled) {\n\t\t\tcontinue;\n\t\t}\n\t\ttry {\n\t\t\trules = styleSheets[i].cssRules;\n\t\t\tif (!rules || !rules.length) {\n\t\t\t\tcontinue;\n\t\t\t}\n\t\t}\n\t\tcatch(e) {\n\t\t\tcontinue;\n\t\t}\n\t\tbuildStyleCacheFromRules(rules);\n\t}\n}\n\n/**\n * @param {CSSRuleList} rules\n */\nfunction buildStyleCacheFromRules(rules) {\n\tfor (var i = 0; i < rules.length; i++) {\n\t\tif (rules[i].type === 1) { // Style rule\n\t\t\tif (\n\t\t\t\trules[i].style.getPropertyValue('width')\n\t\t\t\t|| rules[i].style.getPropertyValue('height')\n\t\t\t) {\n\t\t\t\tsplitSelectors(escapeSelectors(rules[i].selectorText)).forEach(function(selector) {\n\t\t\t\t\tvar rule = {\n\t\t\t\t\t\t_selector: selector,\n\t\t\t\t\t\t_rule: rules[i],\n\t\t\t\t\t\t_specificity: getSpecificity(selector),\n\t\t\t\t\t};\n\t\t\t\t\tvar rightMostSelector = selector\n\t\t\t\t\t\t.replace(/:[a-z-]+\\([^)]*\\)/i, '')\n\t\t\t\t\t\t.replace(/^.*[^\\\\][\\s>+~]\\s*/, '');\n\t\t\t\t\tif (\n\t\t\t\t\t\trightMostSelector.match(PSEUDO_ELEMENT_REGEXP)\n\t\t\t\t\t\t|| rightMostSelector.match(/:(?:before|after)/i)\n\t\t\t\t\t) {\n\t\t\t\t\t\treturn;\n\t\t\t\t\t}\n\t\t\t\t\trightMostSelector = rightMostSelector\n\t\t\t\t\t\t.replace(PSEUDO_CLASS_REGEXP, '')\n\t\t\t\t\t\t.trim();\n\t\t\t\t\t['width', 'height'].forEach(function(prop) {\n\t\t\t\t\t\tif (!rules[i].style.getPropertyValue(prop)) {\n\t\t\t\t\t\t\treturn;\n\t\t\t\t\t\t}\n\t\t\t\t\t\tvar match = rightMostSelector.match(ID_REGEXP);\n\t\t\t\t\t\tif (!match) {\n\t\t\t\t\t\t\tmatch = rightMostSelector.match(CLASS_REGEXP);\n\t\t\t\t\t\t}\n\t\t\t\t\t\tif (!match) {\n\t\t\t\t\t\t\tmatch = rightMostSelector.match(ELEMENT_REGEXP);\n\t\t\t\t\t\t\tif (match) {\n\t\t\t\t\t\t\t\tmatch = [match[0].toLowerCase()];\n\t\t\t\t\t\t\t}\n\t\t\t\t\t\t}\n\t\t\t\t\t\tif (!match) {\n\t\t\t\t\t\t\tmatch = '*';\n\t\t\t\t\t\t}\n\t\t\t\t\t\tif (!styleCache[prop][match[0]]) {\n\t\t\t\t\t\t\tstyleCache[prop][match[0]] = [];\n\t\t\t\t\t\t}\n\t\t\t\t\t\tstyleCache[prop][match[0]].push(rule);\n\t\t\t\t\t});\n\t\t\t\t});\n\t\t\t}\n\t\t}\n\t\telse if (rules[i].cssRules) {\n\t\t\tbuildStyleCacheFromRules(rules[i].cssRules);\n\t\t}\n\t}\n}\n\n/**\n * Step 3: Loop through the `queries` and add or remove the CSS classes of all\n * matching elements\n *\n * @param {boolean}         clearContainerCache\n * @param {Array.<Element>} contexts\n */\nfunction updateClasses(clearContainerCache, contexts) {\n\n\tif (clearContainerCache || !containerCache) {\n\t\tcontainerCache = createCacheMap();\n\t}\n\n\tif (!Object.keys(queries).length) {\n\t\treturn;\n\t}\n\n\tvar elementsTree = buildElementsTree(contexts);\n\n\twhile(updateClassesRead(elementsTree)) {\n\t\tupdateClassesWrite(elementsTree);\n\t}\n\tupdateClassesWrite(elementsTree);\n\n}\n\n/**\n * Update classes read step\n *\n * @param  {Array.<{_element: Element, _children: array, _queries: array, _changes: array, _done: boolean}>} treeNodes\n * @param  {boolean}                                                                                         dontMarkAsDone\n * @return {boolean} True if changes were found\n */\nfunction updateClassesRead(treeNodes, dontMarkAsDone) {\n\tvar hasChanges = false;\n\tvar i, node, j, query;\n\tfor (i = 0; i < treeNodes.length; i++) {\n\t\tnode = treeNodes[i];\n\t\tif (!node._done) {\n\t\t\tfor (j = 0; j < node._queries.length; j++) {\n\t\t\t\tquery = node._queries[j];\n\t\t\t\tvar queryMatches = evaluateQuery(node._element.parentNode, query);\n\t\t\t\tif (queryMatches !== hasQuery(node._element, query)) {\n\t\t\t\t\tnode._changes.push([queryMatches, query]);\n\t\t\t\t}\n\t\t\t}\n\t\t\tnode._done = !dontMarkAsDone;\n\t\t}\n\t\thasChanges = updateClassesRead(node._children, dontMarkAsDone || node._changes.length)\n\t\t\t|| node._changes.length\n\t\t\t|| hasChanges;\n\t}\n\treturn hasChanges;\n}\n\n/**\n * Update classes write step\n *\n * @param  {Array.<{_element: Element, _children: array, _queries: array, _changes: array, _done: boolean}>} treeNodes\n */\nfunction updateClassesWrite(treeNodes) {\n\tvar node, j;\n\tfor (var i = 0; i < treeNodes.length; i++) {\n\t\tnode = treeNodes[i];\n\t\tfor (j = 0; j < node._changes.length; j++) {\n\t\t\t(node._changes[j][0] ? addQuery : removeQuery)(node._element, node._changes[j][1]);\n\t\t}\n\t\tnode._changes = [];\n\t\tupdateClassesWrite(node._children);\n\t}\n}\n\n/**\n * Build tree of all query elements\n *\n * @param  {Array.<Element>} contexts\n * @return {Array.<{_element: Element, _children: array, _queries: array, _changes: array, _done: boolean}>}\n */\nfunction buildElementsTree(contexts) {\n\n\tcontexts = contexts || [document];\n\n\tvar queriesArray = Object.keys(queries).map(function(key) {\n\t\treturn queries[key];\n\t});\n\n\tvar selector = queriesArray.map(function(query) {\n\t\treturn query._selector;\n\t}).join(',');\n\n\tvar elements = [];\n\tcontexts.forEach(function(context) {\n\t\tfor (var node = context.parentNode; node; node = node.parentNode) {\n\t\t\t// Skip nested contexts\n\t\t\tif (contexts.indexOf(node) !== -1) {\n\t\t\t\treturn;\n\t\t\t}\n\t\t}\n\t\tif (context !== document && elementMatchesSelector(context, selector)) {\n\t\t\telements.push(context);\n\t\t}\n\t\telements.push.apply(elements, arrayFrom(context.querySelectorAll(selector)));\n\t});\n\n\tvar tree = [];\n\tvar treeCache = createCacheMap();\n\n\telements.forEach(function(element) {\n\n\t\tif (element === documentElement) {\n\t\t\treturn;\n\t\t}\n\n\t\tvar treeNode = {\n\t\t\t_element: element,\n\t\t\t_children: [],\n\t\t\t_queries: [],\n\t\t\t_changes: [],\n\t\t\t_done: false,\n\t\t};\n\n\t\tvar children = tree;\n\t\tfor (var node = element.parentNode; node; node = node.parentNode) {\n\t\t\tif (treeCache.get(node)) {\n\t\t\t\tchildren = treeCache.get(node)._children;\n\t\t\t\tbreak;\n\t\t\t}\n\t\t}\n\n\t\ttreeCache.set(element, treeNode);\n\n\t\tchildren.push(treeNode);\n\n\t\tqueriesArray.forEach(function(query) {\n\t\t\tif (elementMatchesSelector(element, query._selector)) {\n\t\t\t\ttreeNode._queries.push(query);\n\t\t\t}\n\t\t});\n\n\t});\n\n\treturn tree;\n\n}\n\n/**\n * True if the query matches otherwise false\n *\n * @param  {Element} parent\n * @param  {object}  query\n * @return {boolean}\n */\nfunction evaluateQuery(parent, query) {\n\n\tvar container = getContainer(parent, query._prop);\n\tvar qValues = query._values.slice(0);\n\tvar i;\n\n\tvar cValue;\n\tif (query._prop === 'width' || query._prop === 'height') {\n\t\tcValue = getSize(container, query._prop);\n\t}\n\telse {\n\t\tcValue = getComputedStyle(container).getPropertyValue(query._prop);\n\t}\n\n\tif (query._filter) {\n\t\tvar color = parseColor(cValue);\n\t\tif (query._filter[0] === 'h') {\n\t\t\tcValue = color[0];\n\t\t}\n\t\telse if (query._filter[0] === 's') {\n\t\t\tcValue = color[1];\n\t\t}\n\t\telse if (query._filter[0] === 'l') {\n\t\t\tcValue = color[2];\n\t\t}\n\t\telse if (query._filter[0] === 'a') {\n\t\t\tcValue = color[3];\n\t\t}\n\t\telse {\n\t\t\treturn false;\n\t\t}\n\t}\n\telse if (query._valueType === 'c') {\n\t\tcValue = parseColor(cValue).join(',');\n\t}\n\telse if (query._valueType === 'l') {\n\t\tfor (i = 0; i < qValues.length; i++) {\n\t\t\tqValues[i] = getComputedLength(qValues[i], parent);\n\t\t}\n\t\tif (typeof cValue === 'string') {\n\t\t\tcValue = getComputedLength(cValue, parent);\n\t\t}\n\t}\n\telse if (query._valueType === 'n') {\n\t\tcValue = parseFloat(cValue);\n\t}\n\telse if (typeof cValue === 'string') {\n\t\tcValue = cValue.trim();\n\t}\n\n\tif ((\n\t\tquery._types[0][0] === '>'\n\t\t|| query._types[0][0] === '<'\n\t) && (\n\t\ttypeof cValue !== 'number'\n\t\t|| typeof qValues[0] !== 'number'\n\t)) {\n\t\treturn false;\n\t}\n\n\tfor (i = 0; i < qValues.length; i++) {\n\t\tif (!(\n\t\t\t(query._types[i] === '>=' && cValue >= qValues[i])\n\t\t\t|| (query._types[i] === '<=' && cValue <= qValues[i])\n\t\t\t|| (query._types[i] === '>' && cValue > qValues[i])\n\t\t\t|| (query._types[i] === '<' && cValue < qValues[i])\n\t\t\t|| (query._types[i] === '=' && cValue === qValues[i])\n\t\t)) {\n\t\t\treturn false;\n\t\t}\n\t}\n\n\treturn true;\n\n}\n\n/**\n * Get the nearest qualified container element starting by the element itself\n *\n * @param  {Element} element\n * @param  {string}  prop    CSS property\n * @return {Element}\n */\nfunction getContainer(element, prop) {\n\n\tvar cache;\n\tif (containerCache.has(element)) {\n\t\tcache = containerCache.get(element);\n\t\tif (cache[prop]) {\n\t\t\treturn cache[prop];\n\t\t}\n\t}\n\telse {\n\t\tcache = {};\n\t\tcontainerCache.set(element, cache);\n\t}\n\n\tif (element === documentElement) {\n\t\tcache[prop] = element;\n\t}\n\n\telse if (prop !== 'width' && prop !== 'height') {\n\t\t// Skip transparent background colors\n\t\tif (prop === 'background-color' && !parseColor(getComputedStyle(element).getPropertyValue(prop))[3]) {\n\t\t\tcache[prop] = getContainer(element.parentNode, prop);\n\t\t}\n\t\telse {\n\t\t\tcache[prop] = element;\n\t\t}\n\t}\n\n\t// Skip inline elements\n\telse if (getComputedStyle(element).display === 'inline') {\n\t\tcache[prop] = getContainer(element.parentNode, prop);\n\t}\n\n\telse if (isFixedSize(element, prop)) {\n\t\tcache[prop] = element;\n\t}\n\n\telse {\n\n\t\tvar parentNode = element.parentNode;\n\n\t\t// Skip to next positioned ancestor for absolute positioned elements\n\t\tif (getComputedStyle(element).position === 'absolute') {\n\t\t\twhile (\n\t\t\t\tparentNode.parentNode\n\t\t\t\t&& parentNode.parentNode.nodeType === 1\n\t\t\t\t&& ['relative', 'absolute'].indexOf(getComputedStyle(parentNode).position) === -1\n\t\t\t) {\n\t\t\t\tparentNode = parentNode.parentNode;\n\t\t\t}\n\t\t}\n\n\t\t// Skip to next ancestor with a transform applied for fixed positioned elements\n\t\tif (getComputedStyle(element).position === 'fixed') {\n\t\t\twhile (\n\t\t\t\tparentNode.parentNode\n\t\t\t\t&& parentNode.parentNode.nodeType === 1\n\t\t\t\t&& [undefined, 'none'].indexOf(\n\t\t\t\t\tgetComputedStyle(parentNode).transform\n\t\t\t\t\t|| getComputedStyle(parentNode).MsTransform\n\t\t\t\t\t|| getComputedStyle(parentNode).WebkitTransform\n\t\t\t\t) !== -1\n\t\t\t) {\n\t\t\t\tparentNode = parentNode.parentNode;\n\t\t\t}\n\t\t}\n\n\t\tvar parentContainer = getContainer(parentNode, prop);\n\t\twhile (getComputedStyle(parentNode).display === 'inline') {\n\t\t\tparentNode = parentNode.parentNode;\n\t\t}\n\t\tif (parentNode === parentContainer && !isIntrinsicSize(element, prop)) {\n\t\t\tcache[prop] = element;\n\t\t}\n\t\telse {\n\t\t\tcache[prop] = parentContainer;\n\t\t}\n\t}\n\n\treturn cache[prop];\n\n}\n\n/**\n * Is the size of the element a fixed length e.g. `1px`?\n *\n * @param  {Element} element\n * @param  {string}  prop    `width` or `height`\n * @return {boolean}\n */\nfunction isFixedSize(element, prop) {\n\tvar originalStyle = getOriginalStyle(element, prop);\n\tif (originalStyle && (\n\t\toriginalStyle.match(LENGTH_REGEXP)\n\t\t|| originalStyle.match(/^calc\\([^%]*\\)$/i)\n\t)) {\n\t\treturn true;\n\t}\n\treturn false;\n}\n\n/**\n * Is the size of the element depending on its descendants?\n *\n * @param  {Element} element\n * @param  {string}  prop    `width` or `height`\n * @return {boolean}\n */\nfunction isIntrinsicSize(element, prop) {\n\n\tvar computedStyle = getComputedStyle(element);\n\n\tif (computedStyle.display === 'none') {\n\t\treturn false;\n\t}\n\n\tif (computedStyle.display === 'inline') {\n\t\treturn true;\n\t}\n\n\t// Non-floating non-absolute block elements (only width)\n\tif (\n\t\tprop === 'width'\n\t\t&& ['block', 'list-item', 'flex', 'grid'].indexOf(computedStyle.display) !== -1\n\t\t&& computedStyle.cssFloat === 'none'\n\t\t&& computedStyle.position !== 'absolute'\n\t\t&& computedStyle.position !== 'fixed'\n\t) {\n\t\treturn false;\n\t}\n\n\tvar originalStyle = getOriginalStyle(element, prop);\n\n\t// Fixed size\n\tif (originalStyle && originalStyle.match(LENGTH_REGEXP)) {\n\t\treturn false;\n\t}\n\n\t// Percentage size\n\tif (originalStyle && originalStyle.substr(-1) === '%') {\n\t\treturn false;\n\t}\n\n\t// Calc expression\n\tif (originalStyle && originalStyle.substr(0, 5) === 'calc(') {\n\t\treturn false;\n\t}\n\n\t// Elements without a defined size\n\treturn true;\n\n}\n\n/**\n * Get the computed content-box size\n *\n * @param  {Element} element\n * @param  {string}  prop    `width` or `height`\n * @return {number}\n */\nfunction getSize(element, prop) {\n\tvar style = getComputedStyle(element);\n\tif (prop === 'width') {\n\t\treturn element.offsetWidth\n\t\t\t- parseFloat(style.borderLeftWidth)\n\t\t\t- parseFloat(style.paddingLeft)\n\t\t\t- parseFloat(style.borderRightWidth)\n\t\t\t- parseFloat(style.paddingRight);\n\t}\n\telse {\n\t\treturn element.offsetHeight\n\t\t\t- parseFloat(style.borderTopWidth)\n\t\t\t- parseFloat(style.paddingTop)\n\t\t\t- parseFloat(style.borderBottomWidth)\n\t\t\t- parseFloat(style.paddingBottom);\n\t}\n}\n\n/**\n * Get the computed length in pixel of a CSS length value\n *\n * @param  {string}  value\n * @param  {Element} element\n * @return {number}\n */\nfunction getComputedLength(value, element) {\n\tvar length = value.match(LENGTH_REGEXP);\n\tif (!length) {\n\t\treturn parseFloat(value);\n\t}\n\tvalue = parseFloat(length[1]);\n\tvar unit = length[2].toLowerCase();\n\tif (FIXED_UNIT_MAP[unit]) {\n\t\treturn value * FIXED_UNIT_MAP[unit];\n\t}\n\tif (unit === 'vw') {\n\t\treturn value * window.innerWidth / 100;\n\t}\n\tif (unit === 'vh') {\n\t\treturn value * window.innerHeight / 100;\n\t}\n\tif (unit === 'vmin') {\n\t\treturn value * Math.min(window.innerWidth, window.innerHeight) / 100;\n\t}\n\tif (unit === 'vmax') {\n\t\treturn value * Math.max(window.innerWidth, window.innerHeight) / 100;\n\t}\n\t// em units\n\tif (unit === 'rem') {\n\t\telement = documentElement;\n\t}\n\tif (unit === 'ex') {\n\t\tvalue /= 2;\n\t}\n\treturn parseFloat(getComputedStyle(element).fontSize) * value;\n}\n\n/**\n * @param  {Element} element\n * @return {CSSStyleDeclaration}\n */\nfunction getComputedStyle(element) {\n\n\tvar style = window.getComputedStyle(element);\n\n\t// Fix display inline in some browsers\n\tif (style.display === 'inline' && (\n\t\tstyle.position === 'absolute'\n\t\t|| style.position === 'fixed'\n\t\t|| style.cssFloat !== 'none'\n\t)) {\n\t\tvar newStyle = {};\n\t\tfor (var prop in style) {\n\t\t\tif (typeof style[prop] === 'string') {\n\t\t\t\tnewStyle[prop] = style[prop];\n\t\t\t}\n\t\t}\n\t\tstyle = newStyle;\n\t\tstyle.display = 'block';\n\t\tstyle.getPropertyValue = function(property) {\n\t\t\treturn this[property.replace(/-+(.)/g, function(match, char) {\n\t\t\t\treturn char.toUpperCase();\n\t\t\t})];\n\t\t};\n\t}\n\n\treturn style;\n\n}\n\n/**\n * Get the original style of an element as it was specified in CSS\n *\n * @param  {Element} element\n * @param  {string}  prop    Property to return, e.g. `width` or `height`\n * @return {string}\n */\nfunction getOriginalStyle(element, prop) {\n\n\tvar matchedRules = [];\n\tvar value;\n\tvar j;\n\n\tmatchedRules = sortRulesBySpecificity(\n\t\tfilterRulesByElementAndProp(styleCache[prop], element, prop)\n\t);\n\n\t// Add style attribute\n\tmatchedRules.unshift({\n\t\t_rule: {\n\t\t\tstyle: element.style,\n\t\t},\n\t});\n\n\t// Loop through all important styles\n\tfor (j = 0; j < matchedRules.length; j++) {\n\t\tif (\n\t\t\t(value = matchedRules[j]._rule.style.getPropertyValue(prop))\n\t\t\t&& matchedRules[j]._rule.style.getPropertyPriority(prop) === 'important'\n\t\t) {\n\t\t\treturn value;\n\t\t}\n\t}\n\n\t// Loop through all non-important styles\n\tfor (j = 0; j < matchedRules.length; j++) {\n\t\tif (\n\t\t\t(value = matchedRules[j]._rule.style.getPropertyValue(prop))\n\t\t\t&& matchedRules[j]._rule.style.getPropertyPriority(prop) !== 'important'\n\t\t) {\n\t\t\treturn value;\n\t\t}\n\t}\n\n\treturn undefined;\n\n}\n\n/**\n * @type {CSSStyleDeclaration}\n */\nvar parseColorStyle = createElement('div').style;\n\n/**\n * Parse CSS color and return as HSLA array\n *\n * @param  {string} color\n * @return {Array.<number>}\n */\nfunction parseColor(color) {\n\n\t// Let the browser round the RGBA values for consistency\n\tparseColorStyle.cssText = 'color:' + color;\n\tcolor = parseColorStyle.color;\n\n\tif (!color || !color.split || !color.split('(')[1]) {\n\t\treturn [0, 0, 0, 0];\n\t}\n\n\tcolor = color.split('(')[1].split(',').map(parseFloat);\n\n\tif (color[3] === undefined) {\n\t\tcolor[3] = 1;\n\t}\n\telse if (!color[3]) {\n\t\tcolor[0] = color[1] = color[2] = 0;\n\t}\n\n\tcolor[3] = Math.round(color[3] * 255);\n\n\treturn rgbaToHsla(color);\n\n}\n\n/**\n * @param  {Array.<number>} color\n * @return {Array.<number>}\n */\nfunction rgbaToHsla(color) {\n\n\tvar red = color[0] / 255;\n\tvar green = color[1] / 255;\n\tvar blue = color[2] / 255;\n\n\tvar max = Math.max(red, green, blue);\n\tvar min = Math.min(red, green, blue);\n\n\tvar hue;\n\tvar saturation;\n\tvar lightness = (max + min) / 2;\n\n\thue = saturation = 0;\n\n\tif (max !== min) {\n\t\tvar delta = max - min;\n\t\tsaturation = delta / (lightness > 0.5 ? 2 - max - min : max + min);\n\t\tif (max === red) {\n\t\t\thue = (green - blue) / delta + ((green < blue) * 6);\n\t\t}\n\t\telse if (max === green) {\n\t\t\thue = (blue - red) / delta + 2;\n\t\t}\n\t\telse {\n\t\t\thue = (red - green) / delta + 4;\n\t\t}\n\t\thue /= 6;\n\t}\n\n\treturn [hue * 360, saturation * 100, lightness * 100, color[3]];\n}\n\n/**\n * Filter rules by matching the element and at least one property\n *\n * @param  {{<string>: Array.<{_selector: string, _rule: CSSRule}>}} rules\n * @param  {Element}                                                 element\n * @param  {string}                                                  prop\n * @return {Array.<{_selector: string, _rule: CSSRule}>}\n */\nfunction filterRulesByElementAndProp(rules, element, prop) {\n\tvar foundRules = [];\n\tif (element.id) {\n\t\tfoundRules = foundRules.concat(rules['#' + element.id] || []);\n\t}\n\t(element.getAttribute('class') || '').split(/\\s+/).forEach(function(className) {\n\t\tfoundRules = foundRules.concat(rules['.' + className] || []);\n\t});\n\tfoundRules = foundRules\n\t\t.concat(rules[element.tagName.toLowerCase()] || [])\n\t\t.concat(rules['*'] || []);\n\treturn foundRules.filter(function(rule) {\n\t\treturn rule._rule.style.getPropertyValue(prop)\n\t\t\t&& (\n\t\t\t\t!rule._rule.parentRule\n\t\t\t\t|| rule._rule.parentRule.type !== 4 // @media rule\n\t\t\t\t|| matchesMedia(rule._rule.parentRule.media.mediaText)\n\t\t\t)\n\t\t\t&& elementMatchesSelector(element, rule._selector);\n\t});\n}\n\nvar elementMatchesSelectorMethod = (function(element) {\n\treturn element.matches\n\t\t|| element.mozMatchesSelector\n\t\t|| element.msMatchesSelector\n\t\t|| element.oMatchesSelector\n\t\t|| element.webkitMatchesSelector;\n})(createElement('div'));\n\n/**\n * @param  {Element} element\n * @param  {string}  selector\n * @return {boolean}\n */\nfunction elementMatchesSelector(element, selector) {\n\ttry {\n\t\treturn !!elementMatchesSelectorMethod.call(element, selector);\n\t}\n\tcatch(e) {\n\t\treturn false;\n\t}\n}\n\n/**\n * @param  {Array.<{_specificity: number}>} rules\n * @return {Array.<{_specificity: number}>}\n */\nfunction sortRulesBySpecificity(rules) {\n\treturn rules.map(function(rule, i) {\n\t\treturn [rule, i];\n\t}).sort(function(a, b) {\n\t\treturn (b[0]._specificity - a[0]._specificity) || b[1] - a[1];\n\t}).map(function(rule) {\n\t\treturn rule[0];\n\t});\n}\n\n/**\n * @param  {string} selector\n * @return {number}\n */\nfunction getSpecificity(selector) {\n\n\tvar idScore = 0;\n\tvar classScore = 0;\n\tvar typeScore = 0;\n\n\tselector\n\t\t.replace(SELECTOR_ESCAPED_REGEXP, function() {\n\t\t\tclassScore++;\n\t\t\treturn '';\n\t\t})\n\t\t.replace(SELECTOR_REGEXP, function() {\n\t\t\tclassScore++;\n\t\t\treturn '';\n\t\t})\n\t\t.replace(ATTR_REGEXP, function() {\n\t\t\tclassScore++;\n\t\t\treturn '';\n\t\t})\n\t\t.replace(PSEUDO_NOT_REGEXP, ' ')\n\t\t.replace(ID_REGEXP, function() {\n\t\t\tidScore++;\n\t\t\treturn '';\n\t\t})\n\t\t.replace(CLASS_REGEXP, function() {\n\t\t\tclassScore++;\n\t\t\treturn '';\n\t\t})\n\t\t.replace(PSEUDO_ELEMENT_REGEXP, function() {\n\t\t\ttypeScore++;\n\t\t\treturn '';\n\t\t})\n\t\t.replace(PSEUDO_CLASS_REGEXP, function() {\n\t\t\tclassScore++;\n\t\t\treturn '';\n\t\t})\n\t\t.replace(ELEMENT_REGEXP, function() {\n\t\t\ttypeScore++;\n\t\t\treturn '';\n\t\t});\n\n\treturn (\n\t\t(idScore * 256 * 256)\n\t\t+ (classScore * 256)\n\t\t+ typeScore\n\t);\n\n}\n\n/**\n * Create a new Map or a simple shim of it in non-supporting browsers\n *\n * @return {Map}\n */\nfunction createCacheMap() {\n\n\tif (typeof Map === 'function') {\n\t\treturn new Map();\n\t}\n\n\tvar keys = [];\n\tvar values = [];\n\n\tfunction getIndex(key) {\n\t\treturn keys.indexOf(key);\n\t}\n\n\tfunction get(key) {\n\t\treturn values[getIndex(key)];\n\t}\n\n\tfunction has(key) {\n\t\treturn getIndex(key) !== -1;\n\t}\n\n\tfunction set(key, value) {\n\t\tvar index = getIndex(key);\n\t\tif (index === -1) {\n\t\t\tindex = keys.push(key) - 1;\n\t\t}\n\t\tvalues[index] = value;\n\t}\n\n\tfunction deleteFunc(key) {\n\t\tvar index = getIndex(key);\n\t\tif (index === -1) {\n\t\t\treturn false;\n\t\t}\n\t\tdelete keys[index];\n\t\tdelete values[index];\n\t\treturn true;\n\t}\n\n\tfunction forEach(callback) {\n\t\tkeys.forEach(function(key, index) {\n\t\t\tif (key !== undefined) {\n\t\t\t\tcallback(values[index], key);\n\t\t\t}\n\t\t});\n\t}\n\n\treturn {\n\t\tset: set,\n\t\tget: get,\n\t\thas: has,\n\t\tdelete: deleteFunc,\n\t\tforEach: forEach,\n\t};\n}\n\n/**\n * @param  {Element}                                  element\n * @param  {{_className: string, _attribute: string}} query\n * @return {boolean}\n */\nfunction hasQuery(element, query) {\n\tif (query._className) {\n\t\treturn hasClass(element, query._className);\n\t}\n\treturn hasAttributeValue(element, 'data-cq', query._attribute);\n}\n\n/**\n * @param {Element}                                  element\n * @param {{_className: string, _attribute: string}} query\n */\nfunction addQuery(element, query) {\n\tif (query._className) {\n\t\taddClass(element, query._className);\n\t}\n\telse {\n\t\taddAttributeValue(element, 'data-cq', query._attribute);\n\t}\n}\n\n/**\n * @param {Element}                                  element\n * @param {{_className: string, _attribute: string}} query\n */\nfunction removeQuery(element, query) {\n\tif (query._className) {\n\t\tremoveClass(element, query._className);\n\t}\n\telse {\n\t\tremoveAttributeValue(element, 'data-cq', query._attribute);\n\t}\n}\n\n/**\n * @param {Element} element\n * @param {string}  className\n */\nfunction hasClass(element, className) {\n\tif (element.classList) {\n\t\treturn element.classList.contains(className);\n\t}\n\treturn hasAttributeValue(element, 'class', className);\n}\n\n/**\n * @param {Element} element\n * @param {string}  className\n */\nfunction addClass(element, className) {\n\tif (element.classList) {\n\t\telement.classList.add(className);\n\t}\n\telse if (!hasClass(element, className)) {\n\t\taddAttributeValue(element, 'class', className);\n\t}\n}\n\n/**\n * @param {Element} element\n * @param {string}  className\n */\nfunction removeClass(element, className) {\n\tif (element.classList) {\n\t\telement.classList.remove(className);\n\t}\n\telse {\n\t\tremoveAttributeValue(element, 'class', className);\n\t}\n}\n\n/**\n * @param  {Element} element\n * @param  {string}  attr\n * @param  {string}  value\n * @return {boolean}\n */\nfunction hasAttributeValue(element, attr, value) {\n\treturn !!(element.getAttribute(attr) || '').match(new RegExp(\n\t\t'(?:^|\\\\s+)'\n\t\t+ value.replace(REGEXP_ESCAPE_REGEXP, '\\\\$&')\n\t\t+ '($|\\\\s+)'\n\t));\n}\n\n/**\n * @param {Element} element\n * @param {string}  attr\n * @param {string}  value\n */\nfunction addAttributeValue(element, attr, value) {\n\tif (!hasAttributeValue(element, attr, value)) {\n\t\telement.setAttribute(attr, (element.getAttribute(attr) || '') + ' ' + value);\n\t}\n}\n\n/**\n * @param {Element} element\n * @param {string}  attr\n * @param {string}  value\n */\nfunction removeAttributeValue(element, attr, value) {\n\telement.setAttribute(attr, (element.getAttribute(attr) || '').replace(\n\t\tnew RegExp(\n\t\t\t'(?:^|\\\\s+)'\n\t\t\t+ value.replace(REGEXP_ESCAPE_REGEXP, '\\\\$&')\n\t\t\t+ '($|\\\\s+)'\n\t\t),\n\t\t'$1'\n\t));\n}\n\n/**\n * @param  {string} media\n * @return {boolean}\n */\nfunction matchesMedia(media) {\n\tif (window.matchMedia) {\n\t\treturn window.matchMedia(media).matches;\n\t}\n\treturn (window.styleMedia || window.media).matchMedium(media);\n}\n\n/**\n * Array.from or a simple shim for non-supporting browsers\n *\n * @param  {{length: number}} arrayLike\n * @return {array}\n */\nfunction arrayFrom(arrayLike) {\n\tif (Array.from) {\n\t\treturn Array.from(arrayLike);\n\t}\n\tvar array = [];\n\tfor (var i = 0; i < arrayLike.length; i++) {\n\t\tarray[i] = arrayLike[i];\n\t}\n\treturn array;\n}\n\nstartObserving();\n\nreturn api;\n\n}));\n\n})(window, document);\n\n\n//# sourceURL=webpack:///../node_modules/cq-prolyfill/cq-prolyfill.js?")},595:function(__unused_webpack_module,__webpack_exports__,__webpack_require__){"use strict";eval("__webpack_require__.r(__webpack_exports__);\n// extracted by mini-css-extract-plugin\n\n\n//# sourceURL=webpack:///./sass/main.scss?")},417:function(__unused_webpack_module,__unused_webpack_exports,__webpack_require__){eval("// require main build files\n__webpack_require__(595); \n__webpack_require__(234);\n\n//components\n__webpack_require__(536);\n\n//You should not need to add anything else to this file\n//BPAs go in /src/components/components.js\n\n//# sourceURL=webpack:///./build.js?")},536:function(){eval("//list each component you need to pull in\n\n// require('./bpa-folder/index.js');\n\n//# sourceURL=webpack:///./components/components.js?")},234:function(__unused_webpack_module,__webpack_exports__,__webpack_require__){"use strict";eval("__webpack_require__.r(__webpack_exports__);\n/* harmony import */ var _include_index__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(465);\n// /*!\n//  * global_vars is a file particular to your site\n//  * it contains base functions that are likely but not always used\n//  **/\n\n// check for buildinfo and add classes to body tag\n(function(){\n\n    if ( window.buildinfo !== undefined ){\n        var b = document.getElementsByTagName('body')[0]; \n        b.setAttribute( 'data-buildver', window.buildinfo.ver );\n        b.setAttribute( 'data-sitetemplate', window.buildinfo.template );\n\n        /*\n             // how to style against certain versions\n             body[data-buildver^=\"2.0\"]{\n             margin-top: 100px;\n             }\n         */\n    }\n\n})();\n\n\n\n__webpack_require__(429);\n__webpack_require__(465);\nvar cq = __webpack_require__(233)({ \n    preprocess: true\n});\n\nFS.events.onElementShown('panel', function ($element) {\n    cq.reevaluate(false, false, $.makeArray($element));\n});\n\nFS.events.onElementDialogShown('all', function ($element) {\n    cq.reevaluate(false, false, $.makeArray($element));\n});\n\n\n_include_index__WEBPACK_IMPORTED_MODULE_0__.INITIATE_ALL.init();\n\n\n\n//# sourceURL=webpack:///./js/global.js?")},465:function(__unused_webpack_module,__webpack_exports__,__webpack_require__){"use strict";eval("// ESM COMPAT FLAG\n__webpack_require__.r(__webpack_exports__);\n\n// EXPORTS\n__webpack_require__.d(__webpack_exports__, {\n  INITIATE_ALL: function() { return /* binding */ INITIATE_ALL; }\n});\n\n;// CONCATENATED MODULE: ./js/include/config-vars.js\nconst $body = $('body'),\n\t\t\t\t$html = $('html'),\n\t\t\t\t$pageBodyWrapper = $('#fsPageBodyWrapper'),\n\t\t\t\t$header = $('#fsHeader'),\n\t\t\t\t$section = $('section'),\n\t\t\t\tisHome = $('.home').length,\n\t\t\t\tnotDraftMode = !$('.fsDraftMode').length, // if (isHome && notDraftMode)...\n\t\t\t\tnotComposeMode = !$('.fsComposeMode').length,\n\t\t\t\t$window = $(window),\n\n\t\t\t\t// Constant Custom Classes\n\t\t\t\t// ================================,\n\t\t\t\t$navMain = $('.nav-main'),\n\t\t\t\t$navMain_level1 = $navMain.find('.fsNavLevel1'),\n\t\t\t\t$navSub = $('.nav-sub'),\n\t\t\t\tsectionTitle = $navMain.find('li[class*=\"fsNavCurrentPage\"] > a').first().text(),\n\t\t\t\t$navMainMobile = $('.nav-main-mobile'),\n\t\t\t\tmobileMenuToggle = '.mobile-toggle',\n\t\t\t\tmobileMenuClose = '.mobile-close',\n\t\t\t\t$mobileParentNavs = $navMainMobile.find('.fsNavParentPage'),\n\n\t\t\t\t// Element Specific Variables\n\t\t\t\t// ================================,\n\t\t\t\t$calendarGrid = $('.fsCalendar.fsGrid'),\n\n\t\t\t\t// Slideshow Subtype\n\t\t\t\t// ================================,\n\t\t\t\t$slideshowSubtype = $('.fsSlideshow'),\n\n\t\t\t\t// Specific Custom Classes\n\t\t\t\t// ================================,\n\t\t\t\t$siteSearch = $('.site-search'),\n\t\t\t\t$searchToggle = $('.search-toggle'),\n\n\t\t\t\t// Styles Manager Variables\n\t\t\t\t// ================================\n\t\t\t\t$tableStyle = $('table.table-styled, table.fsElementTable'),\n\t\t\t\t     \t\n\t\t\t\t// Enhancements\n\t\t\t\t// ================================\n\t\t\t\t$hero = $('.hero')\n\n\t;\n;// CONCATENATED MODULE: ./js/internal/responsive-calendar-grid.js\n// ===================================================\n// Calendar Element GRid Subtype Responsive Breakdown\n// ===================================================\n\n\nconst RESPONSIVE_CALENDAR_GRID = {\n\n    init () {\n        this.responsiveCalendarGrid();\n    }, \n\n    responsiveCalendarGrid () {\n        var eventview,\n        scrollUp,\n\n        onClickGridEvent = function onClickGridEvent(event) {\n\n            var element = $(event.target).closest('.fsCalendar.fsGrid'),\n                daybox = $(event.target).closest('.fsCalendarDaybox').hasClass(\"fsCalendarWeekendDayBox\") ? $(event.target).closest('div[tabIndex]') : $(event.target).closest('.fsCalendarDaybox'),\n                offsetTop = 0,\n                date,\n                eventview = element.find('#event-view')\n                ;\n\n            element.find('.fsCalendarEventGrid .fsCalendarDaybox, .fsCalendarWeekendDayBox > div').removeClass('selected');\n\n            if (eventview.length && daybox.length) {\n                date = daybox.clone();\n                daybox.addClass('selected');\n\n                offsetTop = eventview.offset().top - 16;\n                eventview.find(\"> div\").remove();\n                eventview.prepend(date);\n            }\n\n            if ($(window).width() < 900) {\n                $('html,body').animate({\n                    'scrollTop': offsetTop\n                }, 450);\n            }\n        },\n\n        onClickScrollUp = function onClickScrollUp(element) {\n            $('html,body').animate({\n                'scrollTop': element.offset().top\n            }, 450);\n            if(element.find(\".selected\").length) {\n                element.find(\".selected\").focus();\n            } else {\n                element.find(\".fsCalendarDaybox[tabIndex], .fsCalendarWeekendDayBox > div[tabIndex]\").last().focus();\n            }\n        },\n\n        onGridUpdate = function(element) {\n\n            if (element.hasClass('fsCalendar fsGrid')) {\n                var id = element.attr('id'),\n                    i = 0,\n                    max = 20,\n                    grid,\n                    checkGrid = setInterval(function() {\n                        grid = $('#' + id).find('.fsCalendarEventGrid');                        \n\n                        if (grid.length) {\n                            clearInterval(checkGrid);\n                            initCalendar(grid);\n                        }\n\n                        if (i++ > max) {\n                            clearInterval(checkGrid);\n                        }\n                    }, 20);\n            }\n        },\n\n        initCalendar = function initCalendar(element) {\n\n            var eventview = $('<div id=\"event-view\" />').insertAfter(element),\n                scrollUp = $('<button class=\"scroll-up\">Back Up To Calendar</button>').appendTo(eventview),\n                todayDate = element.find('.fsCalendarToday').clone().removeClass('fsCalendarToday')\n                ;\n\n            eventview.each(function() {\n                $(this).prepend(todayDate);\n            });\n\n            scrollUp.on('click', function() {\n                onClickScrollUp(element);\n            });\n\n            element.find('.fsCalendarDaybox:not(.fsCalendarWeekendDayBox), .fsCalendarWeekendDayBox > div').has('.fsCalendarInfo').attr(\"tabIndex\", 0);\n\n            element.on('click', '.fsCalendarDaybox:not(.fsCalendarWeekendDayBox), .fsCalendarWeekendDayBox > div ', onClickGridEvent);\n            element.on(\"keydown\", '.fsCalendarDaybox:not(.fsCalendarWeekendDayBox), .fsCalendarWeekendDayBox > div ', function(event) { \n                var keyCode = (event.keyCode ? event.keyCode : event.which);   \n                if (keyCode == 13 && $(window).width() < 900) { //899 and down we consider mobile for the calendar grid\n                    $(this).trigger('click');\n                    eventview.find(\":focusable\").eq(0).focus();\n                }\n            });\n            eventview.on(\"keydown\", function(e) {\n                var focusableEls = eventview[0].querySelectorAll('a[href]:not([disabled]), div[tabIndex], .scroll-up'),\n                  firstFocusableEl = focusableEls[0],\n                  KEYCODE_TAB = 9,\n                  isTabPressed = (e.key === 'Tab' || e.keyCode === KEYCODE_TAB);\n                if(!isTabPressed && !e.shiftKey) { \n                    return; \n                }\n                if((e.shiftKey && isTabPressed) && document.activeElement === firstFocusableEl) {\n                    if(element.find(\".selected\").length) {\n                        element.find(\".selected\").focus();\n                    } else {\n                        element.find(\".fsCalendarDaybox[tabIndex], .fsCalendarWeekendDayBox > div[tabIndex]\").last().focus();\n                    }\n                    e.preventDefault();\n                }\n            });\n        },\n\n        nextDay = function(element) {\n            const totalRows = element.find(\".fsCalendarRow\").length;\n            const selectedRow = element.find(\".selected\").parent().index() - 1;\n            for(let i = selectedRow; i < totalRows; i++) {\n                if(element.find(\".fsCalendarRow:eq(\"+i+\") .selected\").length) { //first loop\n                    if(element.find(\".fsCalendarRow:eq(\"+i+\") .selected\").is(\":last-child\") || !element.find(\".fsCalendarRow:eq(\"+i+\") .selected\").nextAll(\".fsStateHasEvents\").length) {\n                        continue;\n                    } else {\n                        return element.find(\".fsCalendarRow:eq(\"+i+\") .selected\").nextAll(\".fsStateHasEvents\").first();\n                    }\n                } else {\n                    if(element.find(\".fsCalendarRow:eq(\"+i+\") .fsStateHasEvents\").length) {\n                        return element.find(\".fsCalendarRow:eq(\"+i+\") .fsStateHasEvents\").first();\n                    }\n                }\n            }\n        };\n\n        FS.events.onElementUpdated('Calendar', function($element) {\n            onGridUpdate($element);\n        });\n\n        $('.fsGrid .fsCalendarEventGrid').each(function() {\n            initCalendar( $(this) );\n        });\n    }\n\n}; //end RESPONSIVE_CALENDAR_GRID\n\n;// CONCATENATED MODULE: ./js/include/responsive-slideshow-subtype.js\n// ================================================\n// Element Slideshow Subtype Responsive Breakdown\n// ================================================\n\n\n\n/*\nIf you have Slideshow subtype elements that\nshouldn't have the slick control wrapping,\nfill out the custom class(es) here\n*/\nconst excludedClasses = [\n\t'hero-banner',\n\t// 'another-class'\n];\n\n\nconst RESPONSIVE_SLIDESHOW_SUBTYPE = {\n\n\tinit () {\n\n\t\tthis.responsiveSlideshows();\n\n\t}, \n\n\tresponsiveSlideshows () {\n\n\t\t// var options,\n\t\t// update breakpoint variables here\n\t\tvar bpOneSlide = 600,\n\t\t\tbpTwoSlides = 800,\n\t\t\tbpThreeSlides = 1000,\n\t\t\tbpFourSlides = 1100\n\t\t;\n\n\t\t$slideshowSubtype.find('.fsElementSlideshow').each(function () {\n\n\t\t\tvar target = this;\n\t\t\tvar slidesToShow = $(target).data('slides-to-show');\n\n\t\t\t//determines slides to show & and slides to scroll\n\t\t\tvar stsTwo = slidesToShow < 2 ? slidesToShow : 2;\n\t\t\tvar stsThree =  slidesToShow < 3 ? slidesToShow : 3;\n\t\t\tvar stsFour = slidesToShow < 4 ? slidesToShow : 4;\n\n\n\t\t\tvar  customSlick = [{\n\t\t\t\tbreakpoint: bpFourSlides,\n\t\t\t\tsettings: {\n\t\t\t\t\tslidesToShow: stsFour,\n\t\t\t\t\tslidesToScroll: stsFour\n\t\t\t\t}\n\t\t\t}, {\n\t\t\t\tbreakpoint: bpThreeSlides,\n\t\t\t\tsettings: {\n\t\t\t\t\tslidesToShow: stsThree,\n\t\t\t\t\tslidesToScroll: stsThree\n\t\t\t\t}\n\t\t\t}, {\n\t\t\t\tbreakpoint: bpTwoSlides,\n\t\t\t\tsettings: {\n\t\t\t\t\tslidesToShow: stsTwo,\n\t\t\t\t\tslidesToScroll: stsTwo\n\t\t\t\t}\n\t\t\t}, {\n\t\t\t\tbreakpoint: bpOneSlide,\n\t\t\t\tsettings: {\n\t\t\t\t\tslidesToShow: 1,\n\t\t\t\t\tslidesToScroll: 1\n\t\t\t\t}\n\n\t\t\t}];\n\t\t\t\t\n\t\t\tif( $(target).hasClass('slick-initialized')) {\n\t\t\t\t// responsive slick only for non-Resources horizontal slideshows\n\t\t\t\tif($(target).parents(\".fsSlideshow\").hasClass(\"fsSlideshowHorizontal\")) {\n\t\t\t\t\t$(target).slick( 'slickSetOption', 'responsive', customSlick, true);\n\t\t\t\t}\n\t\t\t\t//wrapping slick controls\n\t\t\t\t$(target).each(function() {\n\t\t\t\t\tif(!excludedClasses.some(x => [...$(target).parents(\".fsSlideshow\")[0].classList].includes(x))) {\n\t\t\t\t\t\tRESPONSIVE_SLIDESHOW_SUBTYPE.slideshowControls($(this).slick(\"getSlick\"), target);\n\t\t\t\t\t}\n\t\t\t\t});\n\t\t\t} else {\n\t\t\t\t// set options on init for Resource slideshows\n\t\t\t\t$(target).on('init', function(event,slick){\n\t\t\t\t\tsetTimeout( function(){\n\t\t\t\t\t\t// responsive slick only for horizontal slideshows\n\t\t\t\t\t\tif($(target).parents(\".fsSlideshow\").hasClass(\"fsSlideshowHorizontal\")) {\n\t\t\t\t\t\t\t$(target).slick( 'slickSetOption', 'responsive', customSlick, true);\n\t\t\t\t\t\t}\n\t\t\t\t\t\t//wrapping slick controls on page load\n\t\t\t\t\t\tif(!excludedClasses.some(x => [...$(target).parents(\".fsSlideshow\")[0].classList].includes(x))) {\n\t\t\t\t\t\t\tRESPONSIVE_SLIDESHOW_SUBTYPE.slideshowControls(slick, target);\n\t\t\t\t\t\t}\n\t\t\t\t\t},1);\n\t\t\t\t});\n\t\t\t}\n\n\t\t\t$(target).on('breakpoint', function(event, slick) {\n\t\t\t\t//removing slick navigation\n\t\t\t\t$(target).find(\".slick-navigation > *\").eq(0).unwrap();\n\t\t\t\t//wrapping slick controls\n\t\t\t\tif(!excludedClasses.some(x => [...$(target).parents(\".fsSlideshow\")[0].classList].includes(x))) {\n\t\t\t\t\tRESPONSIVE_SLIDESHOW_SUBTYPE.slideshowControls(slick, target);\n\t\t\t\t}\n\t\t\t});\n\t\t\t\n\t\t});\n\n\t\tFS.events.onElementDialogShown('all', function ($element) {\n\t\t  $element.find(\".fsElementSlideshow\").on('init', function(event,slick){\n\t\t\t\tsetTimeout( function(){\n\t\t\t\t\t//wrapping slick controls on dialog load\n\t\t\t\t\tRESPONSIVE_SLIDESHOW_SUBTYPE.slideshowControls(slick, $element.find(\".fsElementSlideshow\"));\n\t\t\t\t},1);\n\t\t\t});\n\t\t});\n\n\t},\n\n\tslideshowControls (slick, target) {\n\n\t\tif(slick.$nextArrow.length) {\n\t\t  slick.$nextArrow.appendTo($(target));\n\t\t  slick.$nextArrow.wrap('<div class=\"slick-navigation\"/>');\n\t\t  if($(target).data(\"vertical\")) {\n\t\t    slick.$prevArrow.prependTo($(target));\n\t\t  } else {\n\t\t    slick.$prevArrow.insertBefore(slick.$nextArrow);\n\t\t  }\n\n\t\t\tif($(target).data(\"dots\")) {\n\t\t\t\tslick.$dots.insertBefore(slick.$nextArrow);\n\t\t\t}\n\t\t}\n\t}\n\n}; //end RESPONSIVE_SLIDESHOW_SUBTYPE\n\n;// CONCATENATED MODULE: ./js/include/accessibility.js\n// ================================\n// ADA\n// ================================\n\n__webpack_require__(509);\n\n\n\nconst ACCESSIBILITY = {\n\n  init () {\n    if (notDraftMode) {\n      this.accessibleNavigation();\n    }\n    this.accessibleSearch();\n  },\n\n  // ==============================================================\n  // Accessible Main Navigation\n  // allows user to tab through main navigation and dropdown links\n  // ==============================================================\n\n  accessibleNavigation () {\n\n    $navMain_level1.accessibility_menu();\n\n  },\n\n  // ==============================================================\n  // Accessible Search Flyout\n  // allows user to tab through a search flyout\n  // ==============================================================\n\n  accessibleSearch () {\n\n    $siteSearch.on('focusin click', function() {\n      $(this).addClass(\"search-open\");\n    });\n\n    $siteSearch.on('focusout', function() {\n      $(this).removeClass(\"search-open\");\n    });\n\n    $(document).on('click', function(e) {\n      if (!$(e.target).closest($siteSearch).length) {\n        $siteSearch.removeClass(\"search-open\");\n      }\n    });\n\n  }\n\n}; //end ACCESSIBILITY\n\n\n\n// EXTERNAL MODULE: ./js/internal/utilities.js\nvar utilities = __webpack_require__(239);\n;// CONCATENATED MODULE: ./js/include/navigation.js\n// ================================\n// NAVIGATION\n// ================================\n\n\n\n\n\nconst NAVIGATION = {\n\n  init () {\n    this.offCanvasMenu();\n    this.collapseSubNav();\n    this.navExpanders();\n    this.navigationsSettings();\n  }, \n  \n\n  // Off Canvas Menu\n  // ==================================================\n\n  offCanvasMenu () {\n      // Toggle attribute of the body\n      $(mobileMenuToggle).on('click', function() {\n          $body.toggleClass('drawer-is-active');\n          if($body.hasClass('drawer-is-active')) {\n              $('.fsMenu').find(':focusable').eq(0).focus();\n          }\n      });\n\n      $('.search-trigger').on('click', function() {\n        $body.toggleClass('drawer-is-active');\n        if($body.hasClass('drawer-is-active')) {\n            $('.fsMenu').find(':focusable').eq(0).focus();\n        }\n      });\n\n\n\n\n      $(mobileMenuClose).on('click', function() {\n          $body.removeClass('drawer-is-active');\n          $(mobileMenuToggle).focus();\n      });\n\n      if($('.fsMenu').length) {\n          (0,utilities/* trapFocus */.oM)(document.querySelector('.fsMenu'), function() {\n              // what runs if they hit \"esc\" and leave the menu\n              $body.removeClass('drawer-is-active');\n              $(mobileMenuToggle).focus();\n          });\n      }\n\n      // Remove attribute on the bottom if anything other than\n      // what is mentioned is clicked on\n      $(document).on('click', function(event) {\n          if (!$(event.target).closest('#fsMenu, .mobile-toggle, .search-trigger').length) {\n              $body.removeClass('drawer-is-active');\n          }\n      });\n  },\n\n  // ========================\n  // nav settings\n\nnavigationsSettings () {\n    const $mobileNavSub = $('.nav-sub.mobile-nav-sub'),\n          $horizontalNav = $('.horizontal-nav'),\n          $bread = $('.site-bread.fsBreadcrumb');\n          // $mainNav = $('.fsHeader').find('.nav-main .fsNavLevel1');\n          // $mainNav_titleText = $mainNav.find('> li[class*=\"fsNavCurrentPage\"] > a').text(),\n          // $breadCrumbLocation = $bread.find('ul > li[aria-current*=\"location\"]').text();\n    $horizontalNav.insertBefore('#fsPageBodyWrapper');\n    $mobileNavSub.insertBefore('#fsPageBodyWrapper');\n    $bread.insertBefore('#fsPageBody');\n    if ($horizontalNav.find('nav ul li').length !== 0) {\n      $horizontalNav.removeClass('horizontal-nav-empty');\n      $body.removeClass('no-horizontal-nav');\n    } else {\n      $horizontalNav.addClass('horizontal-nav-empty');\n      $body.addClass('no-horizontal-nav');\n    }\n    if ( $bread.find('nav ul li').length !== 0 ) {\n      // $('<li class=\"to-home\"><a href=\"/\">Home</a><span aria-hidden=\"true\" class=\"fsNavBreadcrumbSeperator\"></span></li>').prependTo($bread.find(\"ul\"));\n      $bread.removeClass('empty-bread');\n    } else {\n      $bread.addClass('empty-bread');\n    }\n  },\n\n\n  // collapsable subnav on mobile\n  // ==================================================\n\n  collapseSubNav () {\n    $navSub.each(function() {\n      const _ = $(this),\n            $navSub_container = _.find('> header'),\n            $navSub_title = _.find('> header > .fsElementTitle'),\n            buttonText = sectionTitle || $navSub_title.text();\n\n\n      if(!_.find('.fsNavLevel1').length) {\n        _.addClass('nav-sub-empty');\n      }\n\n      if(sectionTitle.length) {\n        $navSub_title.html(sectionTitle);\n      }\n\n      if(_.parents('.fsHeader').length) {\n\n        $navSub_container.append([\n         '<button class=\"nav-toggle\" aria-expanded=\"false\">',\n          '<span>Toggle '+buttonText+' Navigation</span>',\n         '</button>'\n       ].join('\\n'));\n\n        $navSub_container.on('click', function() {\n          _.toggleClass('active-nav');\n          $(this).find(\".nav-toggle\")\n            .attr(\"aria-expanded\",  $(this).find(\".nav-toggle\").attr(\"aria-expanded\") !== \"true\");\n        });\n\n        // nav-sub remove click elsewhere\n        $(document).on('click', function(event) {\n          if (!$(event.target).closest(_).length && _.hasClass('active-nav')) {\n            _.removeClass('active-nav')\n              .find(\".nav-toggle\").attr(\"aria-expanded\", \"false\");\n          }\n        });\n\n        _.on(\"focusout\", function(event) {\n          setTimeout(function() {\n            var hasFocus = !! (_.find(':focus').length > 0);\n            if (! hasFocus && _.hasClass('active-nav') && !$(event.target).is(\".nav-toggle\")) {\n              _.removeClass('active-nav')\n                .find(\".nav-toggle\").attr(\"aria-expanded\", \"false\");\n            }\n          }, 10);\n        });\n\n      }\n\n    });\n  },\n\n  // =======================\n  // nav expanders\n\n  navExpanders() {\n    var el = $('.nav-sub .fsNavParentPage, .mobile-main-nav li.fsNavParentPage, .mobile-district-nav .fsNavParentPage');\n    \n    el.each(function() {\n        var navItem = $(this);\n        var linkHeight = navItem.find('> a').outerHeight(); // Height of the link\n    \n        // Add the expansion button and adjust its height\n        if (navItem.find('> .fsNavPageInfo').length) {\n            navItem.addClass('has-expander');\n            navItem.append('<button aria-expanded=\"false\" class=\"expander\">expand this section</button>');\n        }\n    \n        // Recalculate the height of the expansion button for each .expander\n        navItem.find('.expander').css('height', `${linkHeight}px`);\n    \n        // The rest of the code remains unchanged\n        if (navItem.hasClass('fsNavCurrentPageAncestor') || navItem.hasClass('fsNavCurrentPage')) {\n            navItem.addClass('expanded');\n            navItem.attr('aria-expanded', 'true');\n        }\n        navItem.find('> .fsNavPageInfo > ul').hide();\n        if (navItem.hasClass('expanded')) {\n            navItem.find('> .fsNavPageInfo > ul').show();\n        }\n    });\n    \n    $('.expander').click(function(e) {\n        var parentNavItem = $(this).closest('li');\n        var buttonExpander = parentNavItem.find('> button.expander');\n    \n        // Update the height of the expansion button based on the height of the associated link\n        var linkHeight = parentNavItem.find('> a').outerHeight();\n        buttonExpander.css('height', `${linkHeight}px`);\n    \n        parentNavItem.parent().toggleClass('has-expanded');\n        parentNavItem.toggleClass('expanded');\n        var isExpanded = parentNavItem.hasClass('expanded');\n        $(this).attr('aria-expanded', isExpanded ? 'true' : 'false');\n        parentNavItem.siblings().removeClass('expanded').find('.expander').attr('aria-expanded', 'false');\n        parentNavItem.find('> div > ul').slideToggle(400);\n        parentNavItem.siblings().find('> div > ul').hide(400);\n        e.preventDefault();\n    });\n    \n},\n\n\n  // DoubleTap\n// =======================\ndoubleTapNavs () {\n\n  const isTouch = ('ontouchstart' in document.documentElement);\n\n  if ( isTouch ){\n      $('html').addClass('touch-device');\n  } else{\n      $('html').addClass('no-touch-device');\n  }\n\n  const $html = $('html');\n\n  if( $html.hasClass('touch-device') ) {\n      $('.nav-main .fsNavLevel1 > li.fsNavParentPage').doubleTapToGo();\n      $('.header-portals-nav .fsNavLevel1  > li.fsNavParentPage').doubleTapToGo();\n      $('.quicklinks-nav .fsNavLevel1 > li.fsNavParentPage').doubleTapToGo();\n  }\n\n}\n\n\n}; //end NAVIGATION\n\n;// CONCATENATED MODULE: ./js/include/default-styles.js\n// ================================\n// DEFAULT STYLE\n// ================================\n__webpack_require__(517);\n__webpack_require__(534);\n\n\n\nconst DEFAULT_STYLES = {\n\tinit() {\n\t\tthis.iconLibrarySHOW();\n\t\tif ($tableStyle.length) {\n\t\t\tthis.tableStyle();\n\t\t}\n\n\t\tif (notComposeMode) {\n\t\t\tthis.hpFeeds();\n\t\t\tthis.hpAnimations();\n\t\t}\n\t},\n\n\ttableStyle() {\n\t\t// update $tableStyle variable definition in js/include/config-vars.js\n\n\t\tfunction tableStyleWrap() {\n\t\t\t$tableStyle.each(function() {\n\t\t\t\tif (!$(this).parent().hasClass('table-overflow')) {\n\t\t\t\t\t$(this).wrap('<div class=\"table-overflow\" tabindex=\"0\" />');\n\t\t\t\t}\n\t\t\t});\n\t\t}\n\n\t\tif (notComposeMode) {\n\t\t\ttableStyleWrap();\n\t\t}\n\n\t\t// wrap & unwrap table when toggling compose mode on/off\n\t\tFS.events.onComposeModeChanged(function(enabled) {\n\t\t\tif (enabled === false) {\n\t\t\t\ttableStyleWrap();\n\t\t\t} else if (enabled === true) {\n\t\t\t\t$tableStyle.each(function() {\n\t\t\t\t\tif ($(this).parent().hasClass('table-overflow')) {\n\t\t\t\t\t\t$(this).unwrap();\n\t\t\t\t\t}\n\t\t\t\t});\n\t\t\t}\n\t\t});\n\t},\n\n\thpFeeds() {\n\t\tconst feeds = $('.hp-feeds');\n\n\t\tfeeds.each(function() {\n\t\t\tconst area = $(this),\n\t\t\t\twhereToSlick = area.find('>.fsElementContent'),\n\t\t\t\tarrowContainer = $(\"<div class='arrow-container'></div>\"),\n\t\t\t\tarrowDotsContainer = $(\"<div class='arrow-dots-container'></div>\");\n\n\t\t\tarrowDotsContainer.appendTo(area);\n\t\t\tarrowContainer.appendTo(area.find(arrowDotsContainer));\n\n\t\t\tconst options = {\n\t\t\t\tfeed: {\n\t\t\t\t\tper: 5\n\t\t\t\t},\n\t\t\t\tloadMore: false,\n\t\t\t\tdateFormat: 'D MMM YYYY',\n\t\t\t\ttemplates: {\n\t\t\t\t\tInstagram: [\n\t\t\t\t\t\t'<article class=\"feed-article twitter\">',\n\t\t\t\t\t\t'<img class=\"bg-image\" src={image} alt=\"Twitter image posted by @{poster_name} at {date}\"/>',\n\n\t\t\t\t\t\t'<div class=\"text-wrapper\">',\n\n\t\t\t\t\t\t// header\n\t\t\t\t\t\t'<div class=\"header-content\">',\n\t\t\t\t\t\t'<img src={poster_image} alt=\"Profile picture of @{poster_name} at {date}\"/>',\n\t\t\t\t\t\t'<a href=\"{full_url}\" target=\"_blank\" class=\"profile-link\">{poster_display_name}<span>@{poster_name}</span></a>',\n\t\t\t\t\t\t'<a href=\"{full_url}\" target=\"_blank\" class=\"feed-url\">{source.source}</a>',\n\t\t\t\t\t\t'</div>',\n\n\t\t\t\t\t\t// content\n\t\t\t\t\t\t'<div class=\"article-content\">',\n\t\t\t\t\t\t'<div class=\"article-text\">{unformatted_message}</div>',\n\t\t\t\t\t\t'</div>',\n\n\t\t\t\t\t\t// footer\n\t\t\t\t\t\t'<div class=\"article-footer\">',\n\t\t\t\t\t\t'<div class=\"article-date\">{date}</div>',\n\t\t\t\t\t\t'</div>',\n\n\t\t\t\t\t\t'</div>',\n\n\t\t\t\t\t\t'</article>'\n\t\t\t\t\t],\n\n\t\t\t\t\t'X (Twitter)': [\n\t\t\t\t\t\t'<article class=\"feed-article twitter\">',\n\t\t\t\t\t\t'<img class=\"bg-image\" src={image} alt=\"Twitter image posted by @{poster_name} at {date}\"/>',\n\n\t\t\t\t\t\t'<div class=\"text-wrapper\">',\n\n\t\t\t\t\t\t// header\n\t\t\t\t\t\t'<div class=\"header-content\">',\n\t\t\t\t\t\t'<img src={poster_image} alt=\"Profile picture of @{poster_name} at {date}\"/>',\n\t\t\t\t\t\t'<a href=\"{full_url}\" target=\"_blank\" class=\"profile-link\">{poster_display_name}<span>@{poster_name}</span></a>',\n\t\t\t\t\t\t'<a href=\"{full_url}\" target=\"_blank\" class=\"feed-url\">{source.source}</a>',\n\t\t\t\t\t\t'</div>',\n\n\t\t\t\t\t\t// content\n\t\t\t\t\t\t'<div class=\"article-content\">',\n\t\t\t\t\t\t'<div class=\"article-text\">{unformatted_message}</div>',\n\t\t\t\t\t\t'</div>',\n\n\t\t\t\t\t\t// footer\n\t\t\t\t\t\t'<div class=\"article-footer\">',\n\t\t\t\t\t\t'<div class=\"article-date\">{date}</div>',\n\t\t\t\t\t\t'</div>',\n\n\t\t\t\t\t\t'</div>',\n\n\t\t\t\t\t\t'</article>'\n\t\t\t\t\t]\n\t\t\t\t}\n\t\t\t};\n\n\t\t\tarea.fsFeedPull(options, function() {\n\t\t\t\tconst desc = area.find('.article-text');\n\n\t\t\t\twhereToSlick.slick({\n\t\t\t\t\tslidesToShow: 1,\n\t\t\t\t\tslidesToScroll: 1,\n\t\t\t\t\tcentreMode:true,\n\t\t\t\t\tinfinite: true,\n\t\t\t\t\tdots: true,\n\t\t\t\t\tarrows: true,\n\t\t\t\t\tmobileFirst: true,\n\t\t\t\t\tappendArrows: feeds.find('.arrow-container'),\n\t\t\t\t\tappendDots: feeds.find('.arrow-container'),\n\t\t\t\t\tvariableWidth: true,\n\t\t\t\t});\n\n\t\t\t\tarea.find('article').each(function() {\n\t\t\t\t\tconst _ = $(this),\n\t\t\t\t\t\ttextWrapper = $(\".text-wrapper\"),\n\t\t\t\t\t\ttitle = _.find('.header-content');\n\n\t\t\t\t\tlet titleHeight = title.outerHeight();\n\t\t\t\t\tlet translationValue = `calc(100% - ${titleHeight}px)`;\n\n\t\t\t\t\ttextWrapper.css('transform', `translate(-50%, ${translationValue})`);\n\n\t\t\t\t\tsetTimeout(function() {\n\t\t\t\t\t\tlet titleHeight = title.outerHeight();\n\t\t\t\t\t\tlet translationValue = `calc(100% - ${titleHeight}px)`;\n\n\t\t\t\t\t\ttextWrapper.css('transform', `translate(-50%, ${translationValue})`);\n\t\t\t\t\t}, 300);\n\n\t\t\t\t\t$(window).resize(function() {\n\t\t\t\t\t\tlet titleHeight = title.outerHeight();\n\t\t\t\t\t\tlet translationValue = `calc(100% - ${titleHeight}px)`;\n\n\t\t\t\t\t\ttextWrapper.css('transform', `translate(-50%, ${translationValue})`);\n\n\t\t\t\t\t\tsetTimeout(function() {\n\t\t\t\t\t\t\tlet titleHeight = title.outerHeight();\n\t\t\t\t\t\t\tlet translationValue = `calc(100% - ${titleHeight}px)`;\n\n\t\t\t\t\t\t\ttextWrapper.css('transform', `translate(-50%, ${translationValue})`);\n\t\t\t\t\t\t}, 300);\n\t\t\t\t\t});\n\t\t\t\t});\n\t\t\t});\n\t\t});\n\t},\n\n\thpAnimations() {\n\t\t$('.hp-feeds-container, .hp-post-calendar').waypoint(\n\t\t\tfunction(direction) {\n\t\t\t\tif (direction === 'down') {\n\t\t\t\t\t$(this.element).addClass('is-in-view');\n\t\t\t\t} else {\n\t\t\t\t\t$(this.element).removeClass('is-in-view');\n\t\t\t\t}\n\t\t\t},\n\t\t\t{\n\t\t\t\toffset: '40%'\n\t\t\t}\n\t\t);\n\t},\n\n\ticonLibrarySHOW() {\n\t\t$('.icon-library .icon-add-user-solid').appendTo('.icon-library .icon-add-user');\n\t\t$('.icon-library .icon-alumni-solid').appendTo('.icon-library .icon-alumni');\n\t\t$('.icon-library .icon-apple-solid').appendTo('.icon-library .icon-apple');\n\t\t$('.icon-library .icon-apply-solid').appendTo('.icon-library .icon-apply');\n\t\t$('.icon-library .icon-backpack-solid').appendTo('.icon-library .icon-backpack');\n\t\t$('.icon-library .icon-badge-solid').appendTo('.icon-library .icon-badge');\n\t\t$('.icon-library .icon-bar-graph-solid').appendTo('.icon-library .icon-bar-graph');\n\t\t$('.icon-library .icon-baseball-solid').appendTo('.icon-library .icon-baseball');\n\t\t$('.icon-library .icon-block-solid').appendTo('.icon-library .icon-block');\n\t\t$('.icon-library .icon-book-stack-solid').appendTo('.icon-library .icon-book-stack');\n\t\t$('.icon-library .icon-book-solid').appendTo('.icon-library .icon-book');\n\t\t$('.icon-library .icon-brainstorm-solid').appendTo('.icon-library .icon-brainstorm');\n\t\t$('.icon-library .icon-briefcase-solid').appendTo('.icon-library .icon-briefcase');\n\t\t$('.icon-library .icon-bullhorn-solid').appendTo('.icon-library .icon-bullhorn');\n\t\t$('.icon-library .icon-bus-solid').appendTo('.icon-library .icon-bus');\n\t\t$('.icon-library .icon-calendar-solid').appendTo('.icon-library .icon-calendar');\n\t\t$('.icon-library .icon-camera-solid').appendTo('.icon-library .icon-camera');\n\t\t$('.icon-library .icon-certificate-solid').appendTo('.icon-library .icon-certificate');\n\t\t$('.icon-library .icon-chat-alt-solid').appendTo('.icon-library .icon-chat-alt');\n\t\t$('.icon-library .icon-chat-solid').appendTo('.icon-library .icon-chat');\n\t\t$('.icon-library .icon-check-solid').appendTo('.icon-library .icon-check');\n\t\t$('.icon-library .icon-chess-solid').appendTo('.icon-library .icon-chess');\n\t\t$('.icon-library .icon-church-solid').appendTo('.icon-library .icon-church');\n\t\t$('.icon-library .icon-city-solid').appendTo('.icon-library .icon-city');\n\t\t$('.icon-library .icon-classroom-solid').appendTo('.icon-library .icon-classroom');\n\t\t$('.icon-library .icon-cleat-solid').appendTo('.icon-library .icon-cleat');\n\t\t$('.icon-library .icon-clock-solid').appendTo('.icon-library .icon-clock');\n\t\t$('.icon-library .icon-college-solid').appendTo('.icon-library .icon-college');\n\t\t$('.icon-library .icon-connected-users-solid').appendTo('.icon-library .icon-connected-users');\n\t\t$('.icon-library .icon-construction-solid').appendTo('.icon-library .icon-construction');\n\t\t$('.icon-library .icon-credit-card-solid').appendTo('.icon-library .icon-credit-card');\n\t\t$('.icon-library .icon-cross-solid').appendTo('.icon-library .icon-cross');\n\t\t$('.icon-library .icon-diagram-solid').appendTo('.icon-library .icon-diagram');\n\t\t$('.icon-library .icon-diploma-solid').appendTo('.icon-library .icon-diploma');\n\t\t$('.icon-library .icon-directory-solid').appendTo('.icon-library .icon-directory');\n\t\t$('.icon-library .icon-diversity-solid').appendTo('.icon-library .icon-diversity');\n\t\t$('.icon-library .icon-droplet-solid').appendTo('.icon-library .icon-droplet');\n\t\t$('.icon-library .icon-email-solid').appendTo('.icon-library .icon-email');\n\t\t$('.icon-library .icon-family-solid').appendTo('.icon-library .icon-family');\n\t\t$('.icon-library .icon-faq-solid').appendTo('.icon-library .icon-faq');\n\t\t$('.icon-library .icon-flag-solid').appendTo('.icon-library .icon-flag');\n\t\t$('.icon-library .icon-folder-solid').appendTo('.icon-library .icon-folder');\n\t\t$('.icon-library .icon-football-solid').appendTo('.icon-library .icon-football');\n\t\t$('.icon-library .icon-gift-solid').appendTo('.icon-library .icon-gift');\n\t\t$('.icon-library .icon-global-diversity-solid').appendTo('.icon-library .icon-global-diversity');\n\t\t$('.icon-library .icon-globe-solid').appendTo('.icon-library .icon-globe');\n\t\t$('.icon-library .icon-google-classroom-solid').appendTo('.icon-library .icon-google-classroom');\n\t\t$('.icon-library .icon-graduation-solid').appendTo('.icon-library .icon-graduation');\n\t\t$('.icon-library .icon-group-alt-solid').appendTo('.icon-library .icon-group-alt');\n\t\t$('.icon-library .icon-group-solid').appendTo('.icon-library .icon-group');\n\t\t$('.icon-library .icon-handshake-solid').appendTo('.icon-library .icon-handshake');\n\t\t$('.icon-library .icon-heart-hand-alt-solid').appendTo('.icon-library .icon-heart-hand-alt');\n\t\t$('.icon-library .icon-heart-hand-solid').appendTo('.icon-library .icon-heart-hand');\n\t\t$('.icon-library .icon-heart-solid').appendTo('.icon-library .icon-heart');\n\t\t$('.icon-library .icon-info-solid').appendTo('.icon-library .icon-info');\n\t\t$('.icon-library .icon-interactive-solid').appendTo('.icon-library .icon-interactive');\n\t\t$('.icon-library .icon-jersey-solid').appendTo('.icon-library .icon-jersey');\n\t\t$('.icon-library .icon-laurel-solid').appendTo('.icon-library .icon-laurel');\n\t\t$('.icon-library .icon-leader-solid').appendTo('.icon-library .icon-leader');\n\t\t$('.icon-library .icon-leaf-solid').appendTo('.icon-library .icon-leaf');\n\t\t$('.icon-library .icon-lightbulb-solid').appendTo('.icon-library .icon-lightbulb');\n\t\t$('.icon-library .icon-link-solid').appendTo('.icon-library .icon-link');\n\t\t$('.icon-library .icon-location-solid').appendTo('.icon-library .icon-location');\n\t\t$('.icon-library .icon-lock-solid').appendTo('.icon-library .icon-lock');\n\t\t$('.icon-library .icon-map-solid').appendTo('.icon-library .icon-map');\n\t\t$('.icon-library .icon-medal-solid').appendTo('.icon-library .icon-medal');\n\t\t$('.icon-library .icon-medical-solid').appendTo('.icon-library .icon-medical');\n\t\t$('.icon-library .icon-menu-solid').appendTo('.icon-library .icon-menu');\n\t\t$('.icon-library .icon-microphone-alt-solid').appendTo('.icon-library .icon-microphone-alt');\n\t\t$('.icon-library .icon-microphone-solid').appendTo('.icon-library .icon-microphone');\n\t\t$('.icon-library .icon-microscope-solid').appendTo('.icon-library .icon-microscope');\n\t\t$('.icon-library .icon-mobile-phone-solid').appendTo('.icon-library .icon-mobile-phone');\n\t\t$('.icon-library .icon-money-solid').appendTo('.icon-library .icon-money');\n\t\t$('.icon-library .icon-music-solid').appendTo('.icon-library .icon-music');\n\t\t$('.icon-library .icon-name-badge-solid').appendTo('.icon-library .icon-name-badge');\n\t\t$('.icon-library .icon-newspaper-solid').appendTo('.icon-library .icon-newspaper');\n\t\t$('.icon-library .icon-open-door-solid').appendTo('.icon-library .icon-open-door');\n\t\t$('.icon-library .icon-paint-solid').appendTo('.icon-library .icon-paint');\n\t\t$('.icon-library .icon-parent-solid').appendTo('.icon-library .icon-parent');\n\t\t$('.icon-library .icon-park-solid').appendTo('.icon-library .icon-park');\n\t\t$('.icon-library .icon-party-solid').appendTo('.icon-library .icon-party');\n\t\t$('.icon-library .icon-pencil-solid').appendTo('.icon-library .icon-pencil');\n\t\t$('.icon-library .icon-phone-solid').appendTo('.icon-library .icon-phone');\n\t\t$('.icon-library .icon-picture-solid').appendTo('.icon-library .icon-picture');\n\t\t$('.icon-library .icon-pie-chart-solid').appendTo('.icon-library .icon-pie-chart');\n\t\t$('.icon-library .icon-piggy-bank-solid').appendTo('.icon-library .icon-piggy-bank');\n\t\t$('.icon-library .icon-ping-pong-solid').appendTo('.icon-library .icon-ping-pong');\n\t\t$('.icon-library .icon-plate-solid').appendTo('.icon-library .icon-plate');\n\t\t$('.icon-library .icon-playground-solid').appendTo('.icon-library .icon-playground');\n\t\t$('.icon-library .icon-podium-solid').appendTo('.icon-library .icon-podium');\n\t\t$('.icon-library .icon-question-solid').appendTo('.icon-library .icon-question');\n\t\t$('.icon-library .icon-rain-solid').appendTo('.icon-library .icon-rain');\n\t\t$('.icon-library .icon-reading-alt-solid').appendTo('.icon-library .icon-reading-alt');\n\t\t$('.icon-library .icon-reading-solid').appendTo('.icon-library .icon-reading');\n\t\t$('.icon-library .icon-report-card-solid').appendTo('.icon-library .icon-report-card');\n\t\t$('.icon-library .icon-robot-solid').appendTo('.icon-library .icon-robot');\n\t\t$('.icon-library .icon-school-solid').appendTo('.icon-library .icon-school');\n\t\t$('.icon-library .icon-screens-solid').appendTo('.icon-library .icon-screens');\n\t\t$('.icon-library .icon-settings-solid').appendTo('.icon-library .icon-settings');\n\t\t$('.icon-library .icon-shield-police-solid').appendTo('.icon-library .icon-shield-police');\n\t\t$('.icon-library .icon-snowflake-solid').appendTo('.icon-library .icon-snowflake');\n\t\t$('.icon-library .icon-soccer-solid').appendTo('.icon-library .icon-soccer');\n\t\t$('.icon-library .icon-star-david-solid').appendTo('.icon-library .icon-star-david');\n\t\t$('.icon-library .icon-star-solid').appendTo('.icon-library .icon-star');\n\t\t$('.icon-library .icon-store-solid').appendTo('.icon-library .icon-store');\n\t\t$('.icon-library .icon-sun-solid').appendTo('.icon-library .icon-sun');\n\t\t$('.icon-library .icon-task-solid').appendTo('.icon-library .icon-task');\n\t\t$('.icon-library .icon-teacher-solid').appendTo('.icon-library .icon-teacher');\n\t\t$('.icon-library .icon-teamwork-solid').appendTo('.icon-library .icon-teamwork');\n\t\t$('.icon-library .icon-theatre-solid').appendTo('.icon-library .icon-theatre');\n\t\t$('.icon-library .icon-trophies-solid').appendTo('.icon-library .icon-trophies');\n\t\t$('.icon-library .icon-skyward-solid').appendTo('.icon-library .icon-skyward');\n\t\t$('.icon-library .icon-thumbs-up-solid').appendTo('.icon-library .icon-thumbs-up');\n\t\t$('.icon-library .icon-tools-solid').appendTo('.icon-library .icon-tools');\n\t\t$('.icon-library .icon-translate-solid').appendTo('.icon-library .icon-translate');\n\t\t$('.icon-library .icon-tree-solid').appendTo('.icon-library .icon-tree');\n\t\t$('.icon-library .icon-trophy-solid').appendTo('.icon-library .icon-trophy');\n\t\t$('.icon-library .icon-user-solid').appendTo('.icon-library .icon-user');\n\t\t$('.icon-library .icon-video-solid').appendTo('.icon-library .icon-video');\n\t\t$('.icon-library .icon-virus-solid').appendTo('.icon-library .icon-virus');\n\t\t$('.icon-library .icon-warning-solid').appendTo('.icon-library .icon-warning');\n\t\t$('.icon-library .icon-wheelchair-solid').appendTo('.icon-library .icon-wheelchair');\n\t}\n}; //end DEFAULT_STYLES\n\n;// CONCATENATED MODULE: ./js/internal/fs-move-resource-image.js\n// ====================================================\n// Resources - Move Resource to another element\n// Ex: img or other container to use a background\n// ====================================================\n\n/* v1.0.0\n\tmoveResourceImage(ele, target) \n\n\tele = element with a data-image-sizes attribute\n\ttarget = element to apply the image to\n\n\tUsage:\n\n  // Import function within desired file for use: \n\n\timport {\n\t\t moveResourceImage\n\t} from '../internal/fs-move-resource-image';\n\n\tExample 1: \n\n\t\tmoveResourceImage('.custom-resource-element img','.img-out');\n\n\t** Note: if multiple instances on the same page usage is\n\twithin an each statement **\n\n\tExample 2: \n\n\t\t$('.custom-resource-element').each(function() {\n\t\t  var elementImage = $(this).find('img');\n\t\t  var imageDestination = $(this).find('picture');\n\n\t\t  moveResourceImage(elementImage, imageDestination);\n\t\t});\n\n\t** In Composer: \n\tElement Settings > Design > uncheck \"Scale media on load\"\n  \n*/\n\nfunction moveResourceImage(ele, target) {\n\n\tif ( ele.length === 0 || target.length === 0 ) { return false; }\n\n\t\tvar $this = $(ele).eq(0),\n\t\t\t  imgSizes = $this.attr('data-image-sizes'),\n\t\t\t\taspect = $this.attr('data-aspect-ratio');\n\n\t\t$(target).eq(0)\n\t\t\t.attr('data-image-sizes',imgSizes)\n\t\t\t.attr('data-aspect-ratio',aspect);\n\n\t\t// Composer function for finding image size\n\t\tFS.util.updateDynamicImages( $(target).eq(0) );\n\n};\n\n;// CONCATENATED MODULE: ./js/include/enhancements.js\n// ================================\n// ENHANCEMENTS\n// ================================\n\n\n\n\n\n\nconst ENHANCEMENTS = {\n\n  init () {\n\n    if(notComposeMode) {\n      // functions to not run in compose mode\n      this.heroBanner();\n    }\n    this.mainPagefsLayout();\n    this.footerElements();\n  }, \n\n  // Page Layout and Appearance\n  mainPagefsLayout() {\n    $('body:not(.home) .fsPageBodyWrapper .fsTwoColumnLayout:not(.fsElement):not(.fsLayout), body:not(.home) .fsPageBodyWrapper .fsThreeColumnLayout:not(.fsElement):not(.fsLayout)').each(function() {\n      $(this).addClass(\"fsLayout\");\n      $(this).wrap('<div class=\"fsDiv\" style=\"padding: 0px;\"></div>');\n      $(this).css({\n        'width': '100%',\n        'margin-left': '0px',\n        'margin-right': '0px'\n      });\n    });\n  },\n  // Page Layout and Appearance\n\n  footerElements () {\n\n    $('#fsPoweredByFinalsite').appendTo('.fsFooter .footer-utility > .fsElementContent');\n\n  },\n\n    // Main Hero\n  // ------------------------\n  heroBanner () {\n\n    const _hero = $('.hero-banner');\n\n    if ( _hero.length && notComposeMode ) {\n      $body.addClass('hasHero');\n    } else{\n      $body.addClass('noHero');\n    }\n\n    _hero.each(function() {\n\n      const thisHero = $(this);\n      thisHero.insertAfter('.fsHeader');\n\n\n      // Dynamically postion for controls \n      // mobile view\n      if ( thisHero.hasClass('fsSlideshow') ) {\n        const checkSlideshow = setInterval(function() {\n    \n          if ( thisHero.find('.fsElementSlideshow.slick-initialized').length ) {\n          clearInterval(checkSlideshow);\n    \n          const playPause = thisHero.find('.fsElementSlideshowControls');\n          const arrows = thisHero.find('.slick-arrow');\n          const elementSlideshow = thisHero.find('.fsElementSlideshow');\n    \n          playPause.add(arrows).appendTo(elementSlideshow).wrapAll('<div class=\"holder-controls\"></div>');\n          const holderControls = thisHero.find('.holder-controls');\n    \n          }\n    \n        },200);\n      }\n\n\n\n      thisHero.find(\".fsResource\").each(function(){\n        if( $(this).hasClass(\"fsResourceTypeImage\") ) {\n\n        const imgResource = $(this);\n          \n        const heroImg = $(this).find('img'),\n              image_title = $(this).find('.fsTitle:not(.fsResourceName)'),\n              image_caption = $(this).find('figcaption'),\n              pictureTag = $(this).find('picture');\n\n        moveResourceImage(heroImg, pictureTag); \n\n        if ( image_title.length && !image_caption.length ) {\n          imgResource.append('<figcaption></figcaption>');\n          image_title.prependTo(imgResource.find('figcaption'));\n        } \n\n      } \n\n        else if( $(this).hasClass(\"fsResourceTypeVideo\") ) {\n\n          const videoResource = $(this);\n          const video = $(this).find(\"video\"),\n                video_title = $(this).find('.fsTitle:not(.fsResourceName)'),\n                caption_video = $(this).find('.fsDescription');\n\n                video.removeAttr('poster');\n\n          if ( video_title.length && caption_video.length ) {\n              video_title.prependTo(caption_video);\n            } else if ( video_title.length && !caption_video.length ) {\n              $(this).append('<div class=\"fsDescription\"></div>');\n              video_title.prependTo(caption_video);\n          }\n            \n          video.wrap('<div class=\"video-wrapper\"></div>');\n\n        }\n\n      });\n\n      // Custom Play - Pause Button for Single Videos\n      const $playPause = '<div class=\"fsElementSlideshowControls custom\">' + \n                          '<button class=\"customPP pause fsSlideshowPause\" value=\"pause\" type=\"button\">Pause</button>' +\n                          '<button class=\"customPP play fsSlideshowPlay\" value=\"play\" type=\"button\">Play</button>' +\n                        '</div>';\n\n      if( thisHero.hasClass('fsSingleItem') && thisHero.find('.fsResourceTypeVideo').length ) {\n        thisHero.addClass('single-video');\n        thisHero.find(' > .fsElementContent ').append($playPause)\n      } \n\n      thisHero.find('.pause').click(function(){\n\n        thisHero.find('video').get(0).pause();\n        thisHero.find('.customPP.play').addClass('visible').removeClass('hidden');\n        $(this).addClass('hidden').removeClass('visible');\n\n      });\n\n      thisHero.find('.play').click(function(){\n\n        thisHero.find('video').get(0).play();\n        thisHero.find('.customPP.pause').addClass('visible').removeClass('hidden');\n        $(this).addClass('hidden').removeClass('visible');\n\n      });\n\n    }); \n\n    $(document).ready(function() {\n      $body.addClass('hero-animation');\n    })\n  \n  },\n\n}; //end ENHANCEMENTS\n\n// EXTERNAL MODULE: ./js/vendor/enquire.js\nvar enquire = __webpack_require__(269);\nvar enquire_default = /*#__PURE__*/__webpack_require__.n(enquire);\n;// CONCATENATED MODULE: ./js/include/home.js\n// ================================\n// HOMEPAGE\n// ================================\n\n\n\n\n\nconst HOME = {\n  init() {\n    // this.homepageFunction();\n\n    if (notComposeMode) {\n      // functions to not run in compose mode\n      this.hpPostCalendar();\n    }\n  },\n\n  hpPostCalendar() {\n    const mainContainer = $(\".hp-post-calendar\");\n    const postElement = mainContainer.find(\".hp-post\");\n    const calendarElement = mainContainer.find(\".hp-calendar\");\n    const header = mainContainer.find(\"header\");\n    const footer = mainContainer.find(\"footer\");\n\n    enquire_default().register(\"(max-width: 699px)\", {\n        match: function () {\n          footer.prependTo(postElement);\n          header.prependTo(postElement);\n        },\n      })\n      .register(\"(min-width: 700px)\", {\n        match: function () {\n          header.prependTo(postElement);\n          footer.prependTo(calendarElement);\n        },\n      });\n\n    postElement.each(function () {\n      const area = $(this);\n      const slickElement = area.find(\".fsListItems\");\n      const arrowDotsContainer = $(\"<div class='arrow-dots-container'></div>\");\n      const arrowContainer = $(\"<div class='arrow-container'></div>\");\n\n      arrowDotsContainer.appendTo(area);\n      arrowContainer.appendTo(area.find(arrowDotsContainer));\n\n      $(document).ready(function () {\n        area.find(\"article\").each(function () {\n          const _ = $(this),\n            textWrapper = $(\"<div class='text-wrapper'></div>\"),\n            title = _.find(\".fsTitle\");\n\n          textWrapper.appendTo(_);\n          _.children().not(\".fsThumbnail\").appendTo(textWrapper);\n\n          let titleHeight = title.outerHeight(true);\n          let translationValue = `calc(100% - ${titleHeight}px)`;\n\n          textWrapper.css(\"transform\", `translate(-50%, ${translationValue})`);\n\n          setTimeout(function () {\n            let titleHeight = title.outerHeight(true);\n            let translationValue = `calc(100% - ${titleHeight}px)`;\n\n            textWrapper.css(\n              \"transform\",\n              `translate(-50%, ${translationValue})`\n            );\n          }, 300);\n\n          $(window).resize(function () {\n            let titleHeight = title.outerHeight(true);\n            let translationValue = `calc(100% - ${titleHeight}px)`;\n\n            textWrapper.css(\n              \"transform\",\n              `translate(-50%, ${translationValue})`\n            );\n\n            setTimeout(function () {\n              let titleHeight = title.outerHeight(true);\n              let translationValue = `calc(100% - ${titleHeight}px)`;\n\n              textWrapper.css(\n                \"transform\",\n                `translate(-50%, ${translationValue})`\n              );\n            }, 300);\n          });\n        });\n      });\n\n      slickElement.slick({\n        mobileFirst: true,\n        slidesToShow: 1,\n        slidesToScroll: 1,\n        dots: true,\n        appendDots: area.find(\".arrow-dots-container\"),\n        appendArrows: area.find(\".arrow-container\"),\n      });\n    });\n\n    calendarElement.each(function () {\n      const area = $(this);\n      const slickElement = area.find(\".fsListItems\");\n      const arrowDotsContainer = $(\"<div class='arrow-dots-container'></div>\");\n      const arrowContainer = $(\"<div class='arrow-container'></div>\");\n\n      arrowDotsContainer.appendTo(area);\n      arrowContainer.appendTo(area.find(arrowDotsContainer));\n\n      area.find(\"article\").each(function () {\n        if ($(this).parent().hasClass(\"fsDayContainer\")) {\n          $(this).unwrap();\n        }\n\n        $(this).find(\".fsEventDetails\").children().unwrap();\n      });\n\n      slickElement.slick({\n        mobileFirst: true,\n        slidesToShow: 1,\n        slidesToScroll: 1,\n        rows: 3,\n        dots: true,\n        appendDots: area.find(\".arrow-dots-container\"),\n        appendArrows: area.find(\".arrow-container\"),\n      });\n    });\n  },\n\n  homepageFunction() {},\n}; //end HOME\n\n;// CONCATENATED MODULE: ./js/include/index.js\n// ===================================\n// FONT LOADER CODE\n// uncomment out section below for use\n// ===================================\n\n// require('../internal/font-loader');\n// // configure fonts to load from vendors\n// // check src/js/internal/font-loader.js for reference\n// WebFontConfig = {\n//  typekit: {\n//    id: 'xxxxxx'\n//  }\n// };\n\n\n// ===================================\n// IMPORT INCLUDE/ METHODS\n// ===================================\n\n\n\n\n\n\n\n\n\n\n\nconst INITIATE_ALL = {\n\tinit () {\n\n\t\tif (notDraftMode) {\n\t\t\tACCESSIBILITY.init();\n\t\t}\n\n\t\tNAVIGATION.init();\n\n        DEFAULT_STYLES.init();\n\n        if (isHome) {\n            HOME.init();\n        }\n\n        ENHANCEMENTS.init();\n\n        if($calendarGrid.length) {\n            RESPONSIVE_CALENDAR_GRID.init();\n        }\n            \n        RESPONSIVE_SLIDESHOW_SUBTYPE.init();\n        \n\t}\n}\n\n//# sourceURL=webpack:///./js/include/index.js_+_9_modules?")},517:function(__unused_webpack_module,__webpack_exports__,__webpack_require__){"use strict";eval('__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   FSFeeds: function() { return /* binding */ FSFeeds; },\n/* harmony export */   MultiRequestFeed: function() { return /* binding */ MultiRequestFeed; }\n/* harmony export */ });\n/* harmony import */ var _utilities__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(239);\n// ====================================================\n// Custom Finalsite Feeds Plugin\n// ====================================================\n\n\n\nclass FSFeeds {\n  constructor(target, options, callback) {\n    this.target = target;\n    this.endpoint = target.attr(\'data-feed-url\');\n    this.options = options;\n    this.postData;\n    this.defaultTemplate = options.defaultTemplate || [\'<article class="fsFeeds-post">\',\n        \'<div class="post-image" style="background-image: url({image})">\',\n          \'<img src="{image}" class="post-image-inline" alt="Photo from @{poster_name} on {source.source} at {date}" />\',\n        \'</div>\',\n        \'<div class="post-content">\',\n          \'<div class="post-description">{unformatted_message}</div>\',\n        \'</div>\',\n      \'</article>\'];\n    this.dateFormat = options.dateFormat || "D MMMM YYYY";\n    this.templates = options.templates || { \'Facebook\': this.defaultTemplate, \'Twitter\': this.defaultTemplate, \'Instagram\': this.defaultTemplate, \'YouTube\': this.defaultTemplate };\n    this.loadMoreTemplate = options.loadMoreTemplate || "<button>Load More</button>";\n    this.renderedTemplate = [];\n    this.callback = callback;\n    this.loadMore = options.loadMore;\n    this.filters = options.filters || {};\n  }\n\n  requestPostData(endpoint, feedOptions, callback) {\n    $.getJSON(endpoint, feedOptions, function(data) {\n      }.bind(this)).done(function(data) {\n        callback(data.posts.items);\n      }.bind(this)).fail(function() {\n        this.target.append(\'<span>Sorry, an error occured when retrieving this feed data. Please refresh the page to try again.</span>\').css(\'textAlign\', \'center\');\n    });\n  }\n\n  getTemplate(type) {\n    return this.templates ? this.templates[type] || this.defaultTemplate : this.defaultTemplate;\n  }\n\n  construct() {\n    var template = []\n    this.postData.forEach((post) => {\n      var post = post;\n      var postType = post.source.source;\n      if(postType == "Instagram" && post.additional_photos && post.additional_photos.length) {\n        post.image = post.additional_photos[0]\n      }\n      post.date = moment(post.external_created_at).format(this.dateFormat);\n      template.push((0,_utilities__WEBPACK_IMPORTED_MODULE_0__/* .nano */ .X6)(this.getTemplate(postType).join(\'\\n\'), post))\n    });\n    return template;\n  }\n\n  render(template) {\n    this.target.find("> .fsElementContent").append(template.join(\'\'));\n  }\n\n  renderLoadMore() {\n    this.target.append($(this.loadMoreTemplate).addClass("fsFeeds-load-more"));\n    $(document).on("click", ".fsFeeds-load-more", () => {\n      this.options.feed.page = this.options.feed.page + 1;\n      this.requestPostData(this.endpoint, this.options.feed, (data) => {\n        this.postData = this.postOrder ? this.sortPosts(data) : data;\n        this.renderedTemplate = this.construct(this.postData);\n        this.render(this.renderedTemplate);\n        this.callback();\n      });\n    });\n  }\n\n  sortPosts(posts) {\n    var sorted_posts = [];\n    this.postOrder.forEach( function(feed) {\n      var postToSort = posts.find(function(post) {\n        return post.source.source == feed;\n      });\n      sorted_posts.push(postToSort);\n      posts = posts.filter(function(post) {\n        return post !== postToSort\n      });\n    });\n    return sorted_posts;\n  }\n\n  init() {\n    this.options.feed.page = this.options.feed.page || 1;\n    if(this.options.filters !== undefined) {\n      console.warn("Warning: The \'filters\' option is not supported in FSFeeds");\n    }\n    if(this.loadMore) {\n      this.renderLoadMore();\n    }\n    this.requestPostData(this.endpoint, this.options.feed, (data) => {\n      this.postData = this.postOrder ? this.sortPosts(data) : data;\n      this.renderedTemplate = this.construct(this.postData);\n      this.render(this.renderedTemplate);\n      this.callback();\n    });\n  }\n}\n\nclass MultiRequestFeed extends FSFeeds {\n\n  requestPostData(endpoint, feedOptions, callback) {\n    var filters = Object.entries(this.filters);\n    var matches = endpoint.match(/filter=([\\d,]+)/);\n    var nums = matches == null ? [] : matches[1].split(",").filter((x) => x); \n    var output = {};\n    var sourceData = {};\n    var sourceNumbers = Object.entries(endpoint.split("?filter=").pop().split(","));\n    var urls = sourceNumbers.map(filter => endpoint.split("?")[0] + "?filter=" + filter[1]);\n    if(sourceNumbers[0][1] === "") {\n      console.error("Error: Please select sources in the element");\n      return;\n    }\n    if(this.options.feed.per !== undefined) {\n      console.warn("Warning: The \'per\' option is not supported in MultiRequestFeed");\n    }\n    Promise.all(urls.map(url =>\n      fetch(url).then(resp => resp.json())\n    )).then(values => {\n      values.forEach(function(value, index) {\n        if(value.posts.items[0].source.source.toLowerCase() in sourceData) {\n          sourceData[value.posts.items[0].source.source.toLowerCase()] = sourceData[value.posts.items[0].source.source.toLowerCase()] + "," + urls[index].split("?filter=")[1];\n        } else {\n          sourceData[value.posts.items[0].source.source.toLowerCase()] = endpoint.split("?filter")[0] + "?filter=" + urls[index].split("?filter=")[1];\n        }\n      });\n      filters.forEach((x) => {\n        if(sourceData[x[0]] === undefined) {\n          sourceData[x[0]] = endpoint.split("?filter")[0] + "?filter="+x[0] + "&per=" + x[1] + "&page=" + this.options.feed.page;\n        } else {\n          sourceData[x[0]] = sourceData[x[0]] + "&per=" + x[1] + "&page=" + this.options.feed.page;\n        }\n      });\n      let keys = new Set(filters.flatMap((x) => (x[0] in sourceData) ? [x[0]] : [] ).concat(Object.keys(sourceData)));\n      for (const x of keys) {\n        let v = sourceData[x];\n        delete sourceData[x];\n        sourceData[x] = v;\n      }\n      Promise.all(Object.values(sourceData).map(url =>\n        fetch(url).then(resp => resp.json())\n      )).then(callback);\n    });\n  }\n\n  renderLoadMore() {\n    this.target.append($(this.loadMoreTemplate).addClass("fsFeeds-load-more"));\n    $(document).on("click", ".fsFeeds-load-more", () => {\n      this.options.feed.page = this.options.feed.page + 1;\n      this.requestPostData(this.endpoint, this.options.feed, (data) => {\n        this.postData = this.combinePosts(data);\n        this.renderedTemplate = this.construct(this.postData);\n        this.render(this.renderedTemplate);\n        this.callback();\n      });\n    });\n  }\n\n  combinePosts(data) {\n    return data.map(source => source.posts.items).reduce((acc, val) => acc.concat(val), []);\n  }\n\n  init() {\n    this.options.feed.page = this.options.feed.page || 1;\n    if(this.loadMore) {\n      this.renderLoadMore();\n    }\n    this.requestPostData(this.endpoint, this.options.feed, (data) => {\n      this.postData = this.combinePosts(data);\n      this.renderedTemplate = this.construct(this.postData);\n      this.render(this.renderedTemplate);\n      this.callback();\n    });\n  }\n}\n\n$.fn.fsFeedPull = function( options, callback ) { new FSFeeds( this, options, callback ).init(); }\n$.fn.fsMultiFeedPull = function( options, callback ) { new MultiRequestFeed( this, options, callback ).init(); }\n\n\n\n//# sourceURL=webpack:///./js/internal/fs-feed-pull.js?')},509:function(){eval("var keyCodeMap = {\n  48:\"0\", 49:\"1\", 50:\"2\", 51:\"3\", 52:\"4\", 53:\"5\", 54:\"6\", 55:\"7\", 56:\"8\", 57:\"9\", 59:\";\",\n  65:\"a\", 66:\"b\", 67:\"c\", 68:\"d\", 69:\"e\", 70:\"f\", 71:\"g\", 72:\"h\", 73:\"i\", 74:\"j\", 75:\"k\", 76:\"l\",\n  77:\"m\", 78:\"n\", 79:\"o\", 80:\"p\", 81:\"q\", 82:\"r\", 83:\"s\", 84:\"t\", 85:\"u\", 86:\"v\", 87:\"w\", 88:\"x\", 89:\"y\", 90:\"z\",\n  96:\"0\", 97:\"1\", 98:\"2\", 99:\"3\", 100:\"4\", 101:\"5\", 102:\"6\", 103:\"7\", 104:\"8\", 105:\"9\"\n}\n\n$.fn.accessibility_menu = function(options) {\n\n  //Defaults defined here. If not distinguished in function call, use these\n  var settings = $.extend({\n    menuClass: \"menu-item-open\",//The class that gets added to <li> to open menu\n    mainMenuLabel: \"Main\",//Aria-label on <nav>\n    topMenuRole: \"menubar\",//Aria-role on <ul class=\"fsNavLevel1\">\n    listItemsRole: 'menuitem',//Aria-role on <a>\n    subNavRole: 'menu',//Aria-role on <ul class=\"fsNavLevel[1n +2]\">\n    firstTab: 'level2'//Tab to level 2 menu or page desc. Options: \"level2\" or \"pagedesc\"\n  }, options);\n\n  var _self = $(this);\n  var dropdownWrapper = '.fsNavPageInfo';\n  var fsNavLevel1 = '.fsNavLevel1';\n  var subLevels = 'ul[class^=\"fsNavLevel\"]';\n  var pageDesc = '.fsNavPageDescription';\n  var top_level_links = _self.find('> li > a');\n\n  // If the element is not found, don't run the plugin\n  if(!_self.length) {\n    return;\n  }\n\n  // Add ARIA attributes to the apporpriate elements based on settings\n  if(!$(this).parent()[0].hasAttribute(\"aria-label\")) {\n    $(this).parent().attr('aria-label',settings.mainMenuLabel);\n  }\n  $(this)\n    .attr('role', settings.topMenuRole)\n    .find('a')\n    .attr('role', settings.listItemsRole);\n  $(this)\n    .find(subLevels)\n    .attr('role', settings.subNavRole);\n\n  //Set all links in the dropdown to -1 tabindex to not worry\n  $(this)\n    .find(dropdownWrapper)\n    .find('a').attr('tabIndex',-1);\n  \n  // Adding aria roles for the dropdowns / level1 <li>\n  $(top_level_links).each(function(){\n    if($(this).next(dropdownWrapper).length > 0) {\n      $(this).parent('li')\n        .attr('aria-haspopup', 'true')\n        .find(dropdownWrapper)\n        .attr('aria-hidden','true');\n    }\n  });\n  \n  $(top_level_links).unbind();\n  $(top_level_links).bind(\"focus mouseenter mouseleave keydown\",function(){\n    var allAnchors = new Array();\n    $(this)\n      .parents(fsNavLevel1)\n      .find('> li > a').removeAttr('tabindex');\n    $(this)\n      .parents(fsNavLevel1)\n      .find('.'+settings.menuClass)\n      .removeClass(settings.menuClass)\n      .find(dropdownWrapper) \n      .attr('aria-hidden', 'true')\n      .find('a').attr('tabindex',-1);\n    $(this)\n      .next(dropdownWrapper)\n      .attr('aria-hidden', 'false')\n      .parent(\"li\")\n      .addClass(settings.menuClass);\n\n    //Start populating anchor array\n    allAnchors.push($(this)[0]);\n    if(settings.firstTab  == 'level2') {\n      if($(this).next(dropdownWrapper).find(subLevels).find(\"a\").length) {\n        for(var i=0;i<$(this).next(dropdownWrapper).find(subLevels).find(\"a\").length;i++) {\n          allAnchors.push(\n            $(this)\n              .next(dropdownWrapper)\n              .find(subLevels)\n              .find(\"a\")[i]\n          );\n        }\n      }\n      if($(this).next(dropdownWrapper).find(pageDesc).find(\"a\").length) {\n        for(var j=0;j<$(this).next(dropdownWrapper).find(pageDesc).find(\"a\").length;j++) {\n          allAnchors.push(\n            $(this)\n              .next(dropdownWrapper)\n              .find(pageDesc)\n              .find(\"a\")[j]\n          );\n        }\n      }\n    } else if(settings.firstTab == 'pagedesc') {\n      if($(this).next(dropdownWrapper).find(pageDesc).find(\"a\").length) {\n        for(var k=0;k<$(this).next(dropdownWrapper).find(pageDesc).find(\"a\").length;k++) {\n          allAnchors.push(\n            $(this)\n              .next(dropdownWrapper)\n              .find(pageDesc)\n              .find(\"a\")[k]\n          );\n        }\n      }\n      if($(this).next(dropdownWrapper).find(subLevels).find(\"a\").length) {\n        for(var l=0;l<$(this).next(dropdownWrapper).find(subLevels).find(\"a\").length;l++) {\n          allAnchors.push(\n            $(this)\n              .next(dropdownWrapper)\n              .find(subLevels)\n              .find(\"a\")[l]\n          );\n        }\n      }\n    }\n    for(var m=0;m<allAnchors.length;m++) {\n      allAnchors[m].setAttribute(\"tabindex\",m);\n    }\n  });\n\n  $(this).on(\"mouseleave\",function() {\n    $(this)\n      .find('> li > a').removeAttr('tabindex');\n    $(this)\n      .find(\".\"+settings.menuClass)\n      .removeClass(settings.menuClass)\n      .find(dropdownWrapper) \n      .attr('aria-hidden', 'true')\n      .find('a').attr('tabIndex',-1);\n  });\n\n  // Bind arrow keys for top level navigation\n  $(top_level_links).keydown(function(e){\n    var totalLinks = $(this).parent('li').find(dropdownWrapper).find('a').length;\n    if(e.keyCode == 38) {\n      //Up Arrow Key\n      e.preventDefault();\n      if($(this).parent('li').find(dropdownWrapper).find('a').length) {\n        $(this).parent('li')\n          .find(dropdownWrapper)\n          .find('a[tabindex='+totalLinks+']').focus();\n      }\n    } else if(e.keyCode == 39) {\n      //Right Arrow Key\n      e.preventDefault();\n      if($(this).parent('li').next('li').length == 0) {\n        $(this).parents(fsNavLevel1).find('> li').first().find('a').first().focus();\n      } else {\n        $(this).parent('li').next('li').find('a').first().focus();\n      }\n    } else if(e.keyCode == 40) {\n      //Down Arrow Key\n      if($(this).parent('li').find(dropdownWrapper).find('a').length) {\n        e.preventDefault();\n        $(this).parent('li')\n          .addClass(settings.menuClass)\n          .find(dropdownWrapper)\n          .attr('aria-hidden', 'false');\n        $(this)\n          .parent('li')\n          .find('a[tabindex=1]').focus();\n      }\n    } else if(e.keyCode == 37) {\n      //Left Arrow Key\n      e.preventDefault();\n      if($(this).parent('li').prev('li').length == 0) {\n        $(this).parents(fsNavLevel1).find('> li').last().find('a').first().focus();\n      } else {\n        $(this).parent('li').prev('li').find('a').first().focus();\n      }\n    } else if(e.keyCode == 9) {\n      //Tab Key\n      if(e.shiftKey) {\n        if($(this).parent('li').prev('li').length == 0) {\n          $(this)\n              .parents(fsNavLevel1)\n              .find('> li > a').removeAttr('tabindex');\n            $('.'+settings.menuClass)\n              .removeClass(settings.menuClass)\n              .find(dropdownWrapper)\n              .attr('aria-hidden', 'true')\n              .find('a').attr('tabIndex',-1);\n        } else {\n          if($(this).parent('li').prev('li').length) {\n            e.preventDefault();\n            var prevLinkLength = $(this).parent('li').prev('li').find(dropdownWrapper).find('a').length;\n            $(this)\n              .parents(fsNavLevel1)\n              .find('> li > a').removeAttr('tabindex');\n            $('.'+settings.menuClass)\n              .removeClass(settings.menuClass)\n              .find(dropdownWrapper)\n              .attr('aria-hidden', 'true')\n              .find('a').attr('tabIndex',-1);\n            $(this)\n              .parent('li')\n              .prev('li')\n              .addClass(settings.menuClass)\n              .find(dropdownWrapper)\n              .attr('aria-hidden', 'false');\n            $(this).parent('li').prev('li').find(\">a\").focus()\n              .parent().find(dropdownWrapper).find('a[tabindex='+prevLinkLength+']').focus();\n          } else {\n            $(this)\n              .parents(fsNavLevel1)\n              .find('> li > a').removeAttr('tabindex');\n            $('.'+settings.menuClass)\n              .removeClass(settings.menuClass)\n              .find(dropdownWrapper)\n              .attr('aria-hidden', 'true')\n              .find('a').attr('tabIndex',-1);\n          }\n        }\n      } else {\n        if($(this).parent('li').find(dropdownWrapper).find('a').length) {\n          e.preventDefault();\n          $(this).parent('li')\n            .addClass(settings.menuClass)\n            .find(dropdownWrapper)\n            .attr('aria-hidden', 'false');\n          $(this)\n            .parent('li')\n            .find('a[tabindex=1]').focus();\n        }\n      }\n    } else if(e.keyCode == 32) {\n      //Space Bar Key\n      e.preventDefault();\n      window.location = $(this).attr('href');\n    } else if(e.keyCode == 27) {\n      //Escape Key\n      e.preventDefault();\n      $('.'+settings.menuClass)\n        .removeClass(settings.menuClass)\n        .find('> a')\n        .removeAttr('tabindex')\n        .parent('li')\n        .find(dropdownWrapper)\n        .attr('aria-hidden', 'true')\n        .find('a').attr('tabIndex',-1);\n    } else {\n      //Any other key not listed\n      $(this).parent('li').find(dropdownWrapper+'[aria-hidden=false] a').each(function(){\n        if($(this).text().substring(0,1).toLowerCase() == keyCodeMap[e.keyCode]) {\n          $(this).focus();\n          return false;\n        }\n      });\n    }\n  });\n  \n  var subNavLinks = $(this).find(dropdownWrapper).find('a');\n\n  $(subNavLinks).unbind();\n  $(subNavLinks).bind(\"focus mouseenter mouseleave keydown\",function(){\n    $(this)\n      .parent().parent()\n      .find('.'+settings.menuClass)\n      .removeClass(settings.menuClass)\n      .find(dropdownWrapper)\n      .attr('aria-hidden', 'true');\n    $(this)\n      .next(dropdownWrapper)\n      .attr('aria-hidden', 'false')\n      .parentsUntil(fsNavLevel1, \"li\")\n      .addClass(settings.menuClass);\n  });\n  \n  $(subNavLinks).keydown(function(e){\n    var totalLinks = $(this).parents(dropdownWrapper).find('a').length;\n    var currentIndex =  parseInt($(this).attr(\"tabindex\"));\n    if(e.keyCode == 38) {\n      //Up Arrow Key\n      e.preventDefault();\n      if(currentIndex == 1) {\n        $(this).parents(dropdownWrapper).parent('li').find('a').first().focus();\n      } else {\n        $(this).parents(dropdownWrapper).find('a[tabindex=' + (currentIndex - 1) + ']')\n          .parentsUntil(fsNavLevel1, \"li\")\n          .addClass(settings.menuClass)\n          .find('a[tabindex=' + (currentIndex - 1) + ']').focus();\n      }\n    } else if(e.keyCode == 39) {\n      //Right Arrow Key\n      e.preventDefault();\n      if($(this).parents(fsNavLevel1).find(\"a[tabindex='0']\").parent('li').next('li').length == 0) {\n        $(this).parents(fsNavLevel1).find('> li').first().find('a').first().focus();\n      } else {\n        $(this).parents(fsNavLevel1).find(\"a[tabindex='0']\").parent('li').next('li').find('a').first().focus();\n      }\n    } else if(e.keyCode == 40) {\n      //Down Arrow Key\n      e.preventDefault();\n      if(currentIndex == totalLinks) {\n        $(this).parents(dropdownWrapper).parent('li').find('a').first().focus();\n      } else {\n        $(this).parents(dropdownWrapper).find('a[tabindex=' + (currentIndex + 1) + ']').focus();\n      }\n    } else if(e.keyCode == 27 || e.keyCode == 37) {\n      //Left Arrow Key or Escape Key\n      e.preventDefault();\n      $('.'+settings.menuClass)\n        .removeClass(settings.menuClass)\n        .find(dropdownWrapper)\n        .attr('aria-hidden', 'true')\n        .find('a').attr('tabIndex',-1);\n      $(this)\n        .parentsUntil(fsNavLevel1, \"li\")\n        .find('a').first().focus();\n    } else if(e.keyCode == 9) {\n      //Tab Arrow Key\n      if(e.shiftKey) {\n        //Tab + Shift\n        e.preventDefault();\n        if(currentIndex == 1) {\n          $(this).parents(dropdownWrapper).parent('li').find('a').first().focus();\n        } else {\n          $(this).parents(dropdownWrapper).find('a[tabindex=' + (currentIndex - 1) + ']')\n            .parentsUntil(fsNavLevel1, \"li\")\n            .addClass(settings.menuClass)\n            .find('a[tabindex=' + (currentIndex - 1) + ']').focus();\n        }\n      } else {\n        if(currentIndex == totalLinks) {\n          if($(this).parents(dropdownWrapper).parent('li').next('li').length) {\n            e.preventDefault();\n            $(this).parents(dropdownWrapper).parent('li').next('li').find('a').first().focus();\n          } else {\n            $(this)\n              .parents(fsNavLevel1)\n              .find('> li > a').removeAttr('tabindex');\n            $('.'+settings.menuClass)\n              .removeClass(settings.menuClass)\n              .find(dropdownWrapper)\n              .attr('aria-hidden', 'true')\n              .find('a').attr('tabIndex',-1);\n          }\n        } else {\n          e.preventDefault();\n          $(this).parents(dropdownWrapper).find('a[tabindex=' + (currentIndex + 1) + ']').focus();\n        }\n      }\n    } else if(e.keyCode == 32) {\n      e.preventDefault();\n      window.location = $(this).attr('href');\n    } else {\n      var found = false;\n      $(this).parent('li').nextAll('li').find('a').each(function(){\n        if($(this).text().substring(0,1).toLowerCase() == keyCodeMap[e.keyCode]) {\n          $(this).focus();\n          found = true;\n          return false;\n        }\n      });\n      if(!found) {\n        $(this).parent('li').prevAll('li').find('a').each(function(){\n          if($(this).text().substring(0,1).toLowerCase() == keyCodeMap[e.keyCode]) {\n            $(this).focus();\n            return false;\n          }\n        });\n      }\n    }\n  });\n\n  $(document).click(function(){\n    $(this)\n      .parents(fsNavLevel1)\n      .find('> li > a').removeAttr('tabindex');\n    $('.'+settings.menuClass)\n      .removeClass(settings.menuClass)\n      .find(dropdownWrapper)\n      .attr('aria-hidden', 'true')\n      .find('a').attr('tabIndex',-1);\n  });\n  \n  $(this).click(function(e){\n    e.stopPropagation();\n  });\n}\n\n\n//# sourceURL=webpack:///./js/internal/menu-accessibility.js?")},239:function(__unused_webpack_module,__webpack_exports__,__webpack_require__){"use strict";eval('/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   X6: function() { return /* binding */ nano; },\n/* harmony export */   oM: function() { return /* binding */ trapFocus; }\n/* harmony export */ });\n/* unused harmony exports backgroundImage, debounce, placeholder, randomGenerator */\n// =============================\n// Common Utitlies\n// =============================\n\nfunction backgroundImage($element) {\n  backgroundElement = $element;\n  $(backgroundElement).each(function() {\n\n    var image = $(this).find(\'img\').attr(\'src\');\n\n    $(this).css(\'background-image\', \'url("\' + image + \'")\');\n\n  });\n};\n\n// ===================================\n// Debouncing for Window Resize Events\n// ===================================\n\nfunction debounce(func, wait, immediate) {\n  var timeout;\n  return function() {\n    var context = this, args = arguments;\n    var later = function() {\n      timeout = null;\n      if (!immediate) func.apply(context, args);\n    };\n    var callNow = immediate && !timeout;\n    clearTimeout(timeout);\n    timeout = setTimeout(later, wait);\n    if (callNow) func.apply(context, args);\n  }\n};\n\n// ===================================\n// Search Module Placeholder\n// Use: placeholder($(".search-module"), "Search");\n// Note: function MUST be called after $.support.placeholder function below this comment\n// ===================================\n\n// //check if browser supports placeholders for placeholder()\n// $.support.placeholder = (function() {\n//     var i = document.createElement(\'input\');\n//     return \'placeholder\' in i;\n// })();\n\nfunction placeholder(el, text) {\n\n    var counter = 100, // number of attempts before the function self-terminates\n        interval = 100, // milliseconds between attempts\n        searchCheck,\n        timer\n    ;\n\n    searchCheck = function searchCheck(){\n        if(el.find("input.gsc-input").length){\n            if($.support.placeholder) {\n                el.find("input.gsc-input").attr("placeholder", text);\n            }\n            else {\n                el.find("input.gsc-input").attr("value", text);\n            }\n        } else if( counter > 0 ) {\n            timer = setTimeout( searchCheck, interval );\n            counter -= 1;\n        }\n    };\n\n    timer = setTimeout( searchCheck, interval );\n};\n\n// =============================\n// Nano Templates\n// https://github.com/trix/nano\n// =============================\n\nfunction nano(template, data) {\n  return template.replace(/\\{([\\w\\.]*)\\}/g, function(str, key) {\n    var keys = key.split("."), v = data[keys.shift()];\n    for (var i = 0, l = keys.length; i < l; i++) v = v[keys[i]];\n    return (typeof v !== "undefined" && v !== null) ? v : "";\n  });\n};\n\n// =============================\n// Randomize Function\n// =============================\n\nfunction randomGenerator() {\n  $.fn.randomize = function(selector){\n    var $elems = selector ? $(this).find(selector) : $(this).children(),\n        $parents = $elems.parent();\n\n    $parents.each(function(){\n        $(this).children(selector).sort(function(){\n            return Math.round(Math.random()) - 0.5;\n        }).detach().appendTo(this);\n    });\n\n    return this;\n  };\n};\n\n// =============================\n// Trap Focus Function\n// =============================\n\nfunction trapFocus(element, cb) {\n  const KEYCODE_TAB = 9,\n      ESCAPE_TAB = 27;\n  element.addEventListener(\'keydown\', function(e) {\n    var focusableEls = Array.from(element.querySelectorAll(\'a[href]:not([disabled]), button:not([disabled]), textarea:not([disabled]), input[type="text"]:not([disabled]), input[type="radio"]:not([disabled]), input[type="checkbox"]:not([disabled]), select:not([disabled])\')).filter(s =>\n      !!( s.offsetWidth || s.offsetHeight || s.getClientRects().length )\n    );\n    var firstFocusableEl = focusableEls[0];  \n    var lastFocusableEl = focusableEls[focusableEls.length - 1];\n    var isTabPressed = (e.key === \'Tab\' || e.keyCode === KEYCODE_TAB);\n    var isEscPressed = (e.key === \'Escape\' || e.keyCode === ESCAPE_TAB);\n    if(!isTabPressed && !isEscPressed) { \n      return; \n    }\n    if(isEscPressed) {\n      this.callback = cb;\n      this.callback();\n    }\n    if(e.shiftKey) /* shift + tab */ {\n      if(document.activeElement === firstFocusableEl) {\n        lastFocusableEl.focus();\n        e.preventDefault();\n      }\n    } else /* tab */ {\n      if(document.activeElement === lastFocusableEl) {\n        firstFocusableEl.focus();\n        e.preventDefault();\n      }\n    }\n  });\n};\n\n//# sourceURL=webpack:///./js/internal/utilities.js?')},269:function(module,exports,__webpack_require__){eval("var __WEBPACK_AMD_DEFINE_RESULT__;/*!\n * enquire.js v2.1.2 - Awesome Media Queries in JavaScript\n * Copyright (c) 2014 Nick Williams - http://wicky.nillia.ms/enquire.js\n * License: MIT (http://www.opensource.org/licenses/mit-license.php)\n */\n\n;(function (name, context, factory) {\n\tvar matchMedia = window.matchMedia;\n\n\tif ( true && module.exports) {\n\t\tmodule.exports = factory(matchMedia);\n\t}\n\telse if (true) {\n\t\t!(__WEBPACK_AMD_DEFINE_RESULT__ = (function() {\n\t\t\treturn (context[name] = factory(matchMedia));\n\t\t}).call(exports, __webpack_require__, exports, module),\n\t\t__WEBPACK_AMD_DEFINE_RESULT__ !== undefined && (module.exports = __WEBPACK_AMD_DEFINE_RESULT__));\n\t}\n\telse {}\n}('enquire', this, function (matchMedia) {\n\n\t'use strict';\n\n    /*jshint unused:false */\n    /**\n     * Helper function for iterating over a collection\n     *\n     * @param collection\n     * @param fn\n     */\n    function each(collection, fn) {\n        var i      = 0,\n            length = collection.length,\n            cont;\n\n        for(i; i < length; i++) {\n            cont = fn(collection[i], i);\n            if(cont === false) {\n                break; //allow early exit\n            }\n        }\n    }\n\n    /**\n     * Helper function for determining whether target object is an array\n     *\n     * @param target the object under test\n     * @return {Boolean} true if array, false otherwise\n     */\n    function isArray(target) {\n        return Object.prototype.toString.apply(target) === '[object Array]';\n    }\n\n    /**\n     * Helper function for determining whether target object is a function\n     *\n     * @param target the object under test\n     * @return {Boolean} true if function, false otherwise\n     */\n    function isFunction(target) {\n        return typeof target === 'function';\n    }\n\n    /**\n     * Delegate to handle a media query being matched and unmatched.\n     *\n     * @param {object} options\n     * @param {function} options.match callback for when the media query is matched\n     * @param {function} [options.unmatch] callback for when the media query is unmatched\n     * @param {function} [options.setup] one-time callback triggered the first time a query is matched\n     * @param {boolean} [options.deferSetup=false] should the setup callback be run immediately, rather than first time query is matched?\n     * @constructor\n     */\n    function QueryHandler(options) {\n        this.options = options;\n        !options.deferSetup && this.setup();\n    }\n    QueryHandler.prototype = {\n\n        /**\n         * coordinates setup of the handler\n         *\n         * @function\n         */\n        setup : function() {\n            if(this.options.setup) {\n                this.options.setup();\n            }\n            this.initialised = true;\n        },\n\n        /**\n         * coordinates setup and triggering of the handler\n         *\n         * @function\n         */\n        on : function() {\n            !this.initialised && this.setup();\n            this.options.match && this.options.match();\n        },\n\n        /**\n         * coordinates the unmatch event for the handler\n         *\n         * @function\n         */\n        off : function() {\n            this.options.unmatch && this.options.unmatch();\n        },\n\n        /**\n         * called when a handler is to be destroyed.\n         * delegates to the destroy or unmatch callbacks, depending on availability.\n         *\n         * @function\n         */\n        destroy : function() {\n            this.options.destroy ? this.options.destroy() : this.off();\n        },\n\n        /**\n         * determines equality by reference.\n         * if object is supplied compare options, if function, compare match callback\n         *\n         * @function\n         * @param {object || function} [target] the target for comparison\n         */\n        equals : function(target) {\n            return this.options === target || this.options.match === target;\n        }\n\n    };\n    /**\n     * Represents a single media query, manages it's state and registered handlers for this query\n     *\n     * @constructor\n     * @param {string} query the media query string\n     * @param {boolean} [isUnconditional=false] whether the media query should run regardless of whether the conditions are met. Primarily for helping older browsers deal with mobile-first design\n     */\n    function MediaQuery(query, isUnconditional) {\n        this.query = query;\n        this.isUnconditional = isUnconditional;\n        this.handlers = [];\n        this.mql = matchMedia(query);\n\n        var self = this;\n        this.listener = function(mql) {\n            self.mql = mql;\n            self.assess();\n        };\n        this.mql.addListener(this.listener);\n    }\n    MediaQuery.prototype = {\n\n        /**\n         * add a handler for this query, triggering if already active\n         *\n         * @param {object} handler\n         * @param {function} handler.match callback for when query is activated\n         * @param {function} [handler.unmatch] callback for when query is deactivated\n         * @param {function} [handler.setup] callback for immediate execution when a query handler is registered\n         * @param {boolean} [handler.deferSetup=false] should the setup callback be deferred until the first time the handler is matched?\n         */\n        addHandler : function(handler) {\n            var qh = new QueryHandler(handler);\n            this.handlers.push(qh);\n\n            this.matches() && qh.on();\n        },\n\n        /**\n         * removes the given handler from the collection, and calls it's destroy methods\n         * \n         * @param {object || function} handler the handler to remove\n         */\n        removeHandler : function(handler) {\n            var handlers = this.handlers;\n            each(handlers, function(h, i) {\n                if(h.equals(handler)) {\n                    h.destroy();\n                    return !handlers.splice(i,1); //remove from array and exit each early\n                }\n            });\n        },\n\n        /**\n         * Determine whether the media query should be considered a match\n         * \n         * @return {Boolean} true if media query can be considered a match, false otherwise\n         */\n        matches : function() {\n            return this.mql.matches || this.isUnconditional;\n        },\n\n        /**\n         * Clears all handlers and unbinds events\n         */\n        clear : function() {\n            each(this.handlers, function(handler) {\n                handler.destroy();\n            });\n            this.mql.removeListener(this.listener);\n            this.handlers.length = 0; //clear array\n        },\n\n        /*\n         * Assesses the query, turning on all handlers if it matches, turning them off if it doesn't match\n         */\n        assess : function() {\n            var action = this.matches() ? 'on' : 'off';\n\n            each(this.handlers, function(handler) {\n                handler[action]();\n            });\n        }\n    };\n    /**\n     * Allows for registration of query handlers.\n     * Manages the query handler's state and is responsible for wiring up browser events\n     *\n     * @constructor\n     */\n    function MediaQueryDispatch () {\n        if(!matchMedia) {\n            throw new Error('matchMedia not present, legacy browsers require a polyfill');\n        }\n\n        this.queries = {};\n        this.browserIsIncapable = !matchMedia('only all').matches;\n    }\n\n    MediaQueryDispatch.prototype = {\n\n        /**\n         * Registers a handler for the given media query\n         *\n         * @param {string} q the media query\n         * @param {object || Array || Function} options either a single query handler object, a function, or an array of query handlers\n         * @param {function} options.match fired when query matched\n         * @param {function} [options.unmatch] fired when a query is no longer matched\n         * @param {function} [options.setup] fired when handler first triggered\n         * @param {boolean} [options.deferSetup=false] whether setup should be run immediately or deferred until query is first matched\n         * @param {boolean} [shouldDegrade=false] whether this particular media query should always run on incapable browsers\n         */\n        register : function(q, options, shouldDegrade) {\n            var queries         = this.queries,\n                isUnconditional = shouldDegrade && this.browserIsIncapable;\n\n            if(!queries[q]) {\n                queries[q] = new MediaQuery(q, isUnconditional);\n            }\n\n            //normalise to object in an array\n            if(isFunction(options)) {\n                options = { match : options };\n            }\n            if(!isArray(options)) {\n                options = [options];\n            }\n            each(options, function(handler) {\n                if (isFunction(handler)) {\n                    handler = { match : handler };\n                }\n                queries[q].addHandler(handler);\n            });\n\n            return this;\n        },\n\n        /**\n         * unregisters a query and all it's handlers, or a specific handler for a query\n         *\n         * @param {string} q the media query to target\n         * @param {object || function} [handler] specific handler to unregister\n         */\n        unregister : function(q, handler) {\n            var query = this.queries[q];\n\n            if(query) {\n                if(handler) {\n                    query.removeHandler(handler);\n                }\n                else {\n                    query.clear();\n                    delete this.queries[q];\n                }\n            }\n\n            return this;\n        }\n    };\n\n\treturn new MediaQueryDispatch();\n\n}));\n\n//# sourceURL=webpack:///./js/vendor/enquire.js?")},534:function(){eval("/*!\nWaypoints - 4.0.1\nCopyright © 2011-2016 Caleb Troughton\nLicensed under the MIT license.\nhttps://github.com/imakewebthings/waypoints/blob/master/licenses.txt\n*/\n(function() {\n  'use strict'\n\n  var keyCounter = 0\n  var allWaypoints = {}\n\n  /* http://imakewebthings.com/waypoints/api/waypoint */\n  function Waypoint(options) {\n    if (!options) {\n      throw new Error('No options passed to Waypoint constructor')\n    }\n    if (!options.element) {\n      throw new Error('No element option passed to Waypoint constructor')\n    }\n    if (!options.handler) {\n      throw new Error('No handler option passed to Waypoint constructor')\n    }\n\n    this.key = 'waypoint-' + keyCounter\n    this.options = Waypoint.Adapter.extend({}, Waypoint.defaults, options)\n    this.element = this.options.element\n    this.adapter = new Waypoint.Adapter(this.element)\n    this.callback = options.handler\n    this.axis = this.options.horizontal ? 'horizontal' : 'vertical'\n    this.enabled = this.options.enabled\n    this.triggerPoint = null\n    this.group = Waypoint.Group.findOrCreate({\n      name: this.options.group,\n      axis: this.axis\n    })\n    this.context = Waypoint.Context.findOrCreateByElement(this.options.context)\n\n    if (Waypoint.offsetAliases[this.options.offset]) {\n      this.options.offset = Waypoint.offsetAliases[this.options.offset]\n    }\n    this.group.add(this)\n    this.context.add(this)\n    allWaypoints[this.key] = this\n    keyCounter += 1\n  }\n\n  /* Private */\n  Waypoint.prototype.queueTrigger = function(direction) {\n    this.group.queueTrigger(this, direction)\n  }\n\n  /* Private */\n  Waypoint.prototype.trigger = function(args) {\n    if (!this.enabled) {\n      return\n    }\n    if (this.callback) {\n      this.callback.apply(this, args)\n    }\n  }\n\n  /* Public */\n  /* http://imakewebthings.com/waypoints/api/destroy */\n  Waypoint.prototype.destroy = function() {\n    this.context.remove(this)\n    this.group.remove(this)\n    delete allWaypoints[this.key]\n  }\n\n  /* Public */\n  /* http://imakewebthings.com/waypoints/api/disable */\n  Waypoint.prototype.disable = function() {\n    this.enabled = false\n    return this\n  }\n\n  /* Public */\n  /* http://imakewebthings.com/waypoints/api/enable */\n  Waypoint.prototype.enable = function() {\n    this.context.refresh()\n    this.enabled = true\n    return this\n  }\n\n  /* Public */\n  /* http://imakewebthings.com/waypoints/api/next */\n  Waypoint.prototype.next = function() {\n    return this.group.next(this)\n  }\n\n  /* Public */\n  /* http://imakewebthings.com/waypoints/api/previous */\n  Waypoint.prototype.previous = function() {\n    return this.group.previous(this)\n  }\n\n  /* Private */\n  Waypoint.invokeAll = function(method) {\n    var allWaypointsArray = []\n    for (var waypointKey in allWaypoints) {\n      allWaypointsArray.push(allWaypoints[waypointKey])\n    }\n    for (var i = 0, end = allWaypointsArray.length; i < end; i++) {\n      allWaypointsArray[i][method]()\n    }\n  }\n\n  /* Public */\n  /* http://imakewebthings.com/waypoints/api/destroy-all */\n  Waypoint.destroyAll = function() {\n    Waypoint.invokeAll('destroy')\n  }\n\n  /* Public */\n  /* http://imakewebthings.com/waypoints/api/disable-all */\n  Waypoint.disableAll = function() {\n    Waypoint.invokeAll('disable')\n  }\n\n  /* Public */\n  /* http://imakewebthings.com/waypoints/api/enable-all */\n  Waypoint.enableAll = function() {\n    Waypoint.Context.refreshAll()\n    for (var waypointKey in allWaypoints) {\n      allWaypoints[waypointKey].enabled = true\n    }\n    return this\n  }\n\n  /* Public */\n  /* http://imakewebthings.com/waypoints/api/refresh-all */\n  Waypoint.refreshAll = function() {\n    Waypoint.Context.refreshAll()\n  }\n\n  /* Public */\n  /* http://imakewebthings.com/waypoints/api/viewport-height */\n  Waypoint.viewportHeight = function() {\n    return window.innerHeight || document.documentElement.clientHeight\n  }\n\n  /* Public */\n  /* http://imakewebthings.com/waypoints/api/viewport-width */\n  Waypoint.viewportWidth = function() {\n    return document.documentElement.clientWidth\n  }\n\n  Waypoint.adapters = []\n\n  Waypoint.defaults = {\n    context: window,\n    continuous: true,\n    enabled: true,\n    group: 'default',\n    horizontal: false,\n    offset: 0\n  }\n\n  Waypoint.offsetAliases = {\n    'bottom-in-view': function() {\n      return this.context.innerHeight() - this.adapter.outerHeight()\n    },\n    'right-in-view': function() {\n      return this.context.innerWidth() - this.adapter.outerWidth()\n    }\n  }\n\n  window.Waypoint = Waypoint\n}())\n;(function() {\n  'use strict'\n\n  function requestAnimationFrameShim(callback) {\n    window.setTimeout(callback, 1000 / 60)\n  }\n\n  var keyCounter = 0\n  var contexts = {}\n  var Waypoint = window.Waypoint\n  var oldWindowLoad = window.onload\n\n  /* http://imakewebthings.com/waypoints/api/context */\n  function Context(element) {\n    this.element = element\n    this.Adapter = Waypoint.Adapter\n    this.adapter = new this.Adapter(element)\n    this.key = 'waypoint-context-' + keyCounter\n    this.didScroll = false\n    this.didResize = false\n    this.oldScroll = {\n      x: this.adapter.scrollLeft(),\n      y: this.adapter.scrollTop()\n    }\n    this.waypoints = {\n      vertical: {},\n      horizontal: {}\n    }\n\n    element.waypointContextKey = this.key\n    contexts[element.waypointContextKey] = this\n    keyCounter += 1\n    if (!Waypoint.windowContext) {\n      Waypoint.windowContext = true\n      Waypoint.windowContext = new Context(window)\n    }\n\n    this.createThrottledScrollHandler()\n    this.createThrottledResizeHandler()\n  }\n\n  /* Private */\n  Context.prototype.add = function(waypoint) {\n    var axis = waypoint.options.horizontal ? 'horizontal' : 'vertical'\n    this.waypoints[axis][waypoint.key] = waypoint\n    this.refresh()\n  }\n\n  /* Private */\n  Context.prototype.checkEmpty = function() {\n    var horizontalEmpty = this.Adapter.isEmptyObject(this.waypoints.horizontal)\n    var verticalEmpty = this.Adapter.isEmptyObject(this.waypoints.vertical)\n    var isWindow = this.element == this.element.window\n    if (horizontalEmpty && verticalEmpty && !isWindow) {\n      this.adapter.off('.waypoints')\n      delete contexts[this.key]\n    }\n  }\n\n  /* Private */\n  Context.prototype.createThrottledResizeHandler = function() {\n    var self = this\n\n    function resizeHandler() {\n      self.handleResize()\n      self.didResize = false\n    }\n\n    this.adapter.on('resize.waypoints', function() {\n      if (!self.didResize) {\n        self.didResize = true\n        Waypoint.requestAnimationFrame(resizeHandler)\n      }\n    })\n  }\n\n  /* Private */\n  Context.prototype.createThrottledScrollHandler = function() {\n    var self = this\n    function scrollHandler() {\n      self.handleScroll()\n      self.didScroll = false\n    }\n\n    this.adapter.on('scroll.waypoints', function() {\n      if (!self.didScroll || Waypoint.isTouch) {\n        self.didScroll = true\n        Waypoint.requestAnimationFrame(scrollHandler)\n      }\n    })\n  }\n\n  /* Private */\n  Context.prototype.handleResize = function() {\n    Waypoint.Context.refreshAll()\n  }\n\n  /* Private */\n  Context.prototype.handleScroll = function() {\n    var triggeredGroups = {}\n    var axes = {\n      horizontal: {\n        newScroll: this.adapter.scrollLeft(),\n        oldScroll: this.oldScroll.x,\n        forward: 'right',\n        backward: 'left'\n      },\n      vertical: {\n        newScroll: this.adapter.scrollTop(),\n        oldScroll: this.oldScroll.y,\n        forward: 'down',\n        backward: 'up'\n      }\n    }\n\n    for (var axisKey in axes) {\n      var axis = axes[axisKey]\n      var isForward = axis.newScroll > axis.oldScroll\n      var direction = isForward ? axis.forward : axis.backward\n\n      for (var waypointKey in this.waypoints[axisKey]) {\n        var waypoint = this.waypoints[axisKey][waypointKey]\n        if (waypoint.triggerPoint === null) {\n          continue\n        }\n        var wasBeforeTriggerPoint = axis.oldScroll < waypoint.triggerPoint\n        var nowAfterTriggerPoint = axis.newScroll >= waypoint.triggerPoint\n        var crossedForward = wasBeforeTriggerPoint && nowAfterTriggerPoint\n        var crossedBackward = !wasBeforeTriggerPoint && !nowAfterTriggerPoint\n        if (crossedForward || crossedBackward) {\n          waypoint.queueTrigger(direction)\n          triggeredGroups[waypoint.group.id] = waypoint.group\n        }\n      }\n    }\n\n    for (var groupKey in triggeredGroups) {\n      triggeredGroups[groupKey].flushTriggers()\n    }\n\n    this.oldScroll = {\n      x: axes.horizontal.newScroll,\n      y: axes.vertical.newScroll\n    }\n  }\n\n  /* Private */\n  Context.prototype.innerHeight = function() {\n    /*eslint-disable eqeqeq */\n    if (this.element == this.element.window) {\n      return Waypoint.viewportHeight()\n    }\n    /*eslint-enable eqeqeq */\n    return this.adapter.innerHeight()\n  }\n\n  /* Private */\n  Context.prototype.remove = function(waypoint) {\n    delete this.waypoints[waypoint.axis][waypoint.key]\n    this.checkEmpty()\n  }\n\n  /* Private */\n  Context.prototype.innerWidth = function() {\n    /*eslint-disable eqeqeq */\n    if (this.element == this.element.window) {\n      return Waypoint.viewportWidth()\n    }\n    /*eslint-enable eqeqeq */\n    return this.adapter.innerWidth()\n  }\n\n  /* Public */\n  /* http://imakewebthings.com/waypoints/api/context-destroy */\n  Context.prototype.destroy = function() {\n    var allWaypoints = []\n    for (var axis in this.waypoints) {\n      for (var waypointKey in this.waypoints[axis]) {\n        allWaypoints.push(this.waypoints[axis][waypointKey])\n      }\n    }\n    for (var i = 0, end = allWaypoints.length; i < end; i++) {\n      allWaypoints[i].destroy()\n    }\n  }\n\n  /* Public */\n  /* http://imakewebthings.com/waypoints/api/context-refresh */\n  Context.prototype.refresh = function() {\n    /*eslint-disable eqeqeq */\n    var isWindow = this.element == this.element.window\n    /*eslint-enable eqeqeq */\n    var contextOffset = isWindow ? undefined : this.adapter.offset()\n    var triggeredGroups = {}\n    var axes\n\n    this.handleScroll()\n    axes = {\n      horizontal: {\n        contextOffset: isWindow ? 0 : contextOffset.left,\n        contextScroll: isWindow ? 0 : this.oldScroll.x,\n        contextDimension: this.innerWidth(),\n        oldScroll: this.oldScroll.x,\n        forward: 'right',\n        backward: 'left',\n        offsetProp: 'left'\n      },\n      vertical: {\n        contextOffset: isWindow ? 0 : contextOffset.top,\n        contextScroll: isWindow ? 0 : this.oldScroll.y,\n        contextDimension: this.innerHeight(),\n        oldScroll: this.oldScroll.y,\n        forward: 'down',\n        backward: 'up',\n        offsetProp: 'top'\n      }\n    }\n\n    for (var axisKey in axes) {\n      var axis = axes[axisKey]\n      for (var waypointKey in this.waypoints[axisKey]) {\n        var waypoint = this.waypoints[axisKey][waypointKey]\n        var adjustment = waypoint.options.offset\n        var oldTriggerPoint = waypoint.triggerPoint\n        var elementOffset = 0\n        var freshWaypoint = oldTriggerPoint == null\n        var contextModifier, wasBeforeScroll, nowAfterScroll\n        var triggeredBackward, triggeredForward\n\n        if (waypoint.element !== waypoint.element.window) {\n          elementOffset = waypoint.adapter.offset()[axis.offsetProp]\n        }\n\n        if (typeof adjustment === 'function') {\n          adjustment = adjustment.apply(waypoint)\n        }\n        else if (typeof adjustment === 'string') {\n          adjustment = parseFloat(adjustment)\n          if (waypoint.options.offset.indexOf('%') > - 1) {\n            adjustment = Math.ceil(axis.contextDimension * adjustment / 100)\n          }\n        }\n\n        contextModifier = axis.contextScroll - axis.contextOffset\n        waypoint.triggerPoint = Math.floor(elementOffset + contextModifier - adjustment)\n        wasBeforeScroll = oldTriggerPoint < axis.oldScroll\n        nowAfterScroll = waypoint.triggerPoint >= axis.oldScroll\n        triggeredBackward = wasBeforeScroll && nowAfterScroll\n        triggeredForward = !wasBeforeScroll && !nowAfterScroll\n\n        if (!freshWaypoint && triggeredBackward) {\n          waypoint.queueTrigger(axis.backward)\n          triggeredGroups[waypoint.group.id] = waypoint.group\n        }\n        else if (!freshWaypoint && triggeredForward) {\n          waypoint.queueTrigger(axis.forward)\n          triggeredGroups[waypoint.group.id] = waypoint.group\n        }\n        else if (freshWaypoint && axis.oldScroll >= waypoint.triggerPoint) {\n          waypoint.queueTrigger(axis.forward)\n          triggeredGroups[waypoint.group.id] = waypoint.group\n        }\n      }\n    }\n\n    Waypoint.requestAnimationFrame(function() {\n      for (var groupKey in triggeredGroups) {\n        triggeredGroups[groupKey].flushTriggers()\n      }\n    })\n\n    return this\n  }\n\n  /* Private */\n  Context.findOrCreateByElement = function(element) {\n    return Context.findByElement(element) || new Context(element)\n  }\n\n  /* Private */\n  Context.refreshAll = function() {\n    for (var contextId in contexts) {\n      contexts[contextId].refresh()\n    }\n  }\n\n  /* Public */\n  /* http://imakewebthings.com/waypoints/api/context-find-by-element */\n  Context.findByElement = function(element) {\n    return contexts[element.waypointContextKey]\n  }\n\n  window.onload = function() {\n    if (oldWindowLoad) {\n      oldWindowLoad()\n    }\n    Context.refreshAll()\n  }\n\n\n  Waypoint.requestAnimationFrame = function(callback) {\n    var requestFn = window.requestAnimationFrame ||\n      window.mozRequestAnimationFrame ||\n      window.webkitRequestAnimationFrame ||\n      requestAnimationFrameShim\n    requestFn.call(window, callback)\n  }\n  Waypoint.Context = Context\n}())\n;(function() {\n  'use strict'\n\n  function byTriggerPoint(a, b) {\n    return a.triggerPoint - b.triggerPoint\n  }\n\n  function byReverseTriggerPoint(a, b) {\n    return b.triggerPoint - a.triggerPoint\n  }\n\n  var groups = {\n    vertical: {},\n    horizontal: {}\n  }\n  var Waypoint = window.Waypoint\n\n  /* http://imakewebthings.com/waypoints/api/group */\n  function Group(options) {\n    this.name = options.name\n    this.axis = options.axis\n    this.id = this.name + '-' + this.axis\n    this.waypoints = []\n    this.clearTriggerQueues()\n    groups[this.axis][this.name] = this\n  }\n\n  /* Private */\n  Group.prototype.add = function(waypoint) {\n    this.waypoints.push(waypoint)\n  }\n\n  /* Private */\n  Group.prototype.clearTriggerQueues = function() {\n    this.triggerQueues = {\n      up: [],\n      down: [],\n      left: [],\n      right: []\n    }\n  }\n\n  /* Private */\n  Group.prototype.flushTriggers = function() {\n    for (var direction in this.triggerQueues) {\n      var waypoints = this.triggerQueues[direction]\n      var reverse = direction === 'up' || direction === 'left'\n      waypoints.sort(reverse ? byReverseTriggerPoint : byTriggerPoint)\n      for (var i = 0, end = waypoints.length; i < end; i += 1) {\n        var waypoint = waypoints[i]\n        if (waypoint.options.continuous || i === waypoints.length - 1) {\n          waypoint.trigger([direction])\n        }\n      }\n    }\n    this.clearTriggerQueues()\n  }\n\n  /* Private */\n  Group.prototype.next = function(waypoint) {\n    this.waypoints.sort(byTriggerPoint)\n    var index = Waypoint.Adapter.inArray(waypoint, this.waypoints)\n    var isLast = index === this.waypoints.length - 1\n    return isLast ? null : this.waypoints[index + 1]\n  }\n\n  /* Private */\n  Group.prototype.previous = function(waypoint) {\n    this.waypoints.sort(byTriggerPoint)\n    var index = Waypoint.Adapter.inArray(waypoint, this.waypoints)\n    return index ? this.waypoints[index - 1] : null\n  }\n\n  /* Private */\n  Group.prototype.queueTrigger = function(waypoint, direction) {\n    this.triggerQueues[direction].push(waypoint)\n  }\n\n  /* Private */\n  Group.prototype.remove = function(waypoint) {\n    var index = Waypoint.Adapter.inArray(waypoint, this.waypoints)\n    if (index > -1) {\n      this.waypoints.splice(index, 1)\n    }\n  }\n\n  /* Public */\n  /* http://imakewebthings.com/waypoints/api/first */\n  Group.prototype.first = function() {\n    return this.waypoints[0]\n  }\n\n  /* Public */\n  /* http://imakewebthings.com/waypoints/api/last */\n  Group.prototype.last = function() {\n    return this.waypoints[this.waypoints.length - 1]\n  }\n\n  /* Private */\n  Group.findOrCreate = function(options) {\n    return groups[options.axis][options.name] || new Group(options)\n  }\n\n  Waypoint.Group = Group\n}())\n;(function() {\n  'use strict'\n\n  var $ = window.jQuery\n  var Waypoint = window.Waypoint\n\n  function JQueryAdapter(element) {\n    this.$element = $(element)\n  }\n\n  $.each([\n    'innerHeight',\n    'innerWidth',\n    'off',\n    'offset',\n    'on',\n    'outerHeight',\n    'outerWidth',\n    'scrollLeft',\n    'scrollTop'\n  ], function(i, method) {\n    JQueryAdapter.prototype[method] = function() {\n      var args = Array.prototype.slice.call(arguments)\n      return this.$element[method].apply(this.$element, args)\n    }\n  })\n\n  $.each([\n    'extend',\n    'inArray',\n    'isEmptyObject'\n  ], function(i, method) {\n    JQueryAdapter[method] = $[method]\n  })\n\n  Waypoint.adapters.push({\n    name: 'jquery',\n    Adapter: JQueryAdapter\n  })\n  Waypoint.Adapter = JQueryAdapter\n}())\n;(function() {\n  'use strict'\n\n  var Waypoint = window.Waypoint\n\n  function createExtension(framework) {\n    return function() {\n      var waypoints = []\n      var overrides = arguments[0]\n\n      if (framework.isFunction(arguments[0])) {\n        overrides = framework.extend({}, arguments[1])\n        overrides.handler = arguments[0]\n      }\n\n      this.each(function() {\n        var options = framework.extend({}, overrides, {\n          element: this\n        })\n        if (typeof options.context === 'string') {\n          options.context = framework(this).closest(options.context)[0]\n        }\n        waypoints.push(new Waypoint(options))\n      })\n\n      return waypoints\n    }\n  }\n\n  if (window.jQuery) {\n    window.jQuery.fn.waypoint = createExtension(window.jQuery)\n  }\n  if (window.Zepto) {\n    window.Zepto.fn.waypoint = createExtension(window.Zepto)\n  }\n}())\n;\n\n//# sourceURL=webpack:///./js/vendor/jquery.waypoints.js?")},429:function(){eval("/*!\n * modernizr v3.5.0\n * Build https://modernizr.com/download?-audio-flexbox-forcetouch-objectfit-pointerevents-svg-touchevents-setclasses-dontmin\n *\n * Copyright (c)\n *  Faruk Ates\n *  Paul Irish\n *  Alex Sexton\n *  Ryan Seddon\n *  Patrick Kettner\n *  Stu Cox\n *  Richard Herrera\n\n * MIT License\n */\n\n/*\n * Modernizr tests which native CSS3 and HTML5 features are available in the\n * current UA and makes the results available to you in two ways: as properties on\n * a global `Modernizr` object, and as classes on the `<html>` element. This\n * information allows you to progressively enhance your pages with a granular level\n * of control over the experience.\n*/\n\n;(function(window, document, undefined){\n  var classes = [];\n  \n\n  var tests = [];\n  \n\n  /**\n   *\n   * ModernizrProto is the constructor for Modernizr\n   *\n   * @class\n   * @access public\n   */\n\n  var ModernizrProto = {\n    // The current version, dummy\n    _version: '3.5.0',\n\n    // Any settings that don't work as separate modules\n    // can go in here as configuration.\n    _config: {\n      'classPrefix': '',\n      'enableClasses': true,\n      'enableJSClass': true,\n      'usePrefixes': true\n    },\n\n    // Queue of tests\n    _q: [],\n\n    // Stub these for people who are listening\n    on: function(test, cb) {\n      // I don't really think people should do this, but we can\n      // safe guard it a bit.\n      // -- NOTE:: this gets WAY overridden in src/addTest for actual async tests.\n      // This is in case people listen to synchronous tests. I would leave it out,\n      // but the code to *disallow* sync tests in the real version of this\n      // function is actually larger than this.\n      var self = this;\n      setTimeout(function() {\n        cb(self[test]);\n      }, 0);\n    },\n\n    addTest: function(name, fn, options) {\n      tests.push({name: name, fn: fn, options: options});\n    },\n\n    addAsyncTest: function(fn) {\n      tests.push({name: null, fn: fn});\n    }\n  };\n\n  \n\n  // Fake some of Object.create so we can force non test results to be non \"own\" properties.\n  var Modernizr = function() {};\n  Modernizr.prototype = ModernizrProto;\n\n  // Leak modernizr globally when you `require` it rather than force it here.\n  // Overwrite name so constructor name is nicer :D\n  Modernizr = new Modernizr();\n\n  \n/*!\n{\n  \"name\": \"SVG\",\n  \"property\": \"svg\",\n  \"caniuse\": \"svg\",\n  \"tags\": [\"svg\"],\n  \"authors\": [\"Erik Dahlstrom\"],\n  \"polyfills\": [\n    \"svgweb\",\n    \"raphael\",\n    \"amplesdk\",\n    \"canvg\",\n    \"svg-boilerplate\",\n    \"sie\",\n    \"dojogfx\",\n    \"fabricjs\"\n  ]\n}\n!*/\n/* DOC\nDetects support for SVG in `<embed>` or `<object>` elements.\n*/\n\n  Modernizr.addTest('svg', !!document.createElementNS && !!document.createElementNS('http://www.w3.org/2000/svg', 'svg').createSVGRect);\n\n\n  /**\n   * is returns a boolean if the typeof an obj is exactly type.\n   *\n   * @access private\n   * @function is\n   * @param {*} obj - A thing we want to check the type of\n   * @param {string} type - A string to compare the typeof against\n   * @returns {boolean}\n   */\n\n  function is(obj, type) {\n    return typeof obj === type;\n  }\n  ;\n\n  /**\n   * Run through all tests and detect their support in the current UA.\n   *\n   * @access private\n   */\n\n  function testRunner() {\n    var featureNames;\n    var feature;\n    var aliasIdx;\n    var result;\n    var nameIdx;\n    var featureName;\n    var featureNameSplit;\n\n    for (var featureIdx in tests) {\n      if (tests.hasOwnProperty(featureIdx)) {\n        featureNames = [];\n        feature = tests[featureIdx];\n        // run the test, throw the return value into the Modernizr,\n        // then based on that boolean, define an appropriate className\n        // and push it into an array of classes we'll join later.\n        //\n        // If there is no name, it's an 'async' test that is run,\n        // but not directly added to the object. That should\n        // be done with a post-run addTest call.\n        if (feature.name) {\n          featureNames.push(feature.name.toLowerCase());\n\n          if (feature.options && feature.options.aliases && feature.options.aliases.length) {\n            // Add all the aliases into the names list\n            for (aliasIdx = 0; aliasIdx < feature.options.aliases.length; aliasIdx++) {\n              featureNames.push(feature.options.aliases[aliasIdx].toLowerCase());\n            }\n          }\n        }\n\n        // Run the test, or use the raw value if it's not a function\n        result = is(feature.fn, 'function') ? feature.fn() : feature.fn;\n\n\n        // Set each of the names on the Modernizr object\n        for (nameIdx = 0; nameIdx < featureNames.length; nameIdx++) {\n          featureName = featureNames[nameIdx];\n          // Support dot properties as sub tests. We don't do checking to make sure\n          // that the implied parent tests have been added. You must call them in\n          // order (either in the test, or make the parent test a dependency).\n          //\n          // Cap it to TWO to make the logic simple and because who needs that kind of subtesting\n          // hashtag famous last words\n          featureNameSplit = featureName.split('.');\n\n          if (featureNameSplit.length === 1) {\n            Modernizr[featureNameSplit[0]] = result;\n          } else {\n            // cast to a Boolean, if not one already\n            if (Modernizr[featureNameSplit[0]] && !(Modernizr[featureNameSplit[0]] instanceof Boolean)) {\n              Modernizr[featureNameSplit[0]] = new Boolean(Modernizr[featureNameSplit[0]]);\n            }\n\n            Modernizr[featureNameSplit[0]][featureNameSplit[1]] = result;\n          }\n\n          classes.push((result ? '' : 'no-') + featureNameSplit.join('-'));\n        }\n      }\n    }\n  }\n  ;\n\n  /**\n   * docElement is a convenience wrapper to grab the root element of the document\n   *\n   * @access private\n   * @returns {HTMLElement|SVGElement} The root element of the document\n   */\n\n  var docElement = document.documentElement;\n  \n\n  /**\n   * A convenience helper to check if the document we are running in is an SVG document\n   *\n   * @access private\n   * @returns {boolean}\n   */\n\n  var isSVG = docElement.nodeName.toLowerCase() === 'svg';\n  \n\n  /**\n   * setClasses takes an array of class names and adds them to the root element\n   *\n   * @access private\n   * @function setClasses\n   * @param {string[]} classes - Array of class names\n   */\n\n  // Pass in an and array of class names, e.g.:\n  //  ['no-webp', 'borderradius', ...]\n  function setClasses(classes) {\n    var className = docElement.className;\n    var classPrefix = Modernizr._config.classPrefix || '';\n\n    if (isSVG) {\n      className = className.baseVal;\n    }\n\n    // Change `no-js` to `js` (independently of the `enableClasses` option)\n    // Handle classPrefix on this too\n    if (Modernizr._config.enableJSClass) {\n      var reJS = new RegExp('(^|\\\\s)' + classPrefix + 'no-js(\\\\s|$)');\n      className = className.replace(reJS, '$1' + classPrefix + 'js$2');\n    }\n\n    if (Modernizr._config.enableClasses) {\n      // Add the new classes\n      className += ' ' + classPrefix + classes.join(' ' + classPrefix);\n      if (isSVG) {\n        docElement.className.baseVal = className;\n      } else {\n        docElement.className = className;\n      }\n    }\n\n  }\n\n  ;\n\n  /**\n   * createElement is a convenience wrapper around document.createElement. Since we\n   * use createElement all over the place, this allows for (slightly) smaller code\n   * as well as abstracting away issues with creating elements in contexts other than\n   * HTML documents (e.g. SVG documents).\n   *\n   * @access private\n   * @function createElement\n   * @returns {HTMLElement|SVGElement} An HTML or SVG element\n   */\n\n  function createElement() {\n    if (typeof document.createElement !== 'function') {\n      // This is the case in IE7, where the type of createElement is \"object\".\n      // For this reason, we cannot call apply() as Object is not a Function.\n      return document.createElement(arguments[0]);\n    } else if (isSVG) {\n      return document.createElementNS.call(document, 'http://www.w3.org/2000/svg', arguments[0]);\n    } else {\n      return document.createElement.apply(document, arguments);\n    }\n  }\n\n  ;\n/*!\n{\n  \"name\" : \"HTML5 Audio Element\",\n  \"property\": \"audio\",\n  \"tags\" : [\"html5\", \"audio\", \"media\"]\n}\n!*/\n/* DOC\nDetects the audio element\n*/\n\n  // This tests evaluates support of the audio element, as well as\n  // testing what types of content it supports.\n  //\n  // We're using the Boolean constructor here, so that we can extend the value\n  // e.g.  Modernizr.audio     // true\n  //       Modernizr.audio.ogg // 'probably'\n  //\n  // Codec values from : github.com/NielsLeenheer/html5test/blob/9106a8/index.html#L845\n  //                     thx to NielsLeenheer and zcorpan\n\n  // Note: in some older browsers, \"no\" was a return value instead of empty string.\n  //   It was live in FF3.5.0 and 3.5.1, but fixed in 3.5.2\n  //   It was also live in Safari 4.0.0 - 4.0.4, but fixed in 4.0.5\n  Modernizr.addTest('audio', function() {\n    var elem = createElement('audio');\n    var bool = false;\n\n    try {\n      bool = !!elem.canPlayType\n      if (bool) {\n        bool      = new Boolean(bool);\n        bool.ogg  = elem.canPlayType('audio/ogg; codecs=\"vorbis\"') .replace(/^no$/, '');\n        bool.mp3  = elem.canPlayType('audio/mpeg; codecs=\"mp3\"')   .replace(/^no$/, '');\n        bool.opus  = elem.canPlayType('audio/ogg; codecs=\"opus\"')  ||\n                     elem.canPlayType('audio/webm; codecs=\"opus\"') .replace(/^no$/, '');\n\n        // Mimetypes accepted:\n        //   developer.mozilla.org/En/Media_formats_supported_by_the_audio_and_video_elements\n        //   bit.ly/iphoneoscodecs\n        bool.wav  = elem.canPlayType('audio/wav; codecs=\"1\"')     .replace(/^no$/, '');\n        bool.m4a  = (elem.canPlayType('audio/x-m4a;')            ||\n                     elem.canPlayType('audio/aac;'))             .replace(/^no$/, '');\n      }\n    } catch (e) { }\n\n    return bool;\n  });\n\n\n  /**\n   * Modernizr.hasEvent() detects support for a given event\n   *\n   * @memberof Modernizr\n   * @name Modernizr.hasEvent\n   * @optionName Modernizr.hasEvent()\n   * @optionProp hasEvent\n   * @access public\n   * @function hasEvent\n   * @param  {string|*} eventName - the name of an event to test for (e.g. \"resize\")\n   * @param  {Element|string} [element=HTMLDivElement] - is the element|document|window|tagName to test on\n   * @returns {boolean}\n   * @example\n   *  `Modernizr.hasEvent` lets you determine if the browser supports a supplied event.\n   *  By default, it does this detection on a div element\n   *\n   * ```js\n   *  hasEvent('blur') // true;\n   * ```\n   *\n   * However, you are able to give an object as a second argument to hasEvent to\n   * detect an event on something other than a div.\n   *\n   * ```js\n   *  hasEvent('devicelight', window) // true;\n   * ```\n   *\n   */\n\n  var hasEvent = (function() {\n\n    // Detect whether event support can be detected via `in`. Test on a DOM element\n    // using the \"blur\" event b/c it should always exist. bit.ly/event-detection\n    var needsFallback = !('onblur' in document.documentElement);\n\n    function inner(eventName, element) {\n\n      var isSupported;\n      if (!eventName) { return false; }\n      if (!element || typeof element === 'string') {\n        element = createElement(element || 'div');\n      }\n\n      // Testing via the `in` operator is sufficient for modern browsers and IE.\n      // When using `setAttribute`, IE skips \"unload\", WebKit skips \"unload\" and\n      // \"resize\", whereas `in` \"catches\" those.\n      eventName = 'on' + eventName;\n      isSupported = eventName in element;\n\n      // Fallback technique for old Firefox - bit.ly/event-detection\n      if (!isSupported && needsFallback) {\n        if (!element.setAttribute) {\n          // Switch to generic element if it lacks `setAttribute`.\n          // It could be the `document`, `window`, or something else.\n          element = createElement('div');\n        }\n\n        element.setAttribute(eventName, '');\n        isSupported = typeof element[eventName] === 'function';\n\n        if (element[eventName] !== undefined) {\n          // If property was created, \"remove it\" by setting value to `undefined`.\n          element[eventName] = undefined;\n        }\n        element.removeAttribute(eventName);\n      }\n\n      return isSupported;\n    }\n    return inner;\n  })();\n\n\n  ModernizrProto.hasEvent = hasEvent;\n  \n\n  /**\n   * List of property values to set for css tests. See ticket #21\n   * http://git.io/vUGl4\n   *\n   * @memberof Modernizr\n   * @name Modernizr._prefixes\n   * @optionName Modernizr._prefixes\n   * @optionProp prefixes\n   * @access public\n   * @example\n   *\n   * Modernizr._prefixes is the internal list of prefixes that we test against\n   * inside of things like [prefixed](#modernizr-prefixed) and [prefixedCSS](#-code-modernizr-prefixedcss). It is simply\n   * an array of kebab-case vendor prefixes you can use within your code.\n   *\n   * Some common use cases include\n   *\n   * Generating all possible prefixed version of a CSS property\n   * ```js\n   * var rule = Modernizr._prefixes.join('transform: rotate(20deg); ');\n   *\n   * rule === 'transform: rotate(20deg); webkit-transform: rotate(20deg); moz-transform: rotate(20deg); o-transform: rotate(20deg); ms-transform: rotate(20deg);'\n   * ```\n   *\n   * Generating all possible prefixed version of a CSS value\n   * ```js\n   * rule = 'display:' +  Modernizr._prefixes.join('flex; display:') + 'flex';\n   *\n   * rule === 'display:flex; display:-webkit-flex; display:-moz-flex; display:-o-flex; display:-ms-flex; display:flex'\n   * ```\n   */\n\n  // we use ['',''] rather than an empty array in order to allow a pattern of .`join()`ing prefixes to test\n  // values in feature detects to continue to work\n  var prefixes = (ModernizrProto._config.usePrefixes ? ' -webkit- -moz- -o- -ms- '.split(' ') : ['','']);\n\n  // expose these for the plugin API. Look in the source for how to join() them against your input\n  ModernizrProto._prefixes = prefixes;\n\n  \n\n  /**\n   * cssToDOM takes a kebab-case string and converts it to camelCase\n   * e.g. box-sizing -> boxSizing\n   *\n   * @access private\n   * @function cssToDOM\n   * @param {string} name - String name of kebab-case prop we want to convert\n   * @returns {string} The camelCase version of the supplied name\n   */\n\n  function cssToDOM(name) {\n    return name.replace(/([a-z])-([a-z])/g, function(str, m1, m2) {\n      return m1 + m2.toUpperCase();\n    }).replace(/^-/, '');\n  }\n  ;\n\n  /**\n   * If the browsers follow the spec, then they would expose vendor-specific styles as:\n   *   elem.style.WebkitBorderRadius\n   * instead of something like the following (which is technically incorrect):\n   *   elem.style.webkitBorderRadius\n\n   * WebKit ghosts their properties in lowercase but Opera & Moz do not.\n   * Microsoft uses a lowercase `ms` instead of the correct `Ms` in IE8+\n   *   erik.eae.net/archives/2008/03/10/21.48.10/\n\n   * More here: github.com/Modernizr/Modernizr/issues/issue/21\n   *\n   * @access private\n   * @returns {string} The string representing the vendor-specific style properties\n   */\n\n  var omPrefixes = 'Moz O ms Webkit';\n  \n\n  /**\n   * List of JavaScript DOM values used for tests\n   *\n   * @memberof Modernizr\n   * @name Modernizr._domPrefixes\n   * @optionName Modernizr._domPrefixes\n   * @optionProp domPrefixes\n   * @access public\n   * @example\n   *\n   * Modernizr._domPrefixes is exactly the same as [_prefixes](#modernizr-_prefixes), but rather\n   * than kebab-case properties, all properties are their Capitalized variant\n   *\n   * ```js\n   * Modernizr._domPrefixes === [ \"Moz\", \"O\", \"ms\", \"Webkit\" ];\n   * ```\n   */\n\n  var domPrefixes = (ModernizrProto._config.usePrefixes ? omPrefixes.toLowerCase().split(' ') : []);\n  ModernizrProto._domPrefixes = domPrefixes;\n  \n/*!\n{\n  \"name\": \"DOM Pointer Events API\",\n  \"property\": \"pointerevents\",\n  \"tags\": [\"input\"],\n  \"authors\": [\"Stu Cox\"],\n  \"notes\": [\n    {\n      \"name\": \"W3C spec\",\n      \"href\": \"https://www.w3.org/TR/pointerevents/\"\n    }\n  ],\n  \"warnings\": [\"This property name now refers to W3C DOM PointerEvents: https://github.com/Modernizr/Modernizr/issues/548#issuecomment-12812099\"],\n  \"polyfills\": [\"handjs\",\"pep\"]\n}\n!*/\n/* DOC\nDetects support for the DOM Pointer Events API, which provides a unified event interface for pointing input devices, as implemented in IE10+.\n*/\n\n  // **Test name hijacked!**\n  // Now refers to W3C DOM PointerEvents spec rather than the CSS pointer-events property.\n  Modernizr.addTest('pointerevents', function() {\n    // Cannot use `.prefixed()` for events, so test each prefix\n    var bool = false,\n      i = domPrefixes.length;\n\n    // Don't forget un-prefixed...\n    bool = Modernizr.hasEvent('pointerdown');\n\n    while (i-- && !bool) {\n      if (hasEvent(domPrefixes[i] + 'pointerdown')) {\n        bool = true;\n      }\n    }\n    return bool;\n  });\n\n\n  var cssomPrefixes = (ModernizrProto._config.usePrefixes ? omPrefixes.split(' ') : []);\n  ModernizrProto._cssomPrefixes = cssomPrefixes;\n  \n\n  /**\n   * atRule returns a given CSS property at-rule (eg @keyframes), possibly in\n   * some prefixed form, or false, in the case of an unsupported rule\n   *\n   * @memberof Modernizr\n   * @name Modernizr.atRule\n   * @optionName Modernizr.atRule()\n   * @optionProp atRule\n   * @access public\n   * @function atRule\n   * @param {string} prop - String name of the @-rule to test for\n   * @returns {string|boolean} The string representing the (possibly prefixed)\n   * valid version of the @-rule, or `false` when it is unsupported.\n   * @example\n   * ```js\n   *  var keyframes = Modernizr.atRule('@keyframes');\n   *\n   *  if (keyframes) {\n   *    // keyframes are supported\n   *    // could be `@-webkit-keyframes` or `@keyframes`\n   *  } else {\n   *    // keyframes === `false`\n   *  }\n   * ```\n   *\n   */\n\n  var atRule = function(prop) {\n    var length = prefixes.length;\n    var cssrule = window.CSSRule;\n    var rule;\n\n    if (typeof cssrule === 'undefined') {\n      return undefined;\n    }\n\n    if (!prop) {\n      return false;\n    }\n\n    // remove literal @ from beginning of provided property\n    prop = prop.replace(/^@/, '');\n\n    // CSSRules use underscores instead of dashes\n    rule = prop.replace(/-/g, '_').toUpperCase() + '_RULE';\n\n    if (rule in cssrule) {\n      return '@' + prop;\n    }\n\n    for (var i = 0; i < length; i++) {\n      // prefixes gives us something like -o-, and we want O_\n      var prefix = prefixes[i];\n      var thisRule = prefix.toUpperCase() + '_' + rule;\n\n      if (thisRule in cssrule) {\n        return '@-' + prefix.toLowerCase() + '-' + prop;\n      }\n    }\n\n    return false;\n  };\n\n  ModernizrProto.atRule = atRule;\n\n  \n\n  /**\n   * getBody returns the body of a document, or an element that can stand in for\n   * the body if a real body does not exist\n   *\n   * @access private\n   * @function getBody\n   * @returns {HTMLElement|SVGElement} Returns the real body of a document, or an\n   * artificially created element that stands in for the body\n   */\n\n  function getBody() {\n    // After page load injecting a fake body doesn't work so check if body exists\n    var body = document.body;\n\n    if (!body) {\n      // Can't use the real body create a fake one.\n      body = createElement(isSVG ? 'svg' : 'body');\n      body.fake = true;\n    }\n\n    return body;\n  }\n\n  ;\n\n  /**\n   * injectElementWithStyles injects an element with style element and some CSS rules\n   *\n   * @access private\n   * @function injectElementWithStyles\n   * @param {string} rule - String representing a css rule\n   * @param {function} callback - A function that is used to test the injected element\n   * @param {number} [nodes] - An integer representing the number of additional nodes you want injected\n   * @param {string[]} [testnames] - An array of strings that are used as ids for the additional nodes\n   * @returns {boolean}\n   */\n\n  function injectElementWithStyles(rule, callback, nodes, testnames) {\n    var mod = 'modernizr';\n    var style;\n    var ret;\n    var node;\n    var docOverflow;\n    var div = createElement('div');\n    var body = getBody();\n\n    if (parseInt(nodes, 10)) {\n      // In order not to give false positives we create a node for each test\n      // This also allows the method to scale for unspecified uses\n      while (nodes--) {\n        node = createElement('div');\n        node.id = testnames ? testnames[nodes] : mod + (nodes + 1);\n        div.appendChild(node);\n      }\n    }\n\n    style = createElement('style');\n    style.type = 'text/css';\n    style.id = 's' + mod;\n\n    // IE6 will false positive on some tests due to the style element inside the test div somehow interfering offsetHeight, so insert it into body or fakebody.\n    // Opera will act all quirky when injecting elements in documentElement when page is served as xml, needs fakebody too. #270\n    (!body.fake ? div : body).appendChild(style);\n    body.appendChild(div);\n\n    if (style.styleSheet) {\n      style.styleSheet.cssText = rule;\n    } else {\n      style.appendChild(document.createTextNode(rule));\n    }\n    div.id = mod;\n\n    if (body.fake) {\n      //avoid crashing IE8, if background image is used\n      body.style.background = '';\n      //Safari 5.13/5.1.4 OSX stops loading if ::-webkit-scrollbar is used and scrollbars are visible\n      body.style.overflow = 'hidden';\n      docOverflow = docElement.style.overflow;\n      docElement.style.overflow = 'hidden';\n      docElement.appendChild(body);\n    }\n\n    ret = callback(div, rule);\n    // If this is done after page load we don't want to remove the body so check if body exists\n    if (body.fake) {\n      body.parentNode.removeChild(body);\n      docElement.style.overflow = docOverflow;\n      // Trigger layout so kinetic scrolling isn't disabled in iOS6+\n      // eslint-disable-next-line\n      docElement.offsetHeight;\n    } else {\n      div.parentNode.removeChild(div);\n    }\n\n    return !!ret;\n\n  }\n\n  ;\n\n  /**\n   * testStyles injects an element with style element and some CSS rules\n   *\n   * @memberof Modernizr\n   * @name Modernizr.testStyles\n   * @optionName Modernizr.testStyles()\n   * @optionProp testStyles\n   * @access public\n   * @function testStyles\n   * @param {string} rule - String representing a css rule\n   * @param {function} callback - A function that is used to test the injected element\n   * @param {number} [nodes] - An integer representing the number of additional nodes you want injected\n   * @param {string[]} [testnames] - An array of strings that are used as ids for the additional nodes\n   * @returns {boolean}\n   * @example\n   *\n   * `Modernizr.testStyles` takes a CSS rule and injects it onto the current page\n   * along with (possibly multiple) DOM elements. This lets you check for features\n   * that can not be detected by simply checking the [IDL](https://developer.mozilla.org/en-US/docs/Mozilla/Developer_guide/Interface_development_guide/IDL_interface_rules).\n   *\n   * ```js\n   * Modernizr.testStyles('#modernizr { width: 9px; color: papayawhip; }', function(elem, rule) {\n   *   // elem is the first DOM node in the page (by default #modernizr)\n   *   // rule is the first argument you supplied - the CSS rule in string form\n   *\n   *   addTest('widthworks', elem.style.width === '9px')\n   * });\n   * ```\n   *\n   * If your test requires multiple nodes, you can include a third argument\n   * indicating how many additional div elements to include on the page. The\n   * additional nodes are injected as children of the `elem` that is returned as\n   * the first argument to the callback.\n   *\n   * ```js\n   * Modernizr.testStyles('#modernizr {width: 1px}; #modernizr2 {width: 2px}', function(elem) {\n   *   document.getElementById('modernizr').style.width === '1px'; // true\n   *   document.getElementById('modernizr2').style.width === '2px'; // true\n   *   elem.firstChild === document.getElementById('modernizr2'); // true\n   * }, 1);\n   * ```\n   *\n   * By default, all of the additional elements have an ID of `modernizr[n]`, where\n   * `n` is its index (e.g. the first additional, second overall is `#modernizr2`,\n   * the second additional is `#modernizr3`, etc.).\n   * If you want to have more meaningful IDs for your function, you can provide\n   * them as the fourth argument, as an array of strings\n   *\n   * ```js\n   * Modernizr.testStyles('#foo {width: 10px}; #bar {height: 20px}', function(elem) {\n   *   elem.firstChild === document.getElementById('foo'); // true\n   *   elem.lastChild === document.getElementById('bar'); // true\n   * }, 2, ['foo', 'bar']);\n   * ```\n   *\n   */\n\n  var testStyles = ModernizrProto.testStyles = injectElementWithStyles;\n  \n/*!\n{\n  \"name\": \"Touch Events\",\n  \"property\": \"touchevents\",\n  \"caniuse\" : \"touch\",\n  \"tags\": [\"media\", \"attribute\"],\n  \"notes\": [{\n    \"name\": \"Touch Events spec\",\n    \"href\": \"https://www.w3.org/TR/2013/WD-touch-events-20130124/\"\n  }],\n  \"warnings\": [\n    \"Indicates if the browser supports the Touch Events spec, and does not necessarily reflect a touchscreen device\"\n  ],\n  \"knownBugs\": [\n    \"False-positive on some configurations of Nokia N900\",\n    \"False-positive on some BlackBerry 6.0 builds – https://github.com/Modernizr/Modernizr/issues/372#issuecomment-3112695\"\n  ]\n}\n!*/\n/* DOC\nIndicates if the browser supports the W3C Touch Events API.\n\nThis *does not* necessarily reflect a touchscreen device:\n\n* Older touchscreen devices only emulate mouse events\n* Modern IE touch devices implement the Pointer Events API instead: use `Modernizr.pointerevents` to detect support for that\n* Some browsers & OS setups may enable touch APIs when no touchscreen is connected\n* Future browsers may implement other event models for touch interactions\n\nSee this article: [You Can't Detect A Touchscreen](http://www.stucox.com/blog/you-cant-detect-a-touchscreen/).\n\nIt's recommended to bind both mouse and touch/pointer events simultaneously – see [this HTML5 Rocks tutorial](http://www.html5rocks.com/en/mobile/touchandmouse/).\n\nThis test will also return `true` for Firefox 4 Multitouch support.\n*/\n\n  // Chrome (desktop) used to lie about its support on this, but that has since been rectified: http://crbug.com/36415\n  Modernizr.addTest('touchevents', function() {\n    var bool;\n    if (('ontouchstart' in window) || window.DocumentTouch && document instanceof DocumentTouch) {\n      bool = true;\n    } else {\n      // include the 'heartz' as a way to have a non matching MQ to help terminate the join\n      // https://git.io/vznFH\n      var query = ['@media (', prefixes.join('touch-enabled),('), 'heartz', ')', '{#modernizr{top:9px;position:absolute}}'].join('');\n      testStyles(query, function(node) {\n        bool = node.offsetTop === 9;\n      });\n    }\n    return bool;\n  });\n\n\n\n  /**\n   * contains checks to see if a string contains another string\n   *\n   * @access private\n   * @function contains\n   * @param {string} str - The string we want to check for substrings\n   * @param {string} substr - The substring we want to search the first string for\n   * @returns {boolean}\n   */\n\n  function contains(str, substr) {\n    return !!~('' + str).indexOf(substr);\n  }\n\n  ;\n\n  /**\n   * fnBind is a super small [bind](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Function/bind) polyfill.\n   *\n   * @access private\n   * @function fnBind\n   * @param {function} fn - a function you want to change `this` reference to\n   * @param {object} that - the `this` you want to call the function with\n   * @returns {function} The wrapped version of the supplied function\n   */\n\n  function fnBind(fn, that) {\n    return function() {\n      return fn.apply(that, arguments);\n    };\n  }\n\n  ;\n\n  /**\n   * testDOMProps is a generic DOM property test; if a browser supports\n   *   a certain property, it won't return undefined for it.\n   *\n   * @access private\n   * @function testDOMProps\n   * @param {array.<string>} props - An array of properties to test for\n   * @param {object} obj - An object or Element you want to use to test the parameters again\n   * @param {boolean|object} elem - An Element to bind the property lookup again. Use `false` to prevent the check\n   * @returns {false|*} returns false if the prop is unsupported, otherwise the value that is supported\n   */\n  function testDOMProps(props, obj, elem) {\n    var item;\n\n    for (var i in props) {\n      if (props[i] in obj) {\n\n        // return the property name as a string\n        if (elem === false) {\n          return props[i];\n        }\n\n        item = obj[props[i]];\n\n        // let's bind a function\n        if (is(item, 'function')) {\n          // bind to obj unless overriden\n          return fnBind(item, elem || obj);\n        }\n\n        // return the unbound function or obj or value\n        return item;\n      }\n    }\n    return false;\n  }\n\n  ;\n\n\n  /**\n   * wrapper around getComputedStyle, to fix issues with Firefox returning null when\n   * called inside of a hidden iframe\n   *\n   * @access private\n   * @function computedStyle\n   * @param {HTMLElement|SVGElement} - The element we want to find the computed styles of\n   * @param {string|null} [pseudoSelector]- An optional pseudo element selector (e.g. :before), of null if none\n   * @returns {CSSStyleDeclaration}\n   */\n\n  function computedStyle(elem, pseudo, prop) {\n    var result;\n\n    if ('getComputedStyle' in window) {\n      result = getComputedStyle.call(window, elem, pseudo);\n      var console = window.console;\n\n      if (result !== null) {\n        if (prop) {\n          result = result.getPropertyValue(prop);\n        }\n      } else {\n        if (console) {\n          var method = console.error ? 'error' : 'log';\n          console[method].call(console, 'getComputedStyle returning null, its possible modernizr test results are inaccurate');\n        }\n      }\n    } else {\n      result = !pseudo && elem.currentStyle && elem.currentStyle[prop];\n    }\n\n    return result;\n  }\n\n  ;\n\n  /**\n   * domToCSS takes a camelCase string and converts it to kebab-case\n   * e.g. boxSizing -> box-sizing\n   *\n   * @access private\n   * @function domToCSS\n   * @param {string} name - String name of camelCase prop we want to convert\n   * @returns {string} The kebab-case version of the supplied name\n   */\n\n  function domToCSS(name) {\n    return name.replace(/([A-Z])/g, function(str, m1) {\n      return '-' + m1.toLowerCase();\n    }).replace(/^ms-/, '-ms-');\n  }\n  ;\n\n  /**\n   * nativeTestProps allows for us to use native feature detection functionality if available.\n   * some prefixed form, or false, in the case of an unsupported rule\n   *\n   * @access private\n   * @function nativeTestProps\n   * @param {array} props - An array of property names\n   * @param {string} value - A string representing the value we want to check via @supports\n   * @returns {boolean|undefined} A boolean when @supports exists, undefined otherwise\n   */\n\n  // Accepts a list of property names and a single value\n  // Returns `undefined` if native detection not available\n  function nativeTestProps(props, value) {\n    var i = props.length;\n    // Start with the JS API: http://www.w3.org/TR/css3-conditional/#the-css-interface\n    if ('CSS' in window && 'supports' in window.CSS) {\n      // Try every prefixed variant of the property\n      while (i--) {\n        if (window.CSS.supports(domToCSS(props[i]), value)) {\n          return true;\n        }\n      }\n      return false;\n    }\n    // Otherwise fall back to at-rule (for Opera 12.x)\n    else if ('CSSSupportsRule' in window) {\n      // Build a condition string for every prefixed variant\n      var conditionText = [];\n      while (i--) {\n        conditionText.push('(' + domToCSS(props[i]) + ':' + value + ')');\n      }\n      conditionText = conditionText.join(' or ');\n      return injectElementWithStyles('@supports (' + conditionText + ') { #modernizr { position: absolute; } }', function(node) {\n        return computedStyle(node, null, 'position') == 'absolute';\n      });\n    }\n    return undefined;\n  }\n  ;\n\n  /**\n   * Create our \"modernizr\" element that we do most feature tests on.\n   *\n   * @access private\n   */\n\n  var modElem = {\n    elem: createElement('modernizr')\n  };\n\n  // Clean up this element\n  Modernizr._q.push(function() {\n    delete modElem.elem;\n  });\n\n  \n\n  var mStyle = {\n    style: modElem.elem.style\n  };\n\n  // kill ref for gc, must happen before mod.elem is removed, so we unshift on to\n  // the front of the queue.\n  Modernizr._q.unshift(function() {\n    delete mStyle.style;\n  });\n\n  \n\n  // testProps is a generic CSS / DOM property test.\n\n  // In testing support for a given CSS property, it's legit to test:\n  //    `elem.style[styleName] !== undefined`\n  // If the property is supported it will return an empty string,\n  // if unsupported it will return undefined.\n\n  // We'll take advantage of this quick test and skip setting a style\n  // on our modernizr element, but instead just testing undefined vs\n  // empty string.\n\n  // Property names can be provided in either camelCase or kebab-case.\n\n  function testProps(props, prefixed, value, skipValueTest) {\n    skipValueTest = is(skipValueTest, 'undefined') ? false : skipValueTest;\n\n    // Try native detect first\n    if (!is(value, 'undefined')) {\n      var result = nativeTestProps(props, value);\n      if (!is(result, 'undefined')) {\n        return result;\n      }\n    }\n\n    // Otherwise do it properly\n    var afterInit, i, propsLength, prop, before;\n\n    // If we don't have a style element, that means we're running async or after\n    // the core tests, so we'll need to create our own elements to use\n\n    // inside of an SVG element, in certain browsers, the `style` element is only\n    // defined for valid tags. Therefore, if `modernizr` does not have one, we\n    // fall back to a less used element and hope for the best.\n    // for strict XHTML browsers the hardly used samp element is used\n    var elems = ['modernizr', 'tspan', 'samp'];\n    while (!mStyle.style && elems.length) {\n      afterInit = true;\n      mStyle.modElem = createElement(elems.shift());\n      mStyle.style = mStyle.modElem.style;\n    }\n\n    // Delete the objects if we created them.\n    function cleanElems() {\n      if (afterInit) {\n        delete mStyle.style;\n        delete mStyle.modElem;\n      }\n    }\n\n    propsLength = props.length;\n    for (i = 0; i < propsLength; i++) {\n      prop = props[i];\n      before = mStyle.style[prop];\n\n      if (contains(prop, '-')) {\n        prop = cssToDOM(prop);\n      }\n\n      if (mStyle.style[prop] !== undefined) {\n\n        // If value to test has been passed in, do a set-and-check test.\n        // 0 (integer) is a valid property value, so check that `value` isn't\n        // undefined, rather than just checking it's truthy.\n        if (!skipValueTest && !is(value, 'undefined')) {\n\n          // Needs a try catch block because of old IE. This is slow, but will\n          // be avoided in most cases because `skipValueTest` will be used.\n          try {\n            mStyle.style[prop] = value;\n          } catch (e) {}\n\n          // If the property value has changed, we assume the value used is\n          // supported. If `value` is empty string, it'll fail here (because\n          // it hasn't changed), which matches how browsers have implemented\n          // CSS.supports()\n          if (mStyle.style[prop] != before) {\n            cleanElems();\n            return prefixed == 'pfx' ? prop : true;\n          }\n        }\n        // Otherwise just return true, or the property name if this is a\n        // `prefixed()` call\n        else {\n          cleanElems();\n          return prefixed == 'pfx' ? prop : true;\n        }\n      }\n    }\n    cleanElems();\n    return false;\n  }\n\n  ;\n\n  /**\n   * testPropsAll tests a list of DOM properties we want to check against.\n   * We specify literally ALL possible (known and/or likely) properties on\n   * the element including the non-vendor prefixed one, for forward-\n   * compatibility.\n   *\n   * @access private\n   * @function testPropsAll\n   * @param {string} prop - A string of the property to test for\n   * @param {string|object} [prefixed] - An object to check the prefixed properties on. Use a string to skip\n   * @param {HTMLElement|SVGElement} [elem] - An element used to test the property and value against\n   * @param {string} [value] - A string of a css value\n   * @param {boolean} [skipValueTest] - An boolean representing if you want to test if value sticks when set\n   * @returns {false|string} returns the string version of the property, or false if it is unsupported\n   */\n  function testPropsAll(prop, prefixed, elem, value, skipValueTest) {\n\n    var ucProp = prop.charAt(0).toUpperCase() + prop.slice(1),\n      props = (prop + ' ' + cssomPrefixes.join(ucProp + ' ') + ucProp).split(' ');\n\n    // did they call .prefixed('boxSizing') or are we just testing a prop?\n    if (is(prefixed, 'string') || is(prefixed, 'undefined')) {\n      return testProps(props, prefixed, value, skipValueTest);\n\n      // otherwise, they called .prefixed('requestAnimationFrame', window[, elem])\n    } else {\n      props = (prop + ' ' + (domPrefixes).join(ucProp + ' ') + ucProp).split(' ');\n      return testDOMProps(props, prefixed, elem);\n    }\n  }\n\n  // Modernizr.testAllProps() investigates whether a given style property,\n  // or any of its vendor-prefixed variants, is recognized\n  //\n  // Note that the property names must be provided in the camelCase variant.\n  // Modernizr.testAllProps('boxSizing')\n  ModernizrProto.testAllProps = testPropsAll;\n\n  \n\n  /**\n   * prefixed returns the prefixed or nonprefixed property name variant of your input\n   *\n   * @memberof Modernizr\n   * @name Modernizr.prefixed\n   * @optionName Modernizr.prefixed()\n   * @optionProp prefixed\n   * @access public\n   * @function prefixed\n   * @param {string} prop - String name of the property to test for\n   * @param {object} [obj] - An object to test for the prefixed properties on\n   * @param {HTMLElement} [elem] - An element used to test specific properties against\n   * @returns {string|false} The string representing the (possibly prefixed) valid\n   * version of the property, or `false` when it is unsupported.\n   * @example\n   *\n   * Modernizr.prefixed takes a string css value in the DOM style camelCase (as\n   * opposed to the css style kebab-case) form and returns the (possibly prefixed)\n   * version of that property that the browser actually supports.\n   *\n   * For example, in older Firefox...\n   * ```js\n   * prefixed('boxSizing')\n   * ```\n   * returns 'MozBoxSizing'\n   *\n   * In newer Firefox, as well as any other browser that support the unprefixed\n   * version would simply return `boxSizing`. Any browser that does not support\n   * the property at all, it will return `false`.\n   *\n   * By default, prefixed is checked against a DOM element. If you want to check\n   * for a property on another object, just pass it as a second argument\n   *\n   * ```js\n   * var rAF = prefixed('requestAnimationFrame', window);\n   *\n   * raf(function() {\n   *  renderFunction();\n   * })\n   * ```\n   *\n   * Note that this will return _the actual function_ - not the name of the function.\n   * If you need the actual name of the property, pass in `false` as a third argument\n   *\n   * ```js\n   * var rAFProp = prefixed('requestAnimationFrame', window, false);\n   *\n   * rafProp === 'WebkitRequestAnimationFrame' // in older webkit\n   * ```\n   *\n   * One common use case for prefixed is if you're trying to determine which transition\n   * end event to bind to, you might do something like...\n   * ```js\n   * var transEndEventNames = {\n   *     'WebkitTransition' : 'webkitTransitionEnd', * Saf 6, Android Browser\n   *     'MozTransition'    : 'transitionend',       * only for FF < 15\n   *     'transition'       : 'transitionend'        * IE10, Opera, Chrome, FF 15+, Saf 7+\n   * };\n   *\n   * var transEndEventName = transEndEventNames[ Modernizr.prefixed('transition') ];\n   * ```\n   *\n   * If you want a similar lookup, but in kebab-case, you can use [prefixedCSS](#modernizr-prefixedcss).\n   */\n\n  var prefixed = ModernizrProto.prefixed = function(prop, obj, elem) {\n    if (prop.indexOf('@') === 0) {\n      return atRule(prop);\n    }\n\n    if (prop.indexOf('-') != -1) {\n      // Convert kebab-case to camelCase\n      prop = cssToDOM(prop);\n    }\n    if (!obj) {\n      return testPropsAll(prop, 'pfx');\n    } else {\n      // Testing DOM property e.g. Modernizr.prefixed('requestAnimationFrame', window) // 'mozRequestAnimationFrame'\n      return testPropsAll(prop, obj, elem);\n    }\n  };\n\n  \n/*!\n{\n  \"name\": \"Force Touch Events\",\n  \"property\": \"forcetouch\",\n  \"authors\": [\"Kraig Walker\"],\n  \"notes\": [{\n    \"name\": \"Responding to Force Touch Events from JavaScript\",\n    \"href\": \"https://developer.apple.com/library/prerelease/mac/documentation/AppleApplications/Conceptual/SafariJSProgTopics/Articles/RespondingtoForceTouchEventsfromJavaScript.html\"\n  }]\n}\n!*/\n/* DOC\nTests whether the browser supports the detection of Force Touch Events.\nForce Touch Events allow custom behaviours and interactions to take place based on the given pressure or change in pressure from a compatible trackpad.\n\nForce Touch events are available in OS X 10.11 and later on devices equipped with Force Touch trackpads.\n*/\n\n  Modernizr.addTest('forcetouch', function() {\n\t\t// github.com/Modernizr/Modernizr/issues/1613\n    // Test if the browser supports the force touch event progression (see notes link)\n    if (!hasEvent(prefixed('mouseforcewillbegin', window, false), window)) {\n      return false;\n    }\n\n    // Test if the browser provides thresholds defining a \"force touch\" from a normal touch/click event\n    return MouseEvent.WEBKIT_FORCE_AT_MOUSE_DOWN && MouseEvent.WEBKIT_FORCE_AT_FORCE_MOUSE_DOWN;\n  });\n\n/*!\n{\n  \"name\": \"CSS Object Fit\",\n  \"caniuse\": \"object-fit\",\n  \"property\": \"objectfit\",\n  \"tags\": [\"css\"],\n  \"builderAliases\": [\"css_objectfit\"],\n  \"notes\": [{\n    \"name\": \"Opera Article on Object Fit\",\n    \"href\": \"https://dev.opera.com/articles/css3-object-fit-object-position/\"\n  }]\n}\n!*/\n\n  Modernizr.addTest('objectfit', !!prefixed('objectFit'), {aliases: ['object-fit']});\n\n\n  /**\n   * testAllProps determines whether a given CSS property is supported in the browser\n   *\n   * @memberof Modernizr\n   * @name Modernizr.testAllProps\n   * @optionName Modernizr.testAllProps()\n   * @optionProp testAllProps\n   * @access public\n   * @function testAllProps\n   * @param {string} prop - String naming the property to test (either camelCase or kebab-case)\n   * @param {string} [value] - String of the value to test\n   * @param {boolean} [skipValueTest=false] - Whether to skip testing that the value is supported when using non-native detection\n   * @example\n   *\n   * testAllProps determines whether a given CSS property, in some prefixed form,\n   * is supported by the browser.\n   *\n   * ```js\n   * testAllProps('boxSizing')  // true\n   * ```\n   *\n   * It can optionally be given a CSS value in string form to test if a property\n   * value is valid\n   *\n   * ```js\n   * testAllProps('display', 'block') // true\n   * testAllProps('display', 'penguin') // false\n   * ```\n   *\n   * A boolean can be passed as a third parameter to skip the value check when\n   * native detection (@supports) isn't available.\n   *\n   * ```js\n   * testAllProps('shapeOutside', 'content-box', true);\n   * ```\n   */\n\n  function testAllProps(prop, value, skipValueTest) {\n    return testPropsAll(prop, undefined, undefined, value, skipValueTest);\n  }\n  ModernizrProto.testAllProps = testAllProps;\n  \n/*!\n{\n  \"name\": \"Flexbox\",\n  \"property\": \"flexbox\",\n  \"caniuse\": \"flexbox\",\n  \"tags\": [\"css\"],\n  \"notes\": [{\n    \"name\": \"The _new_ flexbox\",\n    \"href\": \"http://dev.w3.org/csswg/css3-flexbox\"\n  }],\n  \"warnings\": [\n    \"A `true` result for this detect does not imply that the `flex-wrap` property is supported; see the `flexwrap` detect.\"\n  ]\n}\n!*/\n/* DOC\nDetects support for the Flexible Box Layout model, a.k.a. Flexbox, which allows easy manipulation of layout order and sizing within a container.\n*/\n\n  Modernizr.addTest('flexbox', testAllProps('flexBasis', '1px', true));\n\n\n  // Run each test\n  testRunner();\n\n  // Remove the \"no-js\" class if it exists\n  setClasses(classes);\n\n  delete ModernizrProto.addTest;\n  delete ModernizrProto.addAsyncTest;\n\n  // Run the things that are supposed to run after the tests\n  for (var i = 0; i < Modernizr._q.length; i++) {\n    Modernizr._q[i]();\n  }\n\n  // Leak Modernizr namespace\n  window.Modernizr = Modernizr;\n\n\n;\n\n})(window, document);\n\n//# sourceURL=webpack:///./js/vendor/modernizr.custom.80802.js?")}},__webpack_module_cache__={};function __webpack_require__(n){var e=__webpack_module_cache__[n];if(void 0!==e)return e.exports;var t=__webpack_module_cache__[n]={exports:{}};return __webpack_modules__[n].call(t.exports,t,t.exports,__webpack_require__),t.exports}__webpack_require__.n=function(n){var e=n&&n.__esModule?function(){return n.default}:function(){return n};return __webpack_require__.d(e,{a:e}),e},__webpack_require__.d=function(n,e){for(var t in e)__webpack_require__.o(e,t)&&!__webpack_require__.o(n,t)&&Object.defineProperty(n,t,{enumerable:!0,get:e[t]})},__webpack_require__.o=function(n,e){return Object.prototype.hasOwnProperty.call(n,e)},__webpack_require__.r=function(n){"undefined"!=typeof Symbol&&Symbol.toStringTag&&Object.defineProperty(n,Symbol.toStringTag,{value:"Module"}),Object.defineProperty(n,"__esModule",{value:!0})};var __webpack_exports__=__webpack_require__(417)}();